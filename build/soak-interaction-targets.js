/** Shared fixed control identities for the soak's measured pointer interaction. */
export const SOAK_INTERACTION_TARGETS = Object.freeze({
  open: 'open-devices-workspace',
  close: 'workspace-close-btn',
})
