# HANDOFF.md — Live Baton

> Read this before doing any work. Keep it short. This is the baton, not the project diary.

## Last Updated

- 2026-06-04 by Codex — `DON-84` completed from the 3-6-26 team feedback intake. The ODT reported that clicking Offline/time/source/zoom in the Device List made the map lose focus and showed the Marker list. Current local `master` and the deployed hosted app no longer reproduce this: status, last-seen, source, and zoom clicks keep Devices open, leave the Tracking tab active, and do not show Marker At GR/marker tools. Root cause was already addressed by the earlier Device Workspace propagation fix (`DON-63`): passive row cells and row controls stop row/backdrop propagation, while Zoom intentionally targets the selected device without switching sidebar tabs. Added explicit Chromium E2E coverage for this exact ODT flow. Verification passed: local Playwright probe on `http://127.0.0.1:1420/?missionHarness=1`; deployed Playwright probe on `https://sartracker-web.vercel.app/?missionHarness=1`; `npx playwright test tests/e2e/devices-workspace.spec.ts --project=chromium` (3 passed); `npm run test` (138 files / 694 tests); `npm run lint`; `npm run build`; `npm run test:backend` (46 passed / 1 ignored); first full standard E2E run had one unrelated transient `v1-regression` seed/navigation failure, exact spec rerun passed (4 passed), and the second full standard E2E run passed (`npx playwright test --project=chromium`, 97 passed).
- 2026-06-04 by Codex — `DON-86` completed from the 3-6-26 team feedback intake. Root cause: the DD/DMS converter treated the first field as a single coordinate when the second field was empty, so pasted latitude-longitude pairs such as `52.004677 -9.748060` fell through to a misleading `Latitude must be a finite number` error and DMS pairs were rejected as malformed latitude values. The converter now accepts split-field DD/DMS input plus pasted DD pairs in the first field (`52.004677 -9.748060`, comma-separated pairs, and hemisphere-suffixed pairs such as `52.004677°N 9.748060°W`), accepts pasted DMS pairs in the first DMS field, validates direction/range, and gives clear incomplete-pair instructions. Operator manual updated for pasted coordinate pairs. Verification passed: red→green `npx vitest run tests/unit/coordinate-tool.test.ts`; `npx vitest run tests/unit/coordinate-tool.test.ts tests/unit/coordinate-converter-dialog.test.ts` (2 files / 13 tests); focused Playwright `npx playwright test tests/e2e/coordinate-converter.spec.ts --project=chromium` (5 passed); `npm run test` (138 files / 694 tests); `npm run lint`; `npm run build`; `npm run test:backend` (46 passed / 1 ignored); full standard E2E `npx playwright test --project=chromium` (96 passed). Inbuilt Browser was attempted for a live local check but CDP navigation timed out; per browser safety policy, Playwright is the browser-backed validation evidence for this slice.
- 2026-06-04 by Codex — `DON-87`, `DON-88`, `DON-89`, and `DON-90` completed from the 3-6-26 team feedback intake. `DON-89` root cause was weather-link rows keyed by editable name/URL text, remounting the inputs and dropping focus after each character; rows now use a stable index key so normal typing/paste/editing works. `DON-87` root cause was `Go To Location` closing the Coordinate Converter; it now queues the map target while keeping the conversion result open so `Create Marker Here` remains available and opens the normal marker form. `DON-88` Search Sector radius field and `DON-90` bare weather domains were already on safe paths but now have explicit regression/UI coverage. Operator manual updated for the post-Go-To marker workflow. Verification passed: red→green focused unit tests for settings focus and coordinate post-Go-To marker creation; `npx vitest run tests/unit/settings-workspace.test.ts tests/unit/coordinate-converter-dialog.test.ts tests/unit/drawing-dialog.test.ts tests/unit/settings-validation.test.ts tests/unit/open-external-url.test.ts` (5 files / 29 tests); `npm run test` (138 files / 689 tests); `npm run lint`; `npm run build`; `npm run test:backend` (46 passed / 1 ignored); focused Playwright `npx playwright test tests/e2e/weather.spec.ts tests/e2e/coordinate-converter.spec.ts tests/e2e/drawing-tools.spec.ts --project=chromium` (15 passed); full standard E2E `npx playwright test --project=chromium` (94 passed); inbuilt Browser on `http://localhost:1420/?missionHarness=1` confirmed weather typing and bare-domain normalization, Weather menu display, Go To Location keeping Create Marker Here usable, marker dialog opening from the converted grid point, and Search Sector name/start/end/radius values persisting.
- 2026-06-04 by Codex — `DON-79` completed. Renderer now has official map metadata/style support, `sartracker-official-map://` MapLibre protocol registration, and an Electron main-process MapGenie proxy that reads the configured local source file, keeps credentials in the Authorization header only, normalizes WMTS `GetCapabilities` URLs to ArcGIS `MapServer/export`, and returns PNG tile bytes. Hosted/browser default remains safe: Official maps appear disabled until local licensed-map configuration exists; public fallback maps remain selectable; Leaflet fallback stays public-basemap-only. Real local source probe through the new Electron proxy over MacGillycuddy Reeks / Carrauntoohil returned valid PNGs for `official_discovery_topo` and `official_aerial_imagery` at z14 x7748 y5411. Electron/local visual proof with isolated temp user data confirmed Discovery Topo and Aerial Imagery both render around Carrauntoohil/Reeks and reach `loaded=true` / `areTilesLoaded=true` with ready badges. `DON-79` is Done in Linear. Verification passed: `npm run test` (138 files / 686 tests), `npm run build`, `npm run lint`, `npm run test:backend` (46 passed / 1 ignored), `git diff --check`, focused proxy/protocol/map tests, Electron CJS syntax checks, inbuilt Browser default-state check, and Electron CDP visual proof. Next map lane chunk: `DON-81` official-map prefetch/offline path planning.
- 2026-06-03 by Codex — Official Irish map lane under `DON-76`: `DON-77`, `DON-78`, and `DON-80` are complete. The USB was copied outside the repo to `/Users/donalocallaghan/SARTracker-private-map-assets/team-usb-2026-06-03`; manifest at `/Users/donalocallaghan/SARTracker-private-map-assets/team-usb-2026-06-03-COPY-MANIFEST.txt`; source-vs-destination SHA-256 check passed. `discovery` is locked as the default official operational topo map; `basemap_premium` is secondary reference; `ortho` and `National_High_Resolution_Imagery` are imagery/reference layers; relief/slope rasters are later overlays. Maps menu groups Official maps vs Public fallback maps. `DON-80` adds local official-map source configuration in Settings and Electron persistence: app settings store only the local source path and safe metadata (status, username, available source IDs, service count, message), not passwords or file contents; diagnostics/support reports expose only safe status/type/service counts. Do not commit or bundle licensed map files/credentials; hosted web should not expose private MapGenie sources unless a controlled server-side/private proxy is deliberately added later. Full verification passed: `npm run lint`, `npm run build`, `npm run test` (135 files / 676 tests), `npx playwright test tests/e2e/settings.spec.ts --project=chromium` (7 passed), `npm run test:backend` (46 passed / 1 ignored), and `npm run test:all` (unit + 121 Playwright including visual + backend). Full-suite closeout also fixed stale drawing color-picker E2E selectors, converted tracking device visibility filters to MapLibre expression form, and disabled Rust doctests for the Tauri app library so the scripted backend gate runs unit tests without rustdoc app-crate resolution failures. Next: `DON-79` local MapGenie rendering via ArcGIS export.
- 2026-06-02 by Codex/Claude — `DON-74` completed as a mixed bug/improvement (weather links + colour palette). **Weather links bug:** the team's `met.ie` and `yr.no` entries failed because (a) bare domains without `https://` were rejected by validation, and (b) the `<a href target="_blank">` approach fails in Tauri/Electron where WKWebView/WebView2 blocks external navigation. Fix: added `addSchemeIfMissing()` to prepend `https://` to bare domains during validation and normalization; replaced the `<a>` links with buttons calling a new `openExternalUrl()` function that routes through `open::that` on Tauri (new `open_external_url` Rust command), `shell.openExternal` on Electron (new IPC channel), and `window.open` in browser mode. Codex follow-up fixed desktop persistence drift so Tauri validation/normalization and Electron settings persistence also prepend `https://` for bare domains before save/open. **Colour palette:** replaced plain-text hex-only colour inputs in drawing dialogs (search area fill, text label color) with a shared `ColorPaletteInput` component offering 12 high-contrast SAR palette swatches plus typed hex entry. Replaced the native `<input type="color">` in device breadcrumb rows with a compact swatch popover using the same palette. Marker dialogs do not have user-selectable colour — not changed. Verification: `npm run test` 133 files / 666 tests; `npm run test:backend` 46 passed / 1 ignored; `npx tsc --noEmit`; `npm run lint`; browser-backed Playwright at `http://127.0.0.1:1420/?missionHarness=1` configured `met.ie` as a bare domain, saved without validation error, confirmed browser settings normalized to `https://met.ie`, opened Weather menu, and clicked Met Éireann through to `https://www.met.ie/`. Operator manual updated in the original DON-74 pass.
- 2026-06-02 by Claude — `DON-72` completed as a drawing-rendering cleanup + legibility change (improvement). **Root cause found and reproduced in-browser:** the visible `mission-drawings-point` circle layer drew a dot at every vertex of every line/polygon because MapLibre 5 **silently drops a filter that nests the legacy `['==','$type',X]` selector inside an `['all', …]` expression** (`setFilter` no-throws, `getFilter` returns `undefined`, layer renders unfiltered). The same dropped filter also defeated drawing-type/per-drawing **visibility toggles** on the fill/line/point layers (hiding "Search Areas" still showed the polygon). Fix: replaced all nested `$type` selectors with the modern `['==',['geometry-type'],X]` expression form in `sync-drawing-overlay.ts` and `sync-tracking-overlay.ts`. Legibility (per Donal's steer — colours not pickable in the UI for rings/sectors/lines, so good defaults are the only lever): added a dark casing layer under all drawing lines, made the line layer render polygon boundaries too (crisp outlines), widened default strokes to 3–3.5px, and retuned muddy default colours (range ring green→cyan `#22D3EE`, search area/sector/bearing brightened) while **keeping LPB percentile colours** intact; mirrored casing in the Leaflet fallback. Removed the visible **Select** button from Map Tools (internal select stays the default/fallback after save/cancel). Removed **Vertices** readouts from the Line and Search Area dialogs. Added **text-label click-and-drag move** with persistence through the existing `upsertDrawing` path (new `moveTextLabel` controller method + pure `text-label-drag.ts` helper). No new drawing schema. Operator manual updated. Verification passed: red→green for `sync-drawing-overlay`, `sync-tracking-overlay` ($type-absence), `drawing-geojson` (recolor/width), `drawing-toolbar` (no Select), `drawing-dialog` (no Vertices), `start-drawing-runtime` (moveTextLabel), and `text-label-drag`; `npm run test` 131 files / 648 tests; `npx tsc --noEmit`; `npm run lint`; `npm run build` (budgets clean); `cargo test` 45 passed / 1 ignored; `git diff --check`. Browser (Chrome DevTools, Donal approved): on `http://127.0.0.1:1420/?missionHarness=1`, seeded search area / LPB rings / sector / text label render as clean continuous lines with dark casing and NO vertex dots, LPB rings keep green/amber/red percentile colours, Map Tools chip shows SELECT with no Select button, and dragging "Command Post" moved its anchor from `[-9.696,51.974]` to `[-9.6848,51.9703]` in both geometry and metadata and survived a reload.
- 2026-06-02 by Codex — `DON-71` completed as a conservative casualty treatment-log change. Casualty marker treatment notes now have an `Existing Treatment Notes` log plus a `New Treatment Update` box; `Add Update` appends a local timestamp and optional `Updated By` name without overwriting earlier entries. The change deliberately stores the appended log in the existing treatment field and avoids new medical-schema/export/retention semantics until the team asks for formal medical-record handling. Operator manual updated. Verification passed: red→green helper and dialog tests for append behavior and empty-update rejection; focused marker tests 2 files / 11 tests; inbuilt Browser at `http://127.0.0.1:1420/?missionHarness=1` confirmed a marker created through Coordinate Converter → `Create Marker Here` can be switched to Casualty, shows the treatment log/update controls, disables `Add Update` while empty, appends `[2026-06-02 16:13] Bravo: Warm drink given` after an existing Alpha entry, and clears the new-update field; `npx tsc --noEmit`; `npm run lint`; `npm run test` 127 files / 628 tests; `npm run build` (budgets clean); `npm run test:backend` 45 passed / 1 ignored; and `git diff --check`.
- 2026-06-02 by Codex — `DON-70` completed as a coordinate footer and converter-marker workflow change. The map footer now shows DD, Irish Grid, and DMS simultaneously, with Convert centered in the live coordinate strip and fixed CRS cells still omitted. DMS formatting now rounds through total seconds so operator displays never show invalid `60.000"` seconds. The Coordinate Converter now offers `Create Marker Here` after a successful IG/DD/DMS conversion; it refreshes the active marker runtime, opens the normal Marker Details form at the converted WGS84 coordinate, and closes the converter. Operator manual updated. Verification passed: red→green tests for footer DMS/readout coverage, converter-created marker workflow, and DMS rollover; focused coordinate tests 4 files / 21 tests; inbuilt Browser at `http://127.0.0.1:1420/?missionHarness=1` confirmed the footer renders DD/Irish Grid/DMS, Convert opens the dialog, converting `Q 99842 04015` then `Create Marker Here` opens Marker Details with `52.179336°N, 9.464944°W` and `Q 99842 04015`; `npx tsc --noEmit`; `npm run lint`; `npm run test` 126 files / 625 tests; `npm run build` (budgets clean); `npm run test:backend` 45 passed / 1 ignored; and `git diff --check`.
- 2026-06-02 by Codex — `DON-69` completed as a conservative clutter-reduction change. Mission Control now offers a header `Minimize` control only for an active configured mission; the collapsed state keeps a restore affordance and mission summary visible while hiding lifecycle controls, and paused/recovery/non-active states stay expanded so `DON-64` safety cues remain reachable. The coordinate footer now keeps operator-facing coordinates plus `Convert`, with low-use fixed CRS cells removed from the live surface. Follow-up correction: the initial closeout wrongly said the inbuilt Browser was unavailable. Browser validation then exposed a React/Zustand render loop from a fresh tracking-style object selector in the map renderers; fixed with `useTrackingStylePreferences()` and a regression test. Operator manual updated. Verification passed: red→green tests for active Mission Control collapse, paused non-collapse, coordinate footer CRS removal, and the stable tracking-style preferences hook; focused tests 3 files / 5 tests; inbuilt Browser at `http://127.0.0.1:1420/?missionHarness=1` confirmed the map shell and coordinate footer render, CRS labels are absent, active Mission Control minimizes with Expand visible and pause/resume hidden, and paused state keeps the banner plus pause/resume visible with no collapse/expand affordance; `npx tsc --noEmit`; `npm run lint`; `npm run test` 125 files / 623 tests; `npm run build` (budgets clean); `npm run test:backend` 45 passed / 1 ignored; and `git diff --check`.
- 2026-06-02 by Codex — `DON-68` completed as a conservative contrast/crosshair improvement. Added a prominent red/white SVG crosshair cursor for armed map placement tools; changed the temporary coordinate go-to target to a larger red ring and red center dot with white contrast stroke; added a shared high-contrast `sar-selected-row` selected-state treatment and applied it to high-use Devices Workspace and Mission Review selected rows. No full theme system was introduced. Operator manual updated. Verification passed: red→green tests for red coordinate target styling, red armed-tool cursor, Devices Workspace selected-row contrast, and Mission Review selected-row contrast; `npm run test` 122 files / 618 tests; `npm run lint`; `npx tsc --noEmit`; `npm run build` (budgets clean); `npm run test:backend` 45 passed / 1 ignored; and `git diff --check`. UI/browser caveat: no Playwright/Chrome DevTools run per Donal's standing restriction, and the inbuilt Browser tool was unavailable in this session.
- 2026-06-02 by Codex — `DON-66` completed as a `DON-60` team-requirements change. Added per-device breadcrumb trail colour controls in the Devices Workspace and one global `Breadcrumb Size` slider for trail width. Styling is persisted in a small frontend/browser-harness store (`sartracker:tracking-style`) and is passed explicitly into MapLibre and Leaflet fallback renderers; no backend schema migration was introduced. Device dots, labels, and breadcrumb trails use the configured device colour, while breadcrumb line/casing widths use the global size. Operator manual updated. Verification passed: red→green tests for configured breadcrumb colour, global size, Leaflet fallback styling, and Devices Workspace controls; focused tracking/map suite 7 files / 32 tests; `npm run test` 119 files / 615 tests; `npm run lint`; `npx tsc --noEmit`; `npm run build` (budgets clean); `npm run test:backend` 45 passed / 1 ignored; and `git diff --check`. UI/browser caveat: component tests verified the visible controls and renderer tests verified MapLibre/Leaflet styling; Playwright/Chrome DevTools were intentionally not run per Donal's standing restriction, and the inbuilt Browser tool was unavailable in this session.
- 2026-06-02 by Codex — `DON-65` completed as a `DON-60` team-requirements change. Added explicit mission-active device selection in the Devices Workspace: `Active Mission Devices` now appears above the full `All Devices` roster, rows have Add/Remove controls, and the full roster remains visible for situational awareness. Map markers, labels, initial tracking fit, and breadcrumbs now focus on the active mission device set once at least one device is selected; when no active devices are selected, the map intentionally continues showing all tracked devices so operators are not left with an empty tracking picture. Selection is persisted per mission in the frontend/browser-harness path for this first slice; no backend schema migration was introduced. Operator manual updated. Verification passed: red→green unit coverage for the new selector/store and workspace split, focused tracking/map suites 6 files / 24 tests, `npm run test` 119 files / 611 tests, `npm run lint`, `npx tsc --noEmit`, `npm run build` (budgets clean), `npm run test:backend` 45 passed / 1 ignored, and `git diff --check`. Browser/UI validation was limited to component-visible behaviour because Donal's standing instruction forbids Playwright/Chrome DevTools unless explicitly requested and the inbuilt Browser tool was unavailable in this session.
- 2026-06-02 by Claude — `DON-73` completed as a `DON-60` team-requirements pass (mixed bug/improvement). Team report: Layer Workspace had Expand All but no Collapse All, and Move Up/Down + Favorite "did not appear to work." **Reproduced empirically before changing anything:** a new published-tree assertion in `tests/unit/start-layer-catalog-runtime.test.ts` and a browser-backed Playwright run both show **Move Up/Down already work end-to-end** — selecting Bravo and clicking Move Up visibly places Bravo above Alpha in the People layer, and all three persistence backends (browser-harness/Tauri/Electron) preserve `displayOrder`. The existing reorder test only asserted the `upsertMetadata` calls, never the re-sorted tree, which is why the working behaviour looked unproven; it was a discoverability issue (buttons live in the inspector, only after selecting a node), not a logic bug. **Favorite** had zero operational consumers (grep-confirmed: only flipped a star + showed "Favorite: Yes/No"); per the issue's explicit fallback ("remove/disable if it has no operational meaning yet") and Donal's steer, the Favorite controls were **removed** from the tree row and inspector, the inert inspector row dropped, and the now-dead `toggleFavorite` controller method deleted. The persisted `isFavorite` field/schema (TS types, SQLite, Rust, Electron, browser harness) was deliberately **kept** — ripping it out is a risky migration for a control-removal fix and an unwritten field defaulting to `false` is harmless; recorded as minimal intentional debt. **Collapse All** was genuinely missing and was added beside Expand All (`resetExpandedNodeIds([])`, the UI store already supported it). Verification: red→green published-tree reorder test; `npm run test` 118 files / 607 tests; `npx tsc --noEmit`, `npm run lint`, `npm run build` (budgets clean); `npm run test:backend` 45 passed / 1 ignored; `npx playwright test tests/e2e/layer-panel.spec.ts tests/e2e/parity-layer-console.spec.ts --project=chromium` 10/10 (new Move Up reorder, Collapse All↔Expand All, and Favorite-absence specs); and a browser-backed Playwright run at `http://127.0.0.1:1420/?missionHarness=1` (`tmp/don-73-verification/`, screenshots inspected) confirming Expand All/Collapse All side-by-side, no star/favorite controls, Bravo moving above Alpha on Move Up, and Collapse All hiding nested items while top-level groups stay. Operator manual updated (Expand All/Collapse All row, reorder persistence note, Favorite removed). Committed and pushed.
- 2026-06-01 by Claude — `DON-75` completed as a `DON-60` team-requirements change (review/clarification, not a bug). Helicopters are now **opt-in** in the Tracking-tab panel. Previously `HelicopterPanel` rendered all four reserved aviation slots (Rescue 118/115, Air Corps 1/2) unconditionally, cluttering the tab with empty editor cards. Donal locked the intent as "important but opt-in"; I read "shown" as the panel clutter, not the map (empty slots create no map features, and an aircraft on the map is meaningful — so **map visibility defaults are intentionally unchanged**, still controlled from the Layers tab). New pure selector `src/features/helicopters/helicopter-panel-slots.ts` (`selectHelicopterPanelSlots`) decides visible slots from assigned slots + a `showEmptySlots` flag: assigned slots always show; empty slots collapse behind an "Add helicopter (N slots free)" control; "Hide empty slots" re-collapses; an assigned slot is never hidden. Zero-assigned shows a calm empty state. Verification passed: red→green `tests/unit/helicopter-panel-slots.test.ts` (6 tests), `npm run test` 118 files / 606 tests, `npx tsc --noEmit`, `npm run lint`, `npm run build` (budgets clean), `npm run test:backend` 45 passed / 1 ignored, updated `tests/e2e/helicopter-panel.spec.ts` 1/1 chromium (reveals empties before assigning; still covers persist → overlay → layer-toggle), and a browser-backed Playwright check (`tmp/don-75-verification/`, screenshots inspected) confirming default empty state, reveal of slot_1..4, and an assigned Rescue 118 slot staying visible after Hide empty slots. Operator manual updated. Committed `26a06c7` and pushed.
- 2026-06-01 by Claude — `DON-64` completed as the fourth `DON-60` team-requirements pass (change, not bug). Made the paused mission state unmistakable: a new pure selector `src/features/mission/mission-phase-presentation.ts` maps each mission phase to label/tone/attention/banner so the command mast pill and Mission Control panel cannot drift. The mast pill (`phasePillClassName`) and the Mission Control header chip now use a new flashing bright-red `sar-status-chip-paused` class (was soft amber). Mission Control gains a prominent red `mission-paused-banner` directly under the header with explicit text ("Mission paused", "Active-search time is frozen … Resume the mission") and its own `Resume Mission` button, plus a red panel ring; the main pause/resume button flips to a flashing amber→red `sar-action-resume-paused` style. The pinned Mission Control dock drops its `max-h-[53vh]` scroll cap while paused so the alarm and Resume can never be clipped/collapsed. Colour/animation never carry the cue alone (text semantics + layout), and `prefers-reduced-motion` keeps the bright red but stops the flash. Verification passed: red→green `tests/unit/mission-phase-presentation.test.ts` (4 tests), `npm run test` 117 files / 600 tests, `npm run lint`, `npx tsc --noEmit`, `npm run build`, `npm run test:backend` 45 passed / 1 ignored. Playwright (user-requested): `tests/e2e/mission.spec.ts` 12/12 (DON-64 paused assertions added), `tests/e2e/visual/visual-mission-lifecycle.spec.ts` 6/6, and `npm run visual:review -- --only mission-paused-state --no-cache` PASS (independent Opus reviewer confirmed all 9 checklist items). Pre-existing unrelated `tests/e2e/devices-workspace.spec.ts` failure reproduces with my changes stashed — not caused by this work. Operator manual updated with a Paused-state note.
- 2026-06-01 by Codex — `DON-63` completed as the third `DON-60` team-requirements bug pass. Reproduced the device-list focus/state bug at the component event boundary: the device row owns selection, but passive child cells such as status bubbled click events to the row and selected a different device. Fixed `DevicesWorkspace` so the explicit device-name button, passive status/time/source cells, visibility checkbox, and zoom control stop row propagation; zoom still intentionally selects and targets the chosen device. Verification passed: red/green `tests/unit/devices-workspace.test.ts`, focused device workspace suite 2 files / 3 tests, `npm run test` 116 files / 596 tests, `npm run lint`, `npm run build`, `npm run test:backend` 45 passed / 1 ignored, and inbuilt-browser smoke at `http://127.0.0.1:1420/?missionHarness=1` confirmed the app shell, MapLibre canvas, and OpenTopoMap readiness with no browser console errors. Playwright E2E was intentionally not run per Donal's explicit browser-tool restriction.
- 2026-06-01 by Codex — `DON-62` completed as the second `DON-60` team-requirements bug pass. Reproduced the intermittent layer visibility failure as a subtree persistence race: hiding a drawing layer pushed the flat visibility store immediately, but descendant metadata writes could resolve before the parent layer write and partially rehydrate the map filters back to visible. Fixed by adding `LayerCatalogController.setNodeVisibilities(...)` so subtree visibility persists as one catalog mutation and rebuilds once, with the layer panel using that batch path after its immediate MapLibre-filter update. Verification passed: red/green `tests/unit/layer-stale-refresh-integration.test.ts`, focused layer suite 5 files / 30 tests, `npm run test` 115 files / 595 tests, `npm run lint`, `npm run build`, `npm run test:backend` 45 passed / 1 ignored, and inbuilt-browser smoke at `http://127.0.0.1:1420/?missionHarness=1` confirmed the shell/map/layer panel renders with no browser console errors. Playwright E2E was intentionally not run per Donal's explicit browser-tool restriction.
- 2026-06-01 by Codex — `DON-61` completed as the first `DON-60` team-requirements bug pass. Reproduced the long-run failure mode at the tracking breadcrumb accumulator: current code allowed live breadcrumb snapshots to grow past the 20k render budget in a 25-device / 25k-point simulation, matching the reported long-session map/UI slowdown risk. Fixed by capping the live breadcrumb snapshot at 20,000 most-recent deduplicated positions before it reaches React/MapLibre; desktop mission persistence still receives incremental positions as they arrive, while browser harness persistence remains separately capped at 2,000. Verification passed: focused tracking/browser-harness unit suite 4 files / 33 tests, `npm run test` 115 files / 594 tests, `npm run lint`, `npm run build`, `npm run test:backend` 45 passed / 1 ignored, and inbuilt-browser smoke at `http://127.0.0.1:1420/?missionHarness=1` confirmed browser testing shell, map surface, mission surface, and tracking surface render. Playwright E2E was intentionally not run per Donal's explicit browser-tool restriction.
- 2026-06-01 by Codex — `DON-57` completed: Electron Linux validation artifacts now build natively in GitHub Actions via `.github/workflows/electron-linux-validation.yml` on `ubuntu-22.04`. Successful run `26746757159` at head `32cb2e315ad61449270f40f2bee2bb6a71e0fd56` produced AppImage SHA256 `000f7e4d44692476ce5873a5ef0bd8901a22848c1e8179c512628cfd0438acd2` and `.deb` SHA256 `58aee86e305e504a9a419dcb2873c7a7840fa8922b0233c78c32937f58031915`; packaged `better_sqlite3.node` inspected as ELF x86-64. CI AppImage smoke rendered SAR Tracker/OpenTopoMap under Xvfb with content mean `0.499086`, and the exact GitHub-built AppImage was copied to `donal-Precision-5570` Ubuntu `24.04.2 LTS` and visually confirmed rendering OpenTopoMap. The workflow now fails black/blank AppImage windows after run `26746265336` exposed that a process/window smoke alone was too weak. Remaining Electron release work is `DON-58` Linux secret-store/diagnostics labeling and `DON-59` packaged Linux GPX/attachments/file-opening smoke.
- 2026-05-31 by Codex — `DON-27` Leaflet raster fallback spike implemented and proved locally behind `?mapRenderer=leaflet`. The fallback is deliberately read-only: it keeps the default MapLibre path unchanged, uses Leaflet `1.9.4` for raster tiles, and reuses the existing tracking, marker, and drawing GeoJSON builders/stores to render critical visibility surfaces without WebGL. Browser-harness validation seed `?leafletFallbackSeed=1` creates a DON-27 mission with one tracked team, an LKP marker, and a search-area drawing. Inbuilt-browser proof on `http://127.0.0.1:1420/?missionHarness=1&mapRenderer=leaflet&leafletFallbackSeed=1` showed Leaflet tiles loaded (`15/15`), no MapLibre canvas, mission `DON-27 Leaflet fallback surface`, tooltips `Alpha Team` and `LKP`, drawing label `Sector Alpha`, and the read-only fallback badge. Verification passed: focused Leaflet tests 2 files / 5 tests, `npm run lint`, `npm run test` 114 files / 592 tests, `npm run build`, and `npm run test:backend` 45 passed / 1 ignored. Recommendation: treat DON-27 as proven for fallback-only/read-only coverage; do not pursue Leaflet edit parity until DON-29 decides that degraded-mode editing is required.
- 2026-05-31 by Codex — `DON-35` Dell Ubuntu validation progressed beyond launch smoke. Target machine: `donal-Precision-5570`, Ubuntu `24.04.2 LTS`, GLIBC `2.39`, GNOME Wayland with Xwayland, Intel Iris Xe + NVIDIA RTX A2000. Fresh macOS-cross-built Electron artifacts first reproduced two packaging defects: packaged `file://` Electron rendered black because Vite emitted absolute `/assets/...` URLs, and the cross-built `.deb` contained a macOS arm64 `better_sqlite3.node` (`invalid ELF header`) despite a Linux target. Fix in progress: set Vite `base: './'` and add `package.json` homepage metadata so Linux `.deb` builds under FPM. Native Linux build on the Dell produced an ELF x86-64 `better_sqlite3.node`, AppImage SHA256 `48c203fa7057da014798af14d8833cc42a2069d407e8d8e82f64c863075bea78`, `.deb` SHA256 `af7e5e65144cfe81f2f026de7294a3eb89be632a0b8ecb355d1db88518021816`. Both native AppImage (`--no-sandbox` required on Ubuntu 24 AppImage) and native `.deb` rendered SAR Tracker and OpenTopoMap. Packaged `.deb` UI smoke then created `Dell Smoke Mission`, wrote it to SQLite WAL, survived process restart into recovery, resumed from recovery, connected live Traccar via `https://kmrtsar.eu`, rendered live device markers, persisted 33 devices / live positions plus `tracking-cache.json`, and exported a sanitized diagnostics report showing Electron/Linux/safeStorage/tracking/SQLite state without credential text. Important caveat: secret save failed without `--password-store=gnome-libsecret` even though GNOME Keyring was present; relaunching with that Electron flag made `safeStorage backend: gnome_libsecret` and save/connect passed. Diagnostics currently labels runtime as `browser validation` inside Electron, which should be corrected before tester release. Local verification for the config fix passed: red/green `tests/unit/vite-config.test.ts`, `npm run build`, and `npm run lint`; Dell native unit/integration parity passed 12 files / 24 tests after `npm rebuild better-sqlite3`. Remaining: make Linux-native artifacts repeatable in CI, bake/fix the Linux password-store behavior, correct the diagnostics runtime label, and still run/manual-prove GPX import plus marker attachment/open-file workflows on the packaged Linux app before publishing wider.
- 2026-05-18 by Codex — S8 Linux runtime reliability path created in Linear after Electron/WebKitGTK/Leaflet research convergence. Parent `DON-25` plus child issues: `DON-26` S8a empirical Electron MapLibre Linux validation (In Progress), `DON-27` S8b Leaflet raster fallback spike, `DON-28` S8c Electron shell bridge/packaging PoC, `DON-29` runtime decision checkpoint. S8a spike app exists at `tmp/s8a-electron-maplibre-validation/`: Electron `40.10.0` / Chromium `144.0.7559.236`, MapLibre raster basemap, GPU/WebGL diagnostics, BrowserWindow screenshot probe, smoke mode, and Linux x64 `.deb` + AppImage artifacts. Local macOS smoke passed (`S8A_MAP_STATUS=ready`, window mean `0.5945162505106207`). Linux x64 artifacts: `.deb` SHA256 `fb65baa21ca7594ca2e276b3c518dc7b54cb49b3eeb92a5aa64edb8ab48b7f5b`, AppImage SHA256 `4576a28106048231a7db17ff94996db16fd02d21018fa3131399003cc33f41d1`. S8b package recon captured at `tmp/s8b-leaflet-fallback-notes.md`: `terra-draw-leaflet-adapter@1.3.0` exists, peers `terra-draw ^1.0.0` + `leaflet ^1.9.4`; production should prove read-only fallback visibility before edit parity. Next: run the S8a `.deb` on Donal's Ubuntu Dell and, ideally, one team machine that currently black-maps under Tauri; do not treat S8a as complete until real Linux render evidence is captured.
- 2026-05-19 by Codex — S8a first real Linux evidence captured from old Lenovo Z580 at `192.168.18.25`: Ubuntu 18.04.6 LTS, kernel `4.15.0-208`, Intel Core i7-3520M, Intel HD Graphics 4000 (`i915`). AppImage rendered OpenTopoMap with WebGL2, renderer `ANGLE (Intel Open Source Technology Center, Mesa DRI Intel(R) HD Graphics 4000 (IVB GT2), OpenGL 4.2)`, tile errors `0`, window mean `0.5188`. The `.deb` installer UI appeared stuck around 67%, but `dpkg -s sartracker-s8a-electron-maplibre-validation` reports `install ok installed`; running the installed app in smoke mode also rendered successfully: `S8A_MAP_STATUS=ready`, tile errors `0`, window mean `0.5693`. Evidence copied to `tmp/s8a-old-ubuntu-lenovo-z580/`, with summary in `evidence-summary.md`. This is a strong positive Electron portability data point, but S8a still needs one representative modern/team Linux machine before closing.
- 2026-05-19 by Codex — S8a first team-machine Linux evidence received from PCLinuxOS/X11/NVIDIA Quadro P620 tester using the AppImage probe. Tester reported maps displayed. Diagnostics: Electron `40.10.0`, Chromium `144.0.7559.236`, Linux `6.18.31-pclos1`, NVIDIA driver `580.159.03`, WebGL2 renderer `ANGLE (NVIDIA Corporation, Quadro P620/PCIe/SSE2, OpenGL 4.5.0)`, Chromium `webgl: enabled`, MapLibre `status: ready`, OpenTopoMap, tile errors `0`, window mean `0.6509`. Evidence summary at `tmp/s8a-team-pclinuxos-nvidia-p620/evidence-summary.md`. This materially increases confidence that the team black-map failure is a Tauri/WebKitGTK Linux runtime problem rather than tile/network/MapLibre itself. Keep `DON-27` Leaflet fallback because Electron still depends on WebGL for MapLibre.
- 2026-05-19 by Codex — `DON-28` Electron shell PoC started after both team S8a probes rendered maps. Added pinned Electron `40.10.0`, `electron/main.cjs`, `electron/preload.cjs`, `electron-builder.json`, scripts `electron:dev`, `electron:preview`, `electron:pack`, `electron:dist:linux`, typed renderer bridge `window.sartrackerElectron.traccarHttpRequest(...)`, and `createElectronTraccarFetch` / `createElectronTraccarClient`. Electron validation mode currently uses the browser-harness mission store but routes Traccar HTTP through Electron main, not Vercel. Settings Test Connection uses the bridge in Electron. Verified `sean/sean` against `https://kmrtsar.eu`: 3 devices / 3 positions; `https://traccar.kmrtsar.eu` behaves like a device listener and is not the app API base. Local verification passed: focused unit tests (4 files / 9 tests), `npm run build`, `npm run lint`, `node --check electron/main.cjs electron/preload.cjs`, and `npm run electron:pack` producing `tmp/electron-dist/mac-arm64/SAR Tracker Electron Validation.app`. Remaining DON-28 gaps: SQLite mission store, filesystem adapters, diagnostics export, keyring/secrets, GPX/file dialogs, tracking cache persistence, Linux artifact validation.
- 2026-05-19 by Codex — `DON-28` planning split completed for the next Electron parity batch. `DON-26` is Done, `DON-28` remains the active parent, and child issues now track the implementation slices: `DON-31` settings/secrets, `DON-32` tracking cache/diagnostics, `DON-33` SQLite mission store, `DON-34` filesystem/GPX/attachments/file opening, and `DON-35` Linux artifacts/field validation. Upfront research confirmed the main risk controls for the goal: Electron `safeStorage` must reject or warn on Linux `basic_text`; preload IPC should expose narrow typed methods and validate sender/input; SQLite native modules need Electron rebuild/packaging care; Ubuntu 24.04+ AppImage sandbox/AppArmor issues mean AppImage is useful but `.deb` also matters.
- 2026-05-19 by Codex — `DON-31` through `DON-34` are implemented locally and `DON-35` local artifact build is done. Electron now uses real settings/secrets under `userData`, refuses Linux `safeStorage` `basic_text`, persists tracking cache and allow-listed diagnostics, uses `better-sqlite3` mission/layer-catalog storage behind typed preload IPC, and supports GPX dialogs/reads, marker attachments, and external file opening through Electron main. Tauri paths are preserved. Local verification: focused Electron parity tests 12 files / 38 tests, `npm run test` 112 files / 587 tests, `npm run test:backend` 45 passed / 1 ignored, `npm run build`, `npm run lint`, `node --check` for all Electron CJS files, `npm run electron:pack`, packaged `.app` stayed running for a 6-second smoke, and `npm run electron:dist:linux` produced AppImage + `.deb`. Caveat: cross-building Linux on macOS rebuilds `better-sqlite3` inside local `node_modules` for Linux x64; run `npm rebuild better-sqlite3` afterwards before local macOS tests. Remaining DON-28 gap is a real Linux runtime smoke of the full Electron app.
- 2026-05-19 by Codex — Old Ubuntu Lenovo install smoke found a native-module Linux baseline blocker. On `192.168.18.25`, old S8a package/config was removed, new `.deb` installed as `sartracker-web 0.1.0~beta.3`, and both new artifacts remain in `/home/donal/Downloads/`. Ubuntu 18.04.6 has GLIBC `2.27`; both installed `.deb` and AppImage log `GLIBC_2.29 not found` from `better_sqlite3.node`. Treat this as a `better-sqlite3` native module packaging/baseline issue, not a map/WebGL result. DON-35 remains open until we either rebuild Linux native modules against an older baseline or decide the full Electron app requires newer Linux than Ubuntu 18.
- 2026-05-19 by Codex — S8a probe prerelease published at https://github.com/donal0c/sartracker-web/releases/tag/s8a-electron-map-probe-0.1.0. This is explicitly not a SAR Tracker app release. Uploaded assets: `sartracker-s8a-map-validation_0.1.0_amd64.deb` SHA256 `fb65baa21ca7594ca2e276b3c518dc7b54cb49b3eeb92a5aa64edb8ab48b7f5b`, and `sartracker-s8a-map-validation_0.1.0_x86_64.AppImage` SHA256 `4576a28106048231a7db17ff94996db16fd02d21018fa3131399003cc33f41d1`. Linear `DON-26` updated with release link and asset details.
- 2026-05-18 by Codex — Follow-up smoke run `26040183978` against existing `v0.1.0-beta.3` assets: gates and the live dependency preflight passed; Linux AppImage process/window launch passed, but the downloaded screenshot showed the runtime startup fault shell because the GitHub runner lacked freedesktop Secret Service (`org.freedesktop.secrets`); Windows smoke failed before app launch due a PowerShell `Join-Path` array-comma bug. Workflow hardened again: Linux launch smoke now runs under `dbus-run-session`, provisions `gnome-keyring`, and OCR-fails on boot/fault shells; Windows path discovery now avoids the comma bug and logs candidate install roots. Re-run `smoke_existing_release=true` before treating CI launch smoke as green release evidence.
- 2026-05-18 by Codex — Smoke rerun `26041016902` passed against existing `v0.1.0-beta.3` assets: gates/live dependency preflight, Linux AppImage launch, and Windows NSIS launch all green. Linux evidence screenshot showed OpenTopoMap tiles visible and `OpenTopoMap basemap ready`. Donal confirmed team testers now get Traccar tracking on `:8082` but still see tracks on a black map background, so remaining issue is map rendering/reachability on their Linux environments, not Traccar auth. Workflow tightened again locally: Linux launch smoke now crops the map viewport and fails if the region is too dark, so future runs catch black-basemap artifacts automatically.
- 2026-05-18 by Codex — Final smoke rerun `26041435928` passed with the black-map detector in place. Gates/live dependency preflight passed, Linux AppImage launch passed, Windows NSIS launch passed. Linux map-region grayscale mean was `0.556917` (threshold `>0.18`), and screenshot shows OpenTopoMap tiles visible. Online research points at two plausible remaining branches for tester black maps: (1) tile provider/network filtering on their machines/network, or (2) Linux WebKitGTK/WebGL/GPU rendering; Tauri has an open Linux/upstream WebGL context-lost issue and WebKitGTK exposes hardware acceleration/WebGL settings with known DMABuf/NVIDIA friction. Next ask to team: on the same connected session, try ESRI World Topo and OpenStreetMap in the Maps menu, export Diagnostics, and report distro/GPU/session (Wayland vs X11) plus whether `WEBKIT_DISABLE_DMABUF_RENDERER=1 ./sartracker-web_0.1.0-beta.3_linux_amd64.AppImage` changes the map.
- 2026-05-18 by Codex — First Linux tester feedback investigated. Traccar failure screenshot is reproducible against `http://kmrtsar.eu:5055`: that port returns bare `400 Bad Request` for root, `/api/server`, and POST `/api/session`; the same credentials succeed against the Traccar web/API endpoint `http://kmrtsar.eu:8082` (18 devices visible), and the older fallback `http://kmrtsar.ddns.net:8082` also succeeds. Settings now reports `400` from `/api/session` as a provider-base-URL/API-port problem, with a specific `5055` listener-port hint, instead of calling it generic authentication failure. Release gating was hardened: `.github/workflows/release.yml` now runs a live dependency preflight (Traccar `:8082` session/devices + representative OpenTopoMap/OpenStreetMap/ESRI tile URLs), then native Linux AppImage and Windows NSIS launch-smoke jobs with screenshot/log artifacts before `SHA256SUMS`. Docs/manual updated to use `http://kmrtsar.eu:8082` and warn against tracker listener ports such as `5055`. Local verification passed: `actionlint .github/workflows/release.yml`, live dependency preflight, `npm run lint`, `npm run test` (559), `npm run build`, `npm run test:backend` (45 passed / 1 ignored). Repo-wide `cargo fmt --check` still reports pre-existing formatting drift in `src-tauri/src/persistence.rs`; not changed here.
- 2026-05-17 by Claude — **`v0.1.0-beta.3` PUBLISHED.** Live release page: https://github.com/donal0c/sartracker-web/releases/tag/v0.1.0-beta.3 — `draft: false`, `prerelease: true`. Four assets attached: `sartracker-web_0.1.0-beta.3_linux_amd64.AppImage` (92.9 MB), `sartracker-web_0.1.0-beta.3_linux_amd64.deb` (15.8 MB), `sartracker-web_0.1.0-beta.3_windows_x64.exe` (12.9 MB, NSIS currentUser install), `SHA256SUMS` (335 B). Filenames match the documented `_linux_amd64`/`_windows_x64` pattern. Smoke testing on a real Linux box was deferred at user request; the release is shareable with testers who have repo access to `donal0c/sartracker-web` (the repo is private). Failed-tag markers `v0.1.0-beta.1` (gates failed) and `v0.1.0-beta.2` (Windows MSI rejected pre-release suffix; Linux-only stale draft still on GitHub Releases) intentionally retained per the project's tag-immutability rule — safe to delete the beta.2 draft via `gh release delete v0.1.0-beta.2 --repo donal0c/sartracker-web --yes` when convenient. SHA256s recorded in the Linear issue `sartracker-web-y6a` final comment for future audit.
- 2026-05-17 by Claude — **B4 first end-to-end CI run SUCCESS on `v0.1.0-beta.3`** (run `26002563213`, ~24m wall-clock). All four jobs green: gates (7m7s), bundle linux-x86_64 (11m9s), bundle windows-x86_64 (16m5s), SHA256SUMS (18s), summary (4s).
- 2026-05-17 by Claude — Re-cut as `v0.1.0-beta.3` after `v0.1.0-beta.2` had a partial-success run: Linux bundle uploaded AppImage + .deb cleanly, Windows bundle failed because Tauri's MSI bundler rejects alphanumeric pre-release suffixes (`-beta.N`). Fixes in this cut: (a) drop MSI from Windows args (NSIS only); follow-up Linear issue `sartracker-web-g1u` tracks MSI re-add when version scheme supports it; (b) rename tauri-action inputs to v0.6.2 names (`assetNamePattern`, `includeUpdaterJson`; dropped `uploadUpdaterSignatures`) — fixes the "Unexpected input" warnings that caused beta.2 assets to land with default Tauri names rather than our `_linux_amd64.<ext>` pattern; (c) bump version to `0.1.0-beta.3`. Failed tag history: `v0.1.0-beta.1` (gates failed) and `v0.1.0-beta.2` (Windows MSI failed) retained on remote per immutability rule.
- 2026-05-17 by Claude — Re-cut first CI-driven beta as `v0.1.0-beta.2` after `v0.1.0-beta.1` failed at the gates → cargo test step (Linux gates job did not install GTK/WebKit apt deps, so `gdk-sys` could not link). Fix added an identical apt step to the gates job. No draft release was created on the v0.1.0-beta.1 run (gates failed before bundle). Per project policy in `docs/releases/README.md`, tags are immutable; failed tag `v0.1.0-beta.1` retained as a marker.
- 2026-05-17 by Claude — B4 cross-platform Tauri beta distribution (`sartracker-web-y6a`) implemented locally on `master`. New workflow `.github/workflows/release.yml` builds Linux (AppImage + .deb on `ubuntu-22.04`, primary target — most of our team are on Linux), Windows (NSIS `currentUser` + MSI on `windows-2022`, secondary target), and macOS arm64 (.dmg + .app.tar.gz on `macos-latest`, parity-only) on `v*` tag push or `workflow_dispatch`. Architecture: `gates` job (Linux, lint/test/build + version-trio assertion + release-notes existence check) → `bundle` matrix (3 OSes via `tauri-apps/tauri-action@v0.6.2`, `fail-fast: false`) → `checksums` job (downloads release assets, generates SHA256SUMS, uploads as a release asset) → `summary` job. Releases land DRAFT + prerelease for human review before publish. Tauri config updated: Windows section now sets NSIS `installMode: currentUser` (no admin/UAC), WebView2 `downloadBootstrapper` with `silent: true` (0 MB bundle overhead), LZMA compression. Operator-facing docs rewritten: `docs/releases/TEMPLATE.md` has per-OS install sections (AppImage chmod+x flow, .deb apt install, NSIS-with-SmartScreen runbook, MSI admin path, macOS Gatekeeper); `docs/releases/README.md` documents Path A (CI-driven) vs Path B (local macOS-only); `docs/tauri-beta-release-plan.md` records B4 outcome and preconditions for wider/signed distribution; operator manual Desktop Beta section rewritten Linux/Windows/macOS. Three research files at `tmp/b4-research/` ({linux,windows,github-actions}-tauri2-2026.md, ~1500 lines combined) capture the 2026-current sources. End-to-end CI run verification is deferred until the first real beta tag is cut. Local verification all green.
- 2026-05-17 by Claude — Three-Linear issue polish chunk completed locally on `master`: `sartracker-web-wn6` (visual prompt drift), `sartracker-web-zq9` (mast tracking ratio), `sartracker-web-2xp` (OpenTopoMap badge over-eager). All three closed. wn6: rewrote the two drifted verificationPrompts (`marker-hazard-dialog`, `shell-idle-state`) to match the actual captured frames; both PASS via `npm run visual:review --only <id> --no-cache`. zq9: replaced the ambiguous mast `${positions.length}/${staleCount}` value (`ONLINE 14/13` scanned as the impossible ratio "14 of 13") with two separate stacked rows inside the same 64px mast column — `ONLINE` mode label, `FIX <n>` row, `STALE <n>` row (amber/warning when > 0). Implementation behind a pure selector at `src/features/tracking/command-mast-tracking-readout.ts` (8 unit tests). Two new chromium regressions in `tests/e2e/v1-regression.spec.ts` plus a new visual entry `mast-tracking-cell-active` (visual review PASS). 2xp: filtered the maplibre `error` channel to tile-level failures only via `src/features/map/is-tile-error-event.ts` (6 unit tests; non-tile errors no longer count against the trust budget) and widened the tile-health defaults from 3-in-10s to 5-in-30s in `src/lib/tile-health-tracker.ts` (4 new tests pinning the operator-trust defaults). Interactive Playwright sanity check at `tmp/2xp-verification/verify-2xp.mjs` proves the operator-facing behavior end-to-end against the live dev server: no false positive on first paint, no false positive on 4 non-tile errors, no false positive on 4 tile errors, correct degraded transition at 5 tile errors, no premature recovery on idle while errors still in window. Operator manual updated to describe the new mast tracking cell. Final verification all green.
- 2026-05-17 by Claude — QGIS Parity Residual-Gap Sweep (`sartracker-web-ag1`) completed locally on `master`. 5-agent wave-1 swarm cross-checked `docs/plugin-parity-matrix.md` against the current QGIS plugin (`~/Documents/Qgis/sartracker/`) and current `sartracker-web`. Wave-1 outputs at `tmp/parity-sweep/w1-{mission-lifecycle,tracking-replay,markers-drawings,layers-persistence-map,operator-tooling}.md`. Synthesis at `tmp/parity-sweep/sweep-report.md`. Headline: the matrix significantly understates current state — 11+ rows it calls Missing/Backend-only are actually Complete or Partial (finalize/unlock workflow, marker attachments, text label tool, focus mode, diagnostics panel, GPX import, layer-tree alias/favorite/reorder/expand-collapse). Several capabilities have no matrix row at all (Marker At GR, coordinate converter, secure keyring, layer-repair tooling, marker log search, attachment open from log, replay-window settings). Genuine retirement-blocking residue captured as C1–C13. No child Linear issues filed at this stage per Donal's call: a single discussion Linear issue (`sartracker-web-l7c`, P3) was filed and queued ~6–7 deep so a Codex+Donal walk-through can decide which residual gaps become Linear issues, which become matrix corrections, and which are out-of-scope. Spot-checks corrected one W1 error (W1 markers agent was wrong — `coordinator_ids`/`updated_by` ARE present in `marker-dialog.tsx` lines 125–127, 133; only the attachment-open button is missing).
- 2026-05-17 by Claude — B6 GPX And Drawing Hit-Test Hardening (`sartracker-web-fy5`) completed locally on `master`. Made marker/drawing/GPX click priority explicit, named, and testable. Headline bug fixed: a marker stacked under or near a drawing (e.g. an LKP placed inside a search-area polygon) is no longer silently swallowed by the enclosing drawing — the marker now wins the click. New pure module `src/features/map/map-click-target-resolver.ts` declares the rule **marker > drawing > empty** in a top-of-file comment, runs all three hit-tests in one place, and returns a structured verdict including a soft `gpxNearbyImportId` signal for future GPX surface work. New pure module `src/features/gpx/gpx-hit-testing.ts` adds projected-segment hit-testing for GPX MultiLineString tracks. Both `useMapMarkerInteractions` and `useMapDrawingInteractions` now consult the resolver instead of racing via `event.stopImmediatePropagation`. New unit suites `tests/unit/gpx-hit-testing.test.ts` (7 tests) and `tests/unit/map-click-target-resolver.test.ts` (14 tests). New Playwright spec `tests/e2e/hit-test-priority.spec.ts` (2 tests) pins the marker-under-polygon fix and the polygon-fallthrough behavior. Operator manual updated with an explicit click-priority paragraph in the Markers section.
- 2026-05-17 by Claude — `sartracker-web-b3c` closed. Updated the 5 V2-discovered drifted verificationPrompts so each describes what its captured element actually contains: the three mission-control panel captures (`mission-active-state`, `mission-backdated-offset`, `mission-paused-state`) drop the mission-name check (rendered on the command mast outside the panel) and verify panel-only invariants instead; `shell-drawing-toolbar` describes the no-mission disabled state ("Mission required" chip, dimmed tool buttons) rather than the post-mission "ACTIVE: SELECT" state and drops the LPB footer item that sits below the toolbar's scroll viewport; `drawing-multiple-on-map` now verifies only the range-ring drawing (the reliably-created one in the headless flow) plus the surrounding operational shell, and explicitly tells the reviewer not to fail on absent polygons / bearings / text labels. Also fixed `tracking-status-panel` (same class, surfaced when the full review re-ran): prompt now matches the "Tracking System / telemetry stream" header and the 4-column DEVICES/FIXES/CACHE/STALE counters strip. Each fix verified PASS via `npm run visual:review --only <id> --no-cache`. Two further drift findings (`marker-hazard-dialog` active-tab ambiguity, `shell-idle-state` map-tiles-loading + collapsed Maps menu) filed as follow-up `sartracker-web-wn6`.
- 2026-05-17 by Claude — V2 Visual Review Automation (`sartracker-web-n9i`) completed locally on `master`. `npm run visual:review` now reads every manifest entry under `test-results/visual-verification/`, spawns one `claude --print --output-format json` reviewer per entry, parses the structured pass/fail reply, and writes per-entry plus aggregate JSON reports. Severity gating via `--fail-on critical|high|medium`; reviewer errors always block; content-hash cache (model + verificationPrompt + screenshot bytes) under `.cache/`. Discovery: full live review surfaced 5 visual specs whose verificationPrompt drifted from the captured element — filed as `sartracker-web-b3c` (P2 bug) for follow-up. CLAUDE.md visual-tests section rewritten to point at the new command.
- 2026-05-17 by Codex — `sartracker-web-qmr` closed after live desktop smoke. Desktop Traccar polling now uses a Tauri command backed by Rust `reqwest`, preserving the existing TypeScript Traccar client contract while moving HTTP out of WKWebView `fetch()`. Removed the macOS ATS blanket workaround file (`src-tauri/Info.plist`) because desktop plain-HTTP Traccar calls no longer need `NSAllowsArbitraryLoads`. Verified with unit, backend, build, browser E2E, visual E2E, macOS `.app` packaging, packaged `Info.plist` inspection, and a packaged-app live poll against the real Traccar server after resuming the recovered mission.
- 2026-05-17 by Claude — V1 Regression E2E Coverage (`sartracker-web-8gw`) completed locally. Cold-start-from-cache now publishes an explicit `OFFLINE MODE — showing last known positions from cache.` status so operators do not silently view stale positions before the first live poll succeeds. Added regression coverage at three seams: unit (start-tracking-runtime cache-hydration status, polling-manager healthy-poll-no-offline guard, layer stale-refresh integration), and E2E (`tests/e2e/v1-regression.spec.ts`) covering the MapLibre device-filter path and the operator-visible "showing last known positions" warning. Replaced the five `waitForTimeout` calls in `tests/e2e/parity-visibility.spec.ts` with state-based `expect.poll` predicates.
- 2026-05-17 by Codex — S5 Mission Control View Model Extraction (`sartracker-web-cgx`) completed locally. Added `useMissionTimer` and shared it between Command Mast and Mission Control; added `useMissionControlViewModel` for lifecycle, recovery, governance, duplicate-name, admin-roster, busy/error, and control-enable state; `MissionControlPanel` now delegates mission orchestration to the hook and keeps rendering/focus-dialog responsibilities.
- 2026-05-17 by Codex — S4 Map Overlay Consolidation And Camera Race Fix (`sartracker-web-s5v`) completed locally. Added shared MapLibre overlay primitives for GeoJSON source sync, idempotent layer creation, filter combination, and SVG icon loading; refactored drawing, marker, measurement, GPX, helicopter, and tracking overlay sync modules onto those primitives; changed basemap style switching to restore the captured camera once on `styledata` instead of immediately and again on `idle`.
- 2026-05-17 by Claude — B3 First Internal Tauri Smoke Build (`sartracker-web-ppr`) **completed end-to-end on packaged build `0.1.0+sha.603771f65431`**. Two desktop bugs uncovered during the original smoke pass were fixed and categorically proven in the live `.app`:
  - `sartracker-web-el9`: keyring crate had no platform features (mock backend in use). Fixed in `000f7d1` by adding `apple-native`, `windows-native`, `sync-secret-service` to the keyring dependency. Symmetric Rust unit, real-keychain integration test (verifies via `security find-generic-password` from a separate process), and TS poller-start unit test added.
  - `sartracker-web-el9` (second root cause): macOS App Transport Security blocked the WKWebView renderer's `fetch()` to plain-HTTP Traccar URLs. Fixed in `603771f` by adding `src-tauri/Info.plist` with `NSAllowsArbitraryLoads = true` (internal-beta scope). `sartracker-web-qmr` filed as P2 follow-up to route renderer fetch through Rust `reqwest` and remove the blanket exception.
  - `sartracker-web-zl4`: closed as false positive. Rust persistence regression test `active_mission_survives_process_restart_without_explicit_finish` added in `000f7d1` to pin the contract. No code path automatically transitions an active mission to finished; the original B3 finding was operator-driven.
- Live end-to-end proof on the packaged `.app`: keychain seeded, active mission seeded, app launched, operator clicked Resume, `tracking-cache.json` populated from 85 bytes (empty placeholder) → 6,231 bytes within 30 seconds, SQLite `devices=18 / positions=14`, mast showed `DEVICES ONLINE 18 / SYSTEM STATUS ONLINE`, 14 trackpoints rendered on the map. Final quit confirmed mission stays active in SQLite (no `mission_finished` event). Evidence under `tmp/beta-artifacts/smoke-rerun/`.

## Operating Rule

The single active planning path is `docs/two-track-execution-workplan.md`.

All new work must be routed there before implementation:

- Track A: hosted team testing
- Track B: Tauri operational readiness
- Shared foundation
- Verification
- Deferred / decision-gated

Supporting docs may explain details, but they must not become separate queues.

## Current State

- `master` is the canonical working branch.
- Desktop Traccar polling routes renderer requests through Rust `reqwest` via Tauri IPC. The previous macOS `NSAllowsArbitraryLoads` plist workaround has been removed.
- Electron validation shell is now present but incomplete. It loads the real frontend through the existing browser-harness mission store and routes Traccar HTTP through Electron main via a typed preload bridge. It is for proving Electron + MapLibre + direct HTTPS Traccar before porting the full native adapter set.
- Hosted team testing runs at `https://sartracker-web.vercel.app/?missionHarness=1`.
- Hosted browser mode is testing/training only. It uses browser session storage and is not operational mission persistence.
- Verification cadence is web-first unless a chunk could plausibly pass in hosted/web but fail in packaged desktop/Electron. Ordinary UI and shared React behaviour should use the hosted/web loop for speed; desktop builds are reserved for runtime boundaries such as shell/preload/IPC, SQLite/filesystem/keyring, diagnostics, packaging/installers, OS permissions, GPU/WebGL/map runtime, offline tiles, direct Traccar/network path, restart/recovery, and release/update behaviour.
- Every tester-facing desktop build must have a one-click, sanitized diagnostics export broad enough for remote triage: app/build version, OS/distro/kernel, Electron/Chromium/Node, GPU/WebGL/map status, safeStorage backend, provider base/auth mode/secret-present only, tracking/cache summary, mission-store path/schema/native SQLite load status, diagnostics/export paths, and recent app-owned fault messages. Never ask testers for whole Electron profile zips.
- Hosted Traccar testing uses the Vercel same-origin proxy:
  - app URL: `https://sartracker-web.vercel.app/?missionHarness=1`
  - hosted provider base URL: `https://sartracker-web.vercel.app`
  - direct `http://kmrtsar.eu:8082` is valid for desktop/Tauri, but browsers block it from the HTTPS hosted app. `http://kmrtsar.ddns.net:8082` remains a working fallback. Do not use listener/device ports such as `:5055` as the provider base URL.
- The A3 team-feedback remediation batch is complete locally, pending final verification/deploy for this closeout turn. Completed A3 surfaces include map placement guardrails, drawing rendering/layers, compact Maps/Map Tools, Marker At GR, coordinate conversion accuracy, roster spacing, converter naming/order, Measure in Map Tools, mission mast cleanup, contrast/theme, static operational notes relocation, and A3.9 Weather links.
- A3.9 adds Settings-managed named weather URLs and a compact Weather mast menu. This is external-link navigation only; SAR Tracker does not fetch, parse, or forecast weather.
- R10 compressed this handoff back to a current-state baton. Historical investigation/supporting docs already point to `docs/two-track-execution-workplan.md` as the active queue.
- R11 added a module-level non-goals note to the browser harness store clarifying that sessionStorage is for hosted validation only, not a browser substitute for Tauri/SQLite persistence.
- S3 extracted subtree visibility patching into `src/features/layers/layer-visibility-service.ts`, with `layer-filter-panel` now acting as a thin adapter for catalog persistence + service invocation. Follow-up review added immediate group, helicopter, and GPX overlay-store patching so those nodes do not rely only on async catalog hydration.
- S3 added explicit layer-visibility service tests and kept parity visibility E2E coverage green.
- Tauri packaging recon found a working macOS arm64 `.app` path: `npm run tauri build -- --bundles app` -> `src-tauri/target/release/bundle/macos/sartracker-web.app`. Full `npm run tauri build` still fails at DMG bundling; unsigned/ad-hoc app is rejected by Gatekeeper as expected for the current internal-beta lane.
- B2 (`sartracker-web-xhz`) made desktop beta drops repeatable. Future beta cuts must run `npm run beta:verify` (no `--steps` filters), copy `docs/releases/TEMPLATE.md` into a `sartracker-web-<version>-beta-DRAFT.md`, attach the JSON report path from `tmp/beta-artifacts/`, then drop the `-DRAFT` suffix once the artifact is uploaded and the smoke checklist is signed off. Distribution channel is GitHub Releases draft/prerelease on `donal0c/sartracker-web` with the "internal beta" tag in the title.
- B3 (`sartracker-web-ppr`) initial run on `0.1.0+sha.e7ead2eb093a` surfaced `sartracker-web-zl4` (claim of active-mission-finished-on-quit) and `sartracker-web-el9` (runtime tracking warns "not configured"). The follow-up investigation closed both: zl4 was a false positive (operator-driven finish, not quit-driven), pinned by the new `active_mission_survives_process_restart_without_explicit_finish` Rust persistence test. el9 had two distinct desktop-only root causes — keyring crate had no platform features (mock backend) and macOS App Transport Security blocked WKWebView fetch to plain-HTTP Traccar — both fixed in `000f7d1` (keyring features) and `603771f` (Info.plist NSAllowsArbitraryLoads). End-to-end live test on the rebuilt `.app` (`0.1.0+sha.603771f65431`) populated `tracking-cache.json` with 18 real Traccar devices and rendered live trackpoints on the map within 30 seconds of clicking Resume. `sartracker-web-qmr` filed as P2 follow-up to remove the ATS blanket exception by routing renderer fetch through Rust `reqwest`.

## Traccar Test Details

Use these only for team testing, not as a production secret model.

- Upstream team Traccar server: `http://kmrtsar.eu:8082`
- New HTTPS Traccar web/API server: `https://kmrtsar.eu`
- Current HTTPS validation credentials: `sean` / `sean` (verified 2026-05-19: 3 devices, 3 positions)
- Do not use `https://traccar.kmrtsar.eu` as SAR Tracker's provider base URL; it returns bare `400` for `/api/server` and appears to be the device/listener endpoint.
- Fallback direct Traccar server: `http://kmrtsar.ddns.net:8082`
- Hosted browser provider base URL: `https://sartracker-web.vercel.app`
- Hosted proxy endpoints: `/api/session`, `/api/devices`, `/api/positions`
- Auth mode: `Basic`
- Email/username: `apiuser`
- Password: `apiuser`
- Hosted browser rule: do not enter the direct HTTP upstream URL in the hosted app; use the Vercel provider base URL above.
- Desktop/Tauri rule: the direct HTTP upstream URL is acceptable because browser mixed-content blocking does not apply.

## Active Planning Docs

- `docs/two-track-execution-workplan.md` — canonical queue and next-task order.
- `docs/hosted-browser-testing-plan.md` — product/deployment strategy.
- `docs/team-testing-feedback-loop.md` — tester instructions and bug template.
- `docs/tauri-beta-release-plan.md` — supporting desktop beta release detail.
- `docs/reports/deep-hardening-investigation-2026-05-13.md` — historical evidence only; not an active backlog.

## Next Task

Current directed work: 3-6-26 team feedback intake under `DON-83`. `DON-84` and `DON-86` through `DON-90` are complete and verified; continue through the remaining unblocked children by priority unless Donal redirects.

Existing map lane context: `DON-76` has `DON-77`, `DON-78`, `DON-79`, and `DON-80` done. Next map chunk: `DON-81` planning for mission-area prefetch and offline official-map handling. `DON-60` team-requirements issues are mostly complete (`DON-61` through `DON-66`, `DON-68` through `DON-75`); `DON-67` is parked for team input.

**Resolve DON-35 by clearing its follow-up children before sending the full Electron build wider.** The Dell Ubuntu 24.04 test proves a Linux-native Electron build can render OpenTopoMap and run the packaged `.deb` mission/SQLite/recovery/live-Traccar/tracking-cache/diagnostics path. MacOS-cross-built artifacts are not trustworthy for `better-sqlite3` native modules. Next: `DON-57` moves Electron Linux artifacts to a Linux builder/CI job, `DON-58` fixes/bakes the Linux secret-store launch behavior and diagnostics runtime label, and `DON-59` proves GPX import, marker attachment save/open, and external file opening in the packaged Linux app. Decide separately whether Ubuntu 18.04 is unsupported or needs an older-glibc build. DON-27 now proves the read-only Leaflet fallback route; Electron remains the preferred runtime lane unless DON-29 decides degraded-mode editing is required.

Optional cleanup: the stale `v0.1.0-beta.2` draft release on GitHub holds Linux-only assets from the partial-success run. Delete via `gh release delete v0.1.0-beta.2 --repo donal0c/sartracker-web --yes` once you do not need the evidence trail.

## Open Linear issues That Matter Now

- `sartracker-web-y6a` — B4 cross-platform Tauri beta distribution. CLOSED 2026-05-17 after the published `v0.1.0-beta.3` release.
- `sartracker-web-590` — Re-add macOS arm64 to release CI matrix when build cadence stabilizes. P3, deferred.
- `sartracker-web-g1u` — Re-add Windows MSI artifact when version scheme supports it. P3, deferred (MSI bundler requires numeric-only pre-release suffixes which `-beta.N` is not).
- `DON-24` — B7: Pre-tester smoke + CI launch-smoke for cross-platform Tauri builds. Local workflow hardening implemented 2026-05-18; first CI smoke run `26040183978` exposed harness gaps now fixed locally; needs follow-up release workflow run and real-machine Linux smoke.
- `DON-25` — S8: Linux runtime reliability path for SAR Tracker desktop. Parent issue, In Progress.
- `DON-26` — S8a: Empirical Electron MapLibre Linux validation. Done after old Ubuntu plus two PCLinuxOS team machines rendered maps in Electron.
- `DON-27` — S8b: Add non-WebGL Leaflet raster fallback spike. **Closed 2026-06-02.** Read-only fallback proved behind `?mapRenderer=leaflet`; Leaflet 1.9.4 renders basemap + all critical overlays without WebGL; edit parity deferred to DON-29.
- `DON-28` — S8c: Electron shell bridge and Linux packaging PoC. In Progress; parent for `DON-31` through `DON-35`.
- `DON-29` — S8d: Linux runtime decision checkpoint. Backlog.
- `DON-31` — S8c.1: Electron settings and secret storage parity. Done locally.
- `DON-32` — S8c.2: Electron tracking cache and diagnostics export parity. Done locally.
- `DON-33` — S8c.3: Electron SQLite mission store parity. Done locally.
- `DON-34` — S8c.4: Electron filesystem, GPX, attachments, and file opening parity. Done locally.
- `DON-35` — S8c.5: Electron Linux artifact build and field-validation gate. Dell Ubuntu 24.04 native AppImage + `.deb` smoke rendered OpenTopoMap; packaged `.deb` also proved SQLite mission lifecycle/recovery, live Traccar, tracking cache, and diagnostics export after forcing `--password-store=gnome-libsecret`. MacOS cross-build produced invalid native module and must be replaced with a Linux-native build path.
- `DON-57` — S8c.5a: Build Electron Linux artifacts natively in CI. Done 2026-06-01; GitHub run `26746757159` built AppImage + `.deb`, checked packaged `better_sqlite3.node` as ELF x86-64, generated SHA256SUMS, failed black-window risk with a content-mean gate, rendered OpenTopoMap in CI, and the exact AppImage rendered on the Dell Ubuntu 24.04 machine.
- `DON-58` — S8c.5b: Fix Linux Electron secret-store launch and diagnostics runtime label. Child of `DON-35`.
- `DON-59` — S8c.5c: Smoke packaged Linux filesystem workflows. Child of `DON-35`.
- `DON-60` — B8: 2026-05-28 team requirements from USB ODT. Intake parent for `DON-61` through `DON-75`.
- `DON-76` — Official Irish Map Provider Integration. Parent map lane. Private/local map policy is locked: do not commit licensed map files or credentials, do not bundle them into public releases, and do not expose them through hosted web. Local verified USB copy lives at `/Users/donalocallaghan/SARTracker-private-map-assets/team-usb-2026-06-03`.
- `DON-77` — Evaluate MapGenie sources and choose operational default. Done 2026-06-03: `discovery` is the locked default official operational topo map.
- `DON-78` — Add grouped map catalogue UX for official and public maps. Done 2026-06-03.
- `DON-79` — Prototype local MapGenie rendering via ArcGIS export. Done 2026-06-04: Electron/local runtime renders Discovery Topo and Aerial Imagery via ArcGIS export over Carrauntoohil/Reeks from local configured MapGenie source, with credentials kept out of URLs/settings/diagnostics.
- `DON-80` — Add local official map source import and configuration for Electron. Done 2026-06-03: Settings and Electron persistence can reference the local MapGenie source file while storing only safe metadata and keeping credentials out of diagnostics/source control.
- `DON-81` — Plan mission-area prefetch and offline official-map path. Related to `DON-7`; defer until default/rendering/import path is clearer.
- `DON-82` — Classify relief and slope rasters as optional map overlays. Defer until primary official basemap path is proven.
- `DON-75` — Review tracking-panel helicopter visibility defaults. Done 2026-06-01: helicopter aviation slots are now opt-in in the Tracking panel (assigned slots always show; empty slots collapse behind an Add-helicopter reveal; map visibility unchanged). Committed `26a06c7`, pushed.
- `DON-74` — Fix weather links and colour palette controls. Done 2026-06-02: weather links now accept bare domains (auto-prepends `https://`) before validation/persistence in browser, Tauri, and Electron, and open via the system browser across runtimes; colour-capable controls (drawing fill, text label, device breadcrumb) use a 12-swatch SAR palette plus typed hex instead of plain text or native `<input type="color">`.
- `DON-72` — Clean up drawing/sector/range-ring/search-area map rendering. Done 2026-06-02: root-caused the per-vertex dots and broken visibility toggles to MapLibre 5 dropping nested legacy `$type` filters; switched drawing+tracking overlays to `['geometry-type']` expression filters; added dark line casing, wider strokes, higher-contrast default colours (LPB percentile colours kept); removed the Select button (internal fallback retained) and the Vertices readouts; added text-label drag-to-move persisted via the existing upsert path. Committed and pushed.
- `DON-73` — Layer workspace controls (Collapse All, Move Up/Down, Favorite). Done 2026-06-02: Collapse All added beside Expand All; Move Up/Down verified working end-to-end (reproduction proved it, was a discoverability not a logic bug); inert Favorite controls removed (persisted `isFavorite` schema kept). Committed and pushed.
- `DON-61` — Long-running web slowdown/freeze. Fixed and pushed 2026-06-01 by bounding live breadcrumb snapshots at 20,000 render points.
- `DON-62` — Layer visibility toggles not applying to map. Fixed and pushed 2026-06-01 by batching subtree visibility metadata persistence so out-of-order descendant writes cannot partially re-show hidden layers.
- `DON-63` — Device list focus behavior opens marker list / defocuses map. Fixed and pushed 2026-06-01 by stopping passive device-list cells and nested controls from bubbling into row selection. Committed `6583ee2`.
- `DON-64` — Make paused mission state unmistakable. Fixed and pushed 2026-06-01: flashing bright-red PAUSED chip on mast + Mission Control, red paused banner with frozen-time text and a dedicated Resume Mission button, panel ring, non-collapsible dock while paused, reduced-motion fallback. Pure selector `mission-phase-presentation.ts`. Committed `921e0ae`.
- `DON-65` — Active mission device selection. Done 2026-06-02: Devices Workspace now separates Active Mission Devices from the full roster, supports Add/Remove per device, and filters mission map overlays/breadcrumbs to the active set once selected while preserving the all-devices fallback when no active devices are selected.
- `DON-66` — Per-device breadcrumb trail styling and global breadcrumb size. Done 2026-06-02: Devices Workspace has device colour swatches plus a global Breadcrumb Size slider; configured colours/sizes drive MapLibre and Leaflet fallback breadcrumb rendering through a small persisted frontend style store.
- `DON-67` — Multi-day mission layer grouping. Parked 2026-06-02 for team input; Linear comment lists the operational questions to ask before implementation.
- `DON-68` — Operational contrast/theme/crosshair visibility. Done 2026-06-02: red/white placement cursor, larger red coordinate target, and stronger selected-row treatment for high-use workspace rows.
- `DON-69` — Reduce mission/control panel clutter and relocate low-use status details. Done 2026-06-02: active configured missions can minimize Mission Control while paused/recovery states stay expanded; coordinate footer dropped low-use CRS cells and keeps live coordinates plus Convert.
- `DON-70` — Improve coordinate footer and converter marker workflow. Done 2026-06-02: footer shows DD, Irish Grid, and DMS; converter can open the normal marker form at a converted coordinate.
- `DON-71` — Redesign casualty treatment notes for repeated updates. Done 2026-06-02: casualty marker treatment notes now append timestamped updates into the existing treatment log, preserving earlier entries while avoiding new medical-record schema semantics.
- `sartracker-web-s8m` — B5: triage first web and Tauri beta feedback. Next-task default once testers have published artifacts to react to.
- `sartracker-web-l7c` — Parity sweep findings walk-through (Codex + Donal). P3, queued. Reads `tmp/parity-sweep/sweep-report.md` and triages C1–C13 with Donal.
- `sartracker-web-ag1` — QGIS Parity Residual-Gap Sweep (closed 2026-05-17). Outputs under `tmp/parity-sweep/`.
- `sartracker-web-fy5` — B6 GPX And Drawing Hit-Test Hardening (completed 2026-05-17; ready to close).
- `sartracker-web-n9i` — V2 Visual Review Automation (completed 2026-05-17; ready to close).
- `sartracker-web-b3c` — closed 2026-05-17 after updating the 5 drifted verificationPrompts and re-verifying each via the V2 review runner.
- `sartracker-web-wn6` — Closed 2026-05-17 after rewriting the two drifted verificationPrompts and re-verifying each via `npm run visual:review --only <id> --no-cache`.
- `sartracker-web-zq9` — Closed 2026-05-17. Mast tracking ratio replaced by separate FIX / STALE chips behind a pure selector; new chromium regressions in `tests/e2e/v1-regression.spec.ts` and a new visual entry `mast-tracking-cell-active` (visual review PASS).
- `sartracker-web-2xp` — Closed 2026-05-17. Tile-only filter at `src/features/map/is-tile-error-event.ts` plus widened defaults (5-in-30s) in `src/lib/tile-health-tracker.ts`; interactive Playwright proof at `tmp/2xp-verification/`.
- `sartracker-web-y6a` — B4: set up cross-platform Tauri beta distribution for Windows/Linux testers.
- `sartracker-web-qmr` — closed 2026-05-17 after keychain-backed packaged-app live Traccar smoke.
- `sartracker-web-vpz` — Hosted browser testing mode and parity hardening.
- `sartracker-web-6y3` — A3 team feedback remediation batch; should be closed/reframed once A3.9 verification/deploy is complete.
- `sartracker-web-8gw` — V1 Regression E2E Coverage (closed 2026-05-17).
- `sartracker-web-cgx` — S5 Mission Control View Model Extraction (closed 2026-05-17).
- `sartracker-web-s5v` — S4 Map Overlay Consolidation And Camera Race Fix (closed 2026-05-17).
- `sartracker-web-4a1` — S3 Layer Visibility Service Extraction (completed 2026-05-17; ready to close if no follow-up findings).
- `sartracker-web-xhz` — B2 Tauri Beta Release Template (completed 2026-05-17; ready to close if no follow-up findings).
- `sartracker-web-zl4` — closed 2026-05-17 as false positive; regression test added.
- `sartracker-web-el9` — closed 2026-05-17 with categorical end-to-end proof on packaged `.app` build `0.1.0+sha.603771f65431`.
- `sartracker-web-ppr` — B3 closed 2026-05-17 after re-run on the post-fix build.

Older parity/UI Linear issues still exist, but new work should be selected through the two-track workplan unless the user explicitly asks for a specific Linear issue.

## Known Limits

- Browser mode is not durable enough for live incidents.
- Browser mode does not have IndexedDB mission persistence, browser backup/export/import, or browser-native filesystem parity.
- Desktop/Tauri is the intended operational lane for SQLite persistence, filesystem workflows, diagnostics, GPX watch/import, archives, and proprietary HD map packages.
- High-definition mountain maps should be local desktop map packages unless requirements change.

## Verification Snapshot

Most recent verification in this turn (`DON-79` local MapGenie rendering prototype):

- Passed focused red/green coverage for map catalogue/style/protocol/proxy/preferences/settings refresh: `npx vitest run tests/unit/browser-settings-store.test.ts tests/unit/map-config.test.ts tests/unit/map-style.test.ts tests/unit/official-map-export.test.ts tests/unit/basemap-switcher.test.ts tests/unit/official-map-protocol.test.ts tests/unit/electron-official-map-proxy.test.ts tests/unit/map-preferences.test.ts` — 8 files / 29 tests.
- Passed real local source probe through `electron/official-map-proxy.cjs` over MacGillycuddy Reeks / Carrauntoohil: `official_discovery_topo` z14 x7748 y5411 returned `image/png`, 159290 bytes; `official_aerial_imagery` z14 x7748 y5411 returned `image/png`, 116702 bytes.
- Passed inbuilt Browser default-state check at `http://127.0.0.1:1420/?missionHarness=1`: Maps menu showed official entries disabled as not configured and public fallback maps selectable.
- Passed: `npm run test` — 138 files / 686 tests.
- Passed: `npm run build` — bundle budgets clean.
- Passed: `npm run lint`.
- Passed: `npm run test:backend` — 46 passed / 1 ignored.
- Passed Electron syntax checks: `node --check electron/main.cjs && node --check electron/preload.cjs && node --check electron/official-map-proxy.cjs`.
- Passed Electron/local visual proof with isolated temp user data and the copied local MapGenie source configured: Maps menu enabled Discovery Topo and Aerial Imagery with no Not Configured labels; Discovery Topo and Aerial Imagery both reached `loaded=true` and `areTilesLoaded=true`, rendered around Carrauntoohil/Reeks, and showed ready badges. During this proof, `better-sqlite3` was rebuilt for Electron ABI, then rebuilt back for Node before final verification.
- Passed closeout: `npm run test`, `npm run build`, `npm run lint`, `npm run test:backend`, and `git diff --check`.

Previous verification in this turn (`DON-72` drawing rendering cleanup + legibility):

- Red reproduced: `npx vitest run tests/unit/sync-drawing-overlay.test.ts tests/unit/sync-tracking-overlay.test.ts tests/unit/drawing-geojson.test.ts tests/unit/drawing-toolbar.test.ts tests/unit/drawing-dialog.test.ts tests/unit/start-drawing-runtime.test.ts tests/unit/text-label-drag.test.ts` failed before implementation (legacy `$type` filters present, no casing layer, muddy green ring default, Select button present, Vertices readouts present, no `moveTextLabel`, missing `text-label-drag` module).
- Passed focused coverage after implementation: same set — 9 files / 56 tests.
- Passed: `npm run test` — 131 files / 648 tests.
- Passed: `npx tsc --noEmit`, `npm run lint`, `npm run build` (bundle budgets clean).
- Passed: `cargo test` (src-tauri) — 45 passed / 1 ignored.
- Passed: `git diff --check`.
- Passed browser validation (Chrome DevTools, Donal explicitly approved) at `http://127.0.0.1:1420/?missionHarness=1`: confirmed the live `mission-drawings-point` filter is the expression form (not `UNDEFINED`); seeded search area + LPB rings + sector + text label render as clean continuous lines with a dark casing and NO per-vertex dots; LPB rings keep green/amber/red percentile colours; Map Tools chip reads SELECT with no Select button in the expanded list; dragging the "Command Post" label moved its anchor from `[-9.696,51.974]` to `[-9.6848,51.9703]` in both geometry and metadata, and the moved anchor survived a page reload.

Previous verification in this turn (`DON-71` casualty treatment update log):

- Red reproduced: `npx vitest run tests/unit/marker-draft.test.ts tests/unit/marker-dialog-treatment-log.test.ts` failed because `appendTreatmentUpdate` did not exist and the casualty form had no `marker-treatment-log-input`.
- Passed focused coverage: `npx vitest run tests/unit/marker-draft.test.ts tests/unit/marker-dialog-treatment-log.test.ts` — 2 files / 11 tests.
- Passed inbuilt Browser validation at `http://127.0.0.1:1420/?missionHarness=1`: started `DON-71 Browser Validation`, used footer Convert with `V 82179 81264`, clicked `Create Marker Here`, switched Marker Details to Casualty, confirmed the treatment log/update controls, appended `Warm drink given` as `[2026-06-02 16:13] Bravo: Warm drink given` after an existing Alpha entry, and confirmed the new-update field cleared.
- Passed: `npx tsc --noEmit`, `npm run lint`.
- Passed: `npm run test` — 127 files / 628 tests.
- Passed: `npm run build` — bundle budgets clean.
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: `git diff --check`.

Previous verification in this turn (`DON-69` panel clutter reduction):

- Red reproduced: `npx vitest run tests/unit/mission-control-panel-collapse.test.ts tests/unit/coordinate-bar.test.ts` failed because Mission Control had no collapse button and the coordinate footer still exposed `Map CRS` / `Work CRS`.
- Browser validation first failed at `http://127.0.0.1:1420/?missionHarness=1` with an empty app root and React "Maximum update depth exceeded" in `MapLibreMapView`; root cause was a fresh object-literal Zustand selector for tracking style preferences in the MapLibre and Leaflet map renderers.
- Red reproduced: `npx vitest run tests/unit/tracking-style-store-selector.test.ts` failed because the selector-safe `useTrackingStylePreferences()` hook did not exist.
- Passed focused coverage: `npx vitest run tests/unit/tracking-style-store-selector.test.ts tests/unit/mission-control-panel-collapse.test.ts tests/unit/coordinate-bar.test.ts` — 3 files / 5 tests.
- Passed inbuilt Browser validation at `http://127.0.0.1:1420/?missionHarness=1`: map shell and coordinate footer rendered; footer text did not include `Map CRS` or `Work CRS`; active mission `DON-69 Browser Validation` exposed `Minimize`, collapsed to a summary with `Expand` visible and `mission-pause-resume-btn` absent; after expanding and pausing, `mission-paused-banner` and `mission-pause-resume-btn` were visible and no collapse/expand affordance was present.
- Passed: `npx tsc --noEmit`, `npm run lint`.
- Passed: `npm run test` — 125 files / 623 tests.
- Passed: `npm run build` — bundle budgets clean.
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: `git diff --check`.

Previous verification in this turn (`DON-68` contrast and crosshair):

- Red reproduced: `npx vitest run tests/unit/map-location-target-style.test.ts tests/unit/devices-workspace.test.ts tests/unit/mission-review-selection-contrast.test.ts` failed because coordinate target specs/helper and selected-row classes did not exist.
- Red reproduced: `npx vitest run tests/unit/map-operational-cursor.test.ts` failed because the red armed-tool cursor helper did not exist.
- Passed focused coverage: `npx vitest run tests/unit/map-location-target-style.test.ts tests/unit/map-operational-cursor.test.ts tests/unit/devices-workspace.test.ts tests/unit/mission-review-selection-contrast.test.ts` — 4 files / 6 tests.
- Passed: `npm run test` — 122 files / 618 tests.
- Passed: `npm run lint`, `npx tsc --noEmit`, `npm run build` (bundle budgets clean).
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: `git diff --check`.
- UI/browser caveat: no Playwright/Chrome DevTools validation was run because Donal's standing instruction forbids those tools unless explicitly requested, and the inbuilt Browser tool was unavailable in this session.

Previous verification in this turn (`DON-66` breadcrumb styling):

- Red reproduced: `npx vitest run tests/unit/tracking-geojson.test.ts tests/unit/sync-tracking-overlay.test.ts tests/unit/leaflet-fallback-renderer.test.ts tests/unit/devices-workspace.test.ts` failed because configured colours/sizes were ignored and `tracking-style-store` did not exist.
- Passed after implementation: same targeted run — 4 files / 23 tests.
- Passed broader targeted coverage: `npx vitest run tests/unit/tracking-geojson.test.ts tests/unit/sync-tracking-overlay.test.ts tests/unit/leaflet-fallback-renderer.test.ts tests/unit/devices-workspace.test.ts tests/unit/device-workspace-model.test.ts tests/unit/mission-active-tracking.test.ts tests/unit/tracking-viewport.test.ts` — 7 files / 32 tests.
- Passed: `npm run test` — 119 files / 615 tests.
- Passed: `npm run lint`, `npx tsc --noEmit`, `npm run build` (bundle budgets clean).
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: `git diff --check`.
- UI/browser caveat: component tests verified the visible Devices Workspace colour/size controls, and renderer tests verified MapLibre/Leaflet styling. No Playwright/Chrome DevTools validation was run because Donal's standing instruction forbids those tools unless explicitly requested, and the inbuilt Browser tool was unavailable in this session.

Previous verification in this turn (`DON-65` active mission device selection):

- Red reproduced: `npx vitest run tests/unit/device-workspace-model.test.ts tests/unit/mission-active-tracking.test.ts tests/unit/devices-workspace.test.ts` failed because the active mission selector/store did not exist and workspace rows had no active state.
- Passed after implementation: same targeted unit run — 3 files / 7 tests.
- Passed broader targeted coverage: `npx vitest run tests/unit/device-workspace-model.test.ts tests/unit/mission-active-tracking.test.ts tests/unit/devices-workspace.test.ts tests/unit/leaflet-fallback-renderer.test.ts tests/unit/sync-tracking-overlay.test.ts tests/unit/tracking-viewport.test.ts` — 6 files / 24 tests.
- Passed: `npm run test` — 119 files / 611 tests.
- Passed: `npm run lint`, `npx tsc --noEmit`, `npm run build` (bundle budgets clean).
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: `git diff --check`.
- UI/browser caveat: component tests verified the visible Devices Workspace Add/Remove split and full-roster preservation. No Playwright/Chrome DevTools validation was run because Donal's standing instruction forbids those tools unless explicitly requested, and the inbuilt Browser tool was unavailable in this session.

Previous verification in this turn (`DON-73` layer workspace controls):

- Red reproduced/confirmed: new published-tree assertion in `tests/unit/start-layer-catalog-runtime.test.ts` shows the runtime re-sorts the tree after reorder (Bravo→above Alpha). This test passed immediately — proving Move Up/Down logic was never broken; the gap was the old test only checking `upsertMetadata` calls, not the rebuilt tree.
- Passed: `npm run test` — 118 files / 607 tests.
- Passed: `npx tsc --noEmit`, `npm run lint`, `npm run build` (bundle budgets clean).
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: `npx playwright test tests/e2e/layer-panel.spec.ts tests/e2e/parity-layer-console.spec.ts --project=chromium` — 10/10, including new specs for Move Up reorder (Bravo moves above Alpha), Collapse All↔Expand All, and Favorite-control absence.
- Passed (browser-backed): Playwright run at `http://127.0.0.1:1420/?missionHarness=1` (`tmp/don-73-verification/`): favorite controls absent (inspector=0, star=0); order before `alpha, bravo` → after Move Up `bravo, alpha`; Collapse All hid device rows while the Tracking group stayed visible; Expand All restored them. Clipped panel screenshot shows Expand All / Collapse All side-by-side and no star buttons on rows.

Previous verification in this turn (`DON-75` opt-in helicopter slots):

- Red reproduced: `npx vitest run tests/unit/helicopter-panel-slots.test.ts` failed because `selectHelicopterPanelSlots` did not exist.
- Passed after implementation: `npx vitest run tests/unit/helicopter-panel-slots.test.ts tests/unit/start-helicopter-runtime.test.ts` — 8 tests.
- Passed: `npm run test` — 118 files / 606 tests.
- Passed: `npx tsc --noEmit`, `npm run lint`, `npm run build` (bundle budgets clean).
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: `npx playwright test tests/e2e/helicopter-panel.spec.ts --project=chromium` — 1/1 (updated to reveal empty slots before assigning; still covers persist → map overlay → layer-toggle).
- Passed (browser-backed): Playwright check at `http://127.0.0.1:1420/?missionHarness=1` (`tmp/don-75-verification/`): default empty state with "Add helicopter (4 slots free)" and no rendered slots; reveal shows slot_1..4; after assigning Rescue 118 and Hide empty slots, slot_1 stays visible, slot_2 is hidden, control reads "Add another helicopter (3 slots free)". Screenshots inspected.

Previous verification in this turn (`DON-64` make paused state unmistakable):

- Red reproduced: `npx vitest run tests/unit/mission-phase-presentation.test.ts` failed because the `selectMissionPhasePresentation` selector did not exist.
- Passed after implementation: `npx vitest run tests/unit/mission-phase-presentation.test.ts` — 4 tests.
- Passed: `npm run test` — 117 files / 600 tests.
- Passed: `npm run lint`, `npx tsc --noEmit`, `npm run build` (bundle budgets clean).
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed (user-requested Playwright): `npx playwright test tests/e2e/mission.spec.ts --project=chromium` — 12/12, including new DON-64 paused assertions (red PAUSED chip, visible `mission-paused-banner` with enabled Resume, `data-mission-phase="paused"`).
- Passed (Playwright): `npx playwright test tests/e2e/visual/visual-mission-lifecycle.spec.ts --project=visual` — 6/6.
- Passed (independent AI visual review): `npm run visual:review -- --only mission-paused-state --no-cache` — PASS; Opus reviewer confirmed all 9 checklist items (red PAUSED chip, red MISSION PAUSED banner with frozen-time text and RESUME MISSION button, ELAPSED > ACTIVE SEARCH, Resume + enabled Finish, no name input/Start). Screenshot manually inspected.
- Also captured the command mast in paused state via a throwaway Playwright spec (since removed): mast renders the red PAUSED chip next to MISSION.
- Pre-existing, unrelated: `tests/e2e/devices-workspace.spec.ts:16` fails identically with these changes `git stash`ed — not introduced by DON-64.

Most recent local verification in this turn (`DON-63` device list focus behavior):

- Red reproduced: `npx vitest run tests/unit/devices-workspace.test.ts` failed because clicking Bravo's passive status cell selected `bravo` instead of leaving the selected device as `alpha`.
- Passed after fix: `npx vitest run tests/unit/devices-workspace.test.ts tests/unit/device-workspace-model.test.ts` — 2 files / 3 tests.
- Passed: `npm run test` — 116 files / 596 tests.
- Passed: `npm run lint`.
- Passed: `npm run build`.
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: inbuilt-browser smoke at `http://127.0.0.1:1420/?missionHarness=1` confirmed the app shell, MapLibre canvas, and OpenTopoMap readiness; browser console had no errors. Full Playwright E2E was not run because Donal explicitly restricted Playwright/DevTools use unless requested.

Previous local verification in this turn (`DON-62` layer visibility toggles not applying to map):

- Red reproduced: `npx vitest run tests/unit/layer-stale-refresh-integration.test.ts` failed because `controller.setNodeVisibilities` did not exist, exposing the missing atomic subtree visibility persist boundary.
- Passed after fix: `npx vitest run tests/unit/layer-stale-refresh-integration.test.ts tests/unit/start-layer-catalog-runtime.test.ts tests/unit/layer-visibility-service.test.ts tests/unit/layer-visibility-store.test.ts tests/unit/map-layer-filters.test.ts` — 5 files / 30 tests.
- Passed: `npm run test` — 115 files / 595 tests.
- Passed: `npm run lint`.
- Passed: `npm run build`.
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: inbuilt-browser smoke at `http://127.0.0.1:1420/?missionHarness=1` confirmed the browser shell, map surface, and Layers panel render; browser console had no errors. Full Playwright E2E was not run because Donal explicitly restricted Playwright/DevTools use unless requested.

Previous local verification in this turn (`DON-61` long-running browser slowdown/freeze):

- Red reproduced: `npx vitest run tests/unit/tracking-geojson.test.ts` failed because a simulated long-run breadcrumb snapshot reached 24,600 live points instead of the 20,000 render budget.
- Passed after fix: `npx vitest run tests/unit/tracking-geojson.test.ts tests/unit/polling-manager.test.ts tests/unit/start-tracking-runtime.test.ts tests/unit/browser-harness-store.test.ts` — 4 files / 33 tests.
- Passed: `npm run test` — 115 files / 594 tests.
- Passed: `npm run lint`.
- Passed: `npm run build`.
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: inbuilt-browser smoke at `http://127.0.0.1:1420/?missionHarness=1` confirmed browser testing shell, map surface, mission surface, and tracking surface render. Heavy seeded UI verification was not run because the inbuilt-browser tab did not expose the browser harness API and Donal has explicitly restricted Playwright/DevTools use unless requested.

Most recent remote verification in this turn (DON-35 Dell Ubuntu 24.04 packaged Electron smoke):

- Passed: native Linux Electron build on `donal-Precision-5570`; `better_sqlite3.node` is ELF x86-64. AppImage SHA256 `48c203fa7057da014798af14d8833cc42a2069d407e8d8e82f64c863075bea78`; `.deb` SHA256 `af7e5e65144cfe81f2f026de7294a3eb89be632a0b8ecb355d1db88518021816`.
- Passed: native AppImage and installed `.deb` rendered SAR Tracker and OpenTopoMap. AppImage required `--no-sandbox` on Ubuntu 24; `.deb` launched without that workaround.
- Passed: Dell native focused unit/integration parity after restoring Node ABI with `npm rebuild better-sqlite3` — 12 files / 24 tests across Electron settings, runtime files, SQLite mission store, Traccar fetch, tracking cache, diagnostics/support report, GPX import source, layer catalog, marker attachments, and adapter coverage.
- Passed through packaged `.deb` UI: created `Dell Smoke Mission`, SQLite WAL stored it, `pragma integrity_check` returned `ok`, process restart exposed recovery state, Resume restored active state and wrote `mission_resumed`.
- Passed through packaged `.deb` UI with `--password-store=gnome-libsecret`: Settings save/connect persisted Traccar config, live tracking connected to `https://kmrtsar.eu`, map rendered live device markers, mast/sidebar showed ONLINE with 33 devices / 5 fixes / 3 stale, SQLite had 33 devices and live position rows, and `tracking-cache.json` contained 33 devices / 5 positions.
- Passed: Diagnostics workspace showed active mission, tracking online, database path, backup path, provider URL, and cache state; Export Report wrote `~/.config/sartracker-web/diagnostics-reports/diagnostics-report-2026-05-31T19-58-46-955Z.txt`. Report included Electron/Chromium/Node/Linux/safeStorage details and did not contain the typed credential string.
- Blocked/not fully proved: secret save fails on this machine unless Electron is launched with `--password-store=gnome-libsecret`; diagnostics runtime label still says `browser validation` inside Electron; packaged GPX import, marker attachment save/open, and external file opening remain to be manually smoke-tested.

Most recent CI/remote verification in this turn (DON-57 Electron Linux CI workflow):

- Passed: GitHub Actions run `26746757159` on head `32cb2e315ad61449270f40f2bee2bb6a71e0fd56`: https://github.com/donal0c/sartracker-web/actions/runs/26746757159
- Passed: AppImage SHA256 `000f7e4d44692476ce5873a5ef0bd8901a22848c1e8179c512628cfd0438acd2`; `.deb` SHA256 `58aee86e305e504a9a419dcb2873c7a7840fa8922b0233c78c32937f58031915`.
- Passed: packaged SQLite native module inspection reported `better_sqlite3.node: ELF 64-bit LSB shared object, x86-64`.
- Passed: CI AppImage launch under Xvfb rendered SAR Tracker/OpenTopoMap; `appimage-content-mean.txt` was `0.499086`, so the black-window gate passed.
- Passed: exact GitHub-built AppImage copied to `donal-Precision-5570` Ubuntu `24.04.2 LTS`; after unlocking the Wayland session, screenshot showed SAR Tracker, OpenTopoMap tiles, recovery state, and `OpenTopoMap basemap ready`.
- Passed locally before/after workflow changes: `actionlint .github/workflows/*.yml`, `npx vitest run tests/unit/vite-config.test.ts`, `npm run lint`, `npm run build`, `npm run test` — 115 files / 593 tests, `npm run test:backend` — 45 passed / 1 ignored, and `git diff --check`.

Most recent local verification in this turn (DON-27 Leaflet read-only fallback):

- Passed: focused Leaflet fallback tests — `npx vitest run tests/unit/map-renderer-mode.test.ts tests/unit/leaflet-fallback-renderer.test.ts` — 2 files / 5 tests.
- Passed: `npm run lint`.
- Passed: `npm run test` — 114 files / 592 tests.
- Passed: `npm run build` — bundle budgets clean.
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: inbuilt-browser validation at `http://127.0.0.1:1420/?missionHarness=1&mapRenderer=leaflet&leafletFallbackSeed=1`: Leaflet container present, MapLibre canvas absent, `15/15` Leaflet tiles loaded, mission `DON-27 Leaflet fallback surface`, read-only fallback badge visible in DOM, tooltips `Alpha Team` and `LKP`, and drawing marker-pane text `Sector Alpha`.

Most recent local verification in this turn (DON-28 Electron parity batch):

- Passed: focused Electron parity tests — 12 files / 38 tests.
- Passed: `npm run test` — 112 files / 587 tests.
- Passed: `npm run test:backend` — 45 passed / 1 ignored.
- Passed: `npm run build` — bundle budgets clean.
- Passed: `npm run lint`.
- Passed: `node --check electron/main.cjs electron/preload.cjs electron/settings-store.cjs electron/runtime-files.cjs electron/mission-store.cjs electron/file-system.cjs`.
- Passed: `npm run electron:pack`; local packaged `.app` stayed running for a 6-second launch smoke.
- Passed: `npm run electron:dist:linux`; produced `tmp/electron-dist/sartracker-electron-validation_0.1.0-beta.3_linux_x86_64.AppImage` (142 MB) and `tmp/electron-dist/sartracker-electron-validation_0.1.0-beta.3_linux_amd64.deb` (94 MB). These were not executed locally because the host is macOS.
- Note: after Linux cross-build on macOS, `npm rebuild better-sqlite3` was required to restore local Node/macOS tests.

Most recent CI verification (B4 first end-to-end run on `v0.1.0-beta.3`):

- Run ID `26002563213` — https://github.com/donal0c/sartracker-web/actions/runs/26002563213
- Started 2026-05-17 20:59 UTC, finished 21:23 UTC, total ~24 min wall-clock.
- Conclusion: SUCCESS for every job.
- Gates (ubuntu-22.04): 7m7s. Tag/version trio matched, release-notes file found, lint clean, vitest 559 pass, web build clean (bundle budgets OK), cargo test 43 pass.
- Bundle linux-x86_64 (ubuntu-22.04): 11m9s. Produced `sartracker-web_0.1.0-beta.3_linux_amd64.AppImage` (92.9 MB) and `sartracker-web_0.1.0-beta.3_linux_amd64.deb` (15.8 MB).
- Bundle windows-x86_64 (windows-2022): 16m5s. Produced `sartracker-web_0.1.0-beta.3_windows_x64.exe` (12.9 MB) — NSIS, currentUser install.
- Checksums (ubuntu-22.04): 18s. Generated and uploaded `SHA256SUMS` (335 bytes).
- Asset name pattern landed correctly (linux_amd64 / windows_x64); zero "Unexpected input" warnings on this run.
- Artifact SHA256s recorded in the Linear issue `sartracker-web-y6a` final comment for audit; the `SHA256SUMS` asset on the published release is the source of truth.

Failed runs prior to success this turn (per immutability rule, retained on remote):
- `v0.1.0-beta.1` (run `26001753808`, 3m56s): gates failed at cargo test — gates job did not install GTK/WebKit apt deps. Fix: add the same apt step to gates.
- `v0.1.0-beta.2` (run `26001903566`, 23m38s): Linux bundle succeeded (assets uploaded to draft release `v0.1.0-beta.2`); Windows MSI bundler rejected the `-beta.N` pre-release suffix (`pre-release identifier must be numeric-only and cannot be greater than 65535 for msi target`). Fix: drop MSI from beta lane; tracked by `sartracker-web-g1u`. Stale `v0.1.0-beta.2` draft release retained on GitHub Releases for evidence; safe to delete via `gh release delete v0.1.0-beta.2 --repo donal0c/sartracker-web --yes` when convenient.

Local verification of the workflow + config edits (pre-tag):
- Workflow YAML parses cleanly via Python `yaml.safe_load`.
- `actionlint` (1.7.12) passes with no findings on `.github/workflows/release.yml`.
- `npm run tauri info` confirms `tauri.conf.json` validates against the config schema (NSIS `currentUser`, WebView2 `downloadBootstrapper`, LZMA compression). First attempt with `installMode: perUser` was rejected — schema requires `currentUser`/`perMachine`/`both`.
- `cargo check --quiet` from `src-tauri/` returns clean.
- `npm run lint` clean. `npm run test` 559/559. `npm run build` bundle budgets clean. `npm run test:backend` 43 pass / 1 ignored.

Research artefacts retained at `tmp/b4-research/`: `w1-linux-tauri2-2026.md`, `w1-windows-tauri2-2026.md`, `w1-github-actions-tauri-release-2026.md` (~1500 lines combined).

Most recent local verification in this turn (polish chunk: `sartracker-web-wn6`, `sartracker-web-zq9`, `sartracker-web-2xp`):

- Red-then-green for each new pure module: `tests/unit/command-mast-tracking-readout.test.ts` (8), `tests/unit/is-tile-error-event.test.ts` (6), and the new tile-health default-threshold tests in `tests/unit/tile-health-tracker.test.ts` (+4). Each suite failed red on the missing module / un-bumped defaults before implementation, then went green after.
- Passed: `npm run lint` and `npx tsc --noEmit`.
- Passed: `npm run test` — 100 files / 559 tests (was 98 files / 541 tests; +18 new tests).
- Passed: `npm run build` — bundle budgets clean.
- Passed: `npx playwright test --project=chromium` — 89 tests (was 87; +2 new mast regressions in `tests/e2e/v1-regression.spec.ts`).
- Passed: `npx playwright test --project=visual` — 28 tests (was 27; +1 `mast-tracking-cell-active`).
- Passed: `npm run visual:review --no-cache` — 28/28 PASS / 0 FAIL / 0 ERROR. Includes the rewritten `marker-hazard-dialog` and `shell-idle-state` prompts (wn6) and the new `mast-tracking-cell-active` entry (zq9).
- Passed: `npm run test:backend` — 43 passed / 1 ignored.
- Interactive Playwright sanity check (`tmp/2xp-verification/verify-2xp.mjs`) against a live `npm run dev` server proves the 2xp behavior end-to-end: initial badge "OpenTopoMap basemap ready" (no false positive on first paint); 4 non-tile errors → still ready (filter works); 4 tile errors burst → still ready (under 5-error threshold); 5 tile errors burst → degraded (threshold honored); idle while errors still in window → stays degraded (no premature recovery). Evidence screenshots `01-04` saved under `tmp/2xp-verification/`.
- Cleanup: dev server stopped after verification; no background processes left running.

Most recent local verification in this turn (B6 hit-test hardening, `sartracker-web-fy5`):

- Red-then-green: new `tests/unit/gpx-hit-testing.test.ts` and `tests/unit/map-click-target-resolver.test.ts` failed red on the missing modules, then 21/21 pass after implementation.
- Passed: `npm run test` — 98 files / 541 tests (was 96 files / 520 tests before B6; +2 new test files, +21 new tests).
- Passed: `npx tsc --noEmit`.
- Passed: `npm run lint`.
- Passed: `npm run build` — bundle budgets clean.
- Passed: `npx playwright test --project=chromium` — 87 tests (was 85; +2 new `tests/e2e/hit-test-priority.spec.ts` cases pinning marker-wins-stacked and polygon-wins-empty).
- Passed: `npx playwright test --project=visual` — 27 tests.
- Passed: `npm run test:backend` — 44 passed / 1 ignored.
- Interactive Playwright sanity check (script `tmp/b6-verification/verify-b6.py`) against a live `npm run dev` server: marker placed at (700,420), polygon drawn around it, Select tool, click on marker pixel → marker edit dialog opened with the saved name `LKP under polygon` (screenshot `tmp/b6-verification/03-MARKER-WINS-priority-fix.png`). Subsequent click on empty polygon area → polygon edit dialog opened with the saved name `Search Area Verify` (screenshot `tmp/b6-verification/04-POLYGON-WINS-when-marker-out-of-radius.png`). Confirms the documented priority `marker > drawing > empty` end-to-end.
- Cleanup: dev server stopped after verification; no background processes left running.

Most recent local verification in this turn (V2 Visual Review Automation, `sartracker-web-n9i`):

- Red-then-green: new `tests/unit/visual-review-lib.test.ts` failed red against the missing `build/visual-review-lib.js`, then 44/44 pass after implementation.
- Passed: `npm run test` — 96 files / 520 tests (was 476 before V2; added 44 unit tests).
- Passed: `npm run lint`.
- Passed: `npx tsc --noEmit`.
- Passed: `npm run build` — bundle budgets clean.
- Passed: `npx playwright test --project=chromium` — 85 tests.
- Passed: `npx playwright test --project=visual` — 27 tests.
- Passed: `npm run test:backend` — 43 passed / 1 ignored.
- Live single-entry review with real Opus call: `npm run visual:review -- --only shell-idle-state` returned PASS in ~11 s; per-entry result file written.
- Live full-batch review: `npm run visual:review --no-cache --concurrency 3` produced 22 PASS / 5 FAIL / 0 ERROR across 27 entries with deterministic output. The 5 FAILs are real spec-drift findings filed as `sartracker-web-b3c` (verification prompts asking for content outside the captured element); these are out of scope for V2.
- Cached re-run: `npm run visual:review` returned 27/27 cache hits, total runtime under 2 seconds, exit code 1 (matching the live findings).
- Empty-manifest run returns exit 3 (`OVERALL: FAIL  (no manifest entries — did the visual Playwright project run?)`).
- `--only nonexistent` returns exit 2 with a clear error message.
- Cleanup: no background processes left running.

Most recent local verification in this turn (`sartracker-web-qmr`):

- Red-then-green: `npm run test -- tests/unit/tauri-traccar-fetch.test.ts tests/unit/start-app-runtime.test.ts` failed before the Tauri fetch adapter/runtime wiring existed, then passed after implementation.
- Passed: `npm run test -- tests/unit/tauri-traccar-fetch.test.ts tests/unit/start-app-runtime.test.ts tests/unit/traccar-client.test.ts` — 16 tests.
- Passed: `cargo test traccar_http_request --lib` from `src-tauri/` — 2 tests.
- Passed: `npx tsc --noEmit`, `npm run lint`, `npm run build`.
- Passed: `npm run test` — 95 files / 476 tests.
- Passed: `npm run test:backend` — 43 passed / 1 ignored.
- Passed: `npx playwright test --project=chromium` — 85 tests.
- Passed: `npx playwright test --project=visual` — 27 tests.
- Passed: `npm run tauri build -- --bundles app`; output app at `src-tauri/target/release/bundle/macos/sartracker-web.app`.
- Passed: packaged app `Info.plist` inspection confirmed `NSAppTransportSecurity` does not exist, so there is no `NSAllowsArbitraryLoads` blanket in the built `.app`.
- Passed: keychain-backed live desktop Traccar smoke on the packaged `.app`. After resuming the recovered mission, the mast showed `DEVICES 18 ONLINE 14/14`, the tracking panel showed `ONLINE DEVICES 18 FIXES 14`, `tracking-cache.json` refreshed from the 85-byte placeholder to 499,490 bytes, and SQLite held `devices=18 / positions=1884`.
- Cleanup: stopped the packaged app process after the smoke so it was not left polling in the background.

Most recent local verification in this turn (V1 Regression E2E Coverage):

- Red-then-green: new test in `tests/unit/start-tracking-runtime.test.ts` failed before the runtime cold-start-from-cache `applyStatus` call was added, then passed after.
- Red-then-green confirmed for `tests/unit/layer-stale-refresh-integration.test.ts` by temporarily disabling the catalog invalidation guard in `start-layer-catalog-runtime.ts`; the new spec failed red as expected, then passed after restoring the guard.
- Passed: `npm run test` — 94 files / 474 tests.
- Passed: `npx tsc --noEmit`, `npm run lint`, `npm run build` (bundle budgets OK).
- Passed: `npx playwright test --project=chromium` — 85 tests, including the new `tests/e2e/v1-regression.spec.ts` and the rewired `tests/e2e/parity-visibility.spec.ts` (no `waitForTimeout` left).
- Passed: `npx playwright test --project=visual` — 27 tests.
- Passed: `npm run test:backend` — 41 passed / 1 ignored.
- Browser-backed verification ran via Playwright at `http://127.0.0.1:1420/?missionHarness=1`. No Vercel redeploy needed; the only new operator-facing runtime change is the cold-start-offline status copy, which is exercised by the new E2E.

Most recent local verification in this turn (S5 mission-control view-model extraction):

- Red checks first: new hook tests failed because `use-mission-timer` and `use-mission-control-view-model` did not exist.
- Passed: `npm run test -- tests/unit/use-mission-timer.test.ts tests/unit/use-mission-control-view-model.test.ts`.
- Passed: `npm run test -- tests/unit/use-mission-timer.test.ts tests/unit/use-mission-control-view-model.test.ts tests/unit/start-mission-runtime.test.ts tests/unit/mission-tracking-status-bridge.test.ts`.
- Passed: `npx tsc --noEmit`.
- Passed: `npm run lint`.
- Passed: `npm run test` — 93 files / 470 tests.
- Passed: `npm run build`.
- Passed: `npm run test:backend` — 41 passed / 1 ignored.
- Not run: Playwright E2E/browser automation because local instructions require explicit user approval before using Playwright.

Most recent local verification in this turn (S4 overlay/camera consolidation):

- Red checks first: new overlay primitive test failed on missing module; camera preservation test failed because the current implementation restored on `idle` and jumped immediately.
- Passed: `npm run test -- tests/unit/map-overlay-primitives.test.ts tests/unit/apply-map-style-preserving-camera.test.ts tests/unit/sync-tracking-overlay.test.ts tests/unit/map-layer-filters.test.ts tests/unit/drawing-geojson.test.ts tests/unit/marker-geojson.test.ts tests/unit/measurement-geojson.test.ts tests/unit/gpx-geojson.test.ts tests/unit/start-helicopter-runtime.test.ts`.
- Passed: `npx tsc --noEmit`.
- Passed: `npm run lint`.
- Passed: `npm run test` — 91 files / 465 tests.
- Passed: `npm run build`.
- Passed: `npm run test:backend` — 41 passed / 1 ignored.
- Not run: Playwright E2E/browser automation because local instructions require explicit user approval before using Playwright. The app code path affected is covered by unit/build checks; use the inbuilt browser or an explicitly approved E2E pass for rendered basemap switching if needed.

Most recent local verification in this turn (B3 re-run on `0.1.0+sha.603771f65431`):

- Passed: `npm run beta:verify -- --no-smoke` end-to-end (lint, build, test 461/461, test-backend 41/41 + 1 ignored, package). Report at `tmp/beta-artifacts/verify-0.1.0-sha.603771f65431-2026-05-17T08-01-33Z.json`.
- New regression tests, all passing: `active_mission_survives_process_restart_without_explicit_finish` (Rust), `builds_runtime_bootstrap_with_traccar_config_when_auto_connect_is_enabled_and_secret_present` (Rust), `keyring_secret_store_writes_to_real_macos_keychain` (Rust, `#[ignore]`'d, run locally with `--ignored` and confirmed against the real macOS keychain), `starts the poller when the runtime config is present` (TS).
- Live end-to-end smoke on the packaged `.app` (`src-tauri/target/release/bundle/macos/sartracker-web.app`, build `0.1.0+sha.603771f65431`):
  - Item 1 (launches): PASS — multiple PIDs across the session.
  - Item 2 (version chip): PASS — mast read `0.1.0+SHA.603771F65431` in screenshot `09-tracking-live-with-devices.png`.
  - Item 3 (start mission): PASS — direct UI Start initially, then SQLite seed + Resume click in the post-fix run; mast read `MISSION ACTIVE` with elapsed/active timers ticking.
  - Item 4 (persists across quit/restart): PASS — `kill -TERM` and AppleScript quit both leave mission status unchanged in SQLite, no `mission_finished` event ever emitted.
  - Item 5 (tracking save + actually polls): PASS — `tracking-cache.json` populated 85 → 6,231 bytes within 30 seconds; SQLite `devices=18 / positions=14`; mast `DEVICES ONLINE 18 / SYSTEM STATUS ONLINE`.
  - Item 6 (diagnostics export): PASS — file written, version line matches mast.
- Evidence kept under `tmp/beta-artifacts/smoke-rerun/` (01-initial-launch through 09-tracking-live-with-devices PNGs, `devices.tsv`, etc.) and `tmp/beta-artifacts/smoke/` (initial run pre-fix evidence).
- Beta artifact promotion is deferred: do not upload or push desktop artifacts to the Windows/Linux tester group until `sartracker-web-y6a` defines the cross-platform build/download/instructions process. The current macOS `.app` smoke remains useful internal evidence, not the team distribution path.

Earlier B2 verification (kept for context):

- Red-then-green: `npm run test -- tests/unit/beta-verify-lib.test.ts` failed first against the missing `build/beta-verify-lib.js`, then passed 15/15 after implementing the helpers.
- Passed: `npm run beta:verify -- --steps lint --no-smoke --report-dir tmp/beta-artifacts` end-to-end through the new runner; report written to `tmp/beta-artifacts/verify-0.1.0-sha.5d3ba8ad7603-2026-05-17T06-34-52Z.json`.
- Passed: `npm run lint`, `npm run build`, `npm run test`.

Earlier S3 verification (kept for context):

- Passed: `npm run test -- tests/unit/layer-visibility-service.test.ts tests/unit/layer-catalog-store.test.ts tests/unit/layer-visibility-store.test.ts`
- Passed: `npm run test:e2e -- tests/e2e/layer-panel.spec.ts tests/e2e/parity-visibility.spec.ts --project=chromium`
- Passed after rereview fix: `npm run test -- tests/unit/layer-visibility-service.test.ts tests/unit/layer-visibility-store.test.ts`
- Passed after rereview fix: `npm run test:e2e -- tests/e2e/layer-panel.spec.ts tests/e2e/parity-visibility.spec.ts tests/e2e/helicopter-panel.spec.ts tests/e2e/gpx-import.spec.ts --project=chromium`
- Passed: `npm run lint`
- Passed: `npm run build`
- Passed after rereview fix: `npm run test:all` — 89 unit files / 445 tests, 110 browser + visual E2E tests, 39 backend tests

- Focused A3.9 red checks failed before implementation for the missing weather settings model and Weather menu; the same focused checks passed after implementation.
- Passed: `npm run test -- tests/unit/settings-validation.test.ts tests/unit/browser-settings-store.test.ts`
- Passed: `npm run test:backend -- settings`
- Passed: `npx tsc --noEmit`
- Passed: `npm run test:e2e -- tests/e2e/weather.spec.ts --project=chromium`
- Passed: `npm run lint`
- Passed: `npm run build`
- Passed: `npm run test` — 88 files / 439 tests
- Passed: `npm run test:e2e` — 110 Chromium + visual tests
- Passed: `npm run test:backend` — 39 Rust tests
- Passed: `npm run test:all` — 88 unit files / 439 tests, 110 browser + visual E2E tests, 39 backend tests
- Passed after Linear issue/doc updates: `npm run lint && npm run build`
- Passed: `git diff --check`
- Inbuilt-browser sanity check at `http://127.0.0.1:1420/?missionHarness=1` confirmed the Weather mast control and Settings Weather Links section rendered. Direct manual clicking in the inbuilt browser was unreliable because of element coordinate/runtime issues, so the full configure/open flow is covered by the checked-in Chromium E2E instead.
- Deploy status: completed via the documented Vercel production prebuilt flow; production alias is `https://sartracker-web.vercel.app`.
