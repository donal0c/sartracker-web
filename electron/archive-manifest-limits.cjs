'use strict'

// Creation and independent verification must admit the same bounded inventory.
module.exports = Object.freeze({
  MAX_ARCHIVE_MANIFEST_BYTES: 4 * 1024 * 1024,
  MAX_ARCHIVE_MANIFEST_ENTRIES: 10_000,
})
