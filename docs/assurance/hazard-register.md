# SAR Tracker Whole-Application Hazard Register

**Application assurance snapshot:** `eec92812b783a795c093f37268b295dd2179a3af`

**Register status:** WAR-01 post-PR5 reconciliation plus targeted WAR-04
platform-services and WAR-04B release-control overlays.

**WAR-04B overlay boundary:** `PKG-002` and `REL-001`–`REL-004` were refreshed
from exact source base `3d0d36b3874947d3d620bdb5262d9cd2d7233fcf` and the
2026-08-30 live GitHub/dependency/release snapshot. That targeted refresh does
not advance the application-code evidence boundary, provide an audited-base
artifact, or refresh unrelated hazard rows.

**WAR-04 overlay:** platform-services rows `MAP-001`, `SEC-001`, `SEC-002`,
`SEC-003`, and the `EVD-003` non-overlap judgment were retraced from exact base
`3d0d36b3874947d3d620bdb5262d9cd2d7233fcf`. All other rows retain the WAR-01
snapshot and proof limits above; this overlay is not a whole-register re-audit.

**Current master sync:** WAR-04 PR #8 merged from final head
`3a2278ee8804a9ded0f2fd26626c4c00743c05a6` at
`341d95add5a7eceb6db506a2afd0ea70cb1fc944`. Its evidence/docs and isolated
synthetic tests leave every WAR-04B package, lock, builder, release-workflow,
publisher and support-policy input blob-identical to the WAR-04B source base.

**Merged evidence boundary:** GitHub PR #5 merged at the assurance snapshot
from final head `f5ba8647131950dde457e50ce36fe0b8ded7337d`. Exact-head Linux
run `33260131951` was green. Its final focused correction/package proof is not
the earlier four-review wave; those reviews remain prior-head evidence. The
`DON-248`/`DON-252`/`DON-253` archive successor is actively being implemented
separately but remains unmerged, mutable, and outside this assurance snapshot.

Read this register with the [assurance charter](README.md). `SAR-QA-*` references
resolve through `docs/breadcrumb-team-question-and-answer-ledger.md` to the
verbatim record in `team-feedback/breadcrumb-question-answers-20260822.md`.
Evidence tiers and the five permitted gap classifications are defined in the
charter. “Inspected test” means the test body was opened during WAR-01; it does
not mean this documentation slice reran every historical qualification gate.

## 1. Tracking and current position

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `TRK-001` — current position delayed or hidden | A stale or absent position can misdirect teams and delay help. | `SAR-QA-002`; `docs/breadcrumb-mission-history-architecture-decision.md`; pause warning and visibility guidance in [DON-193](https://linear.app/donal-oc/issue/DON-193) and `public/manual/index.html`; the charter's absolute blocker and one-normal-polling-cycle budget. **Invariant:** before the operational renderer exists, current availability cannot wait without a visible bounded failure state for Electron-main diagnostics/crash I/O, synchronous store open/migration, an unclean-session evidence-loss fence, active-mission/restart bookkeeping, or renderer loading. Renderer request/publication cadence then never waits for history, archive, reconciliation, coverage, replay, evidence bookkeeping, unbounded cache I/O, slow roster transport, service-worker registration, non-tracking core initialization or mission/governance/participant hydration, settings/credential/cache filesystem reads, or deferred-evidence module initialization. Starting or replacing tracking never leaves the application without an active current-position request path; a prompt roster response may join only inside the explicit 50 ms metadata grace, after which publication uses last-known metadata and a warning; participant-scope loading/error or historical selection never hides live or cached current positions; older history never overwrites current state. The ADR requires a separate explicit operator action before live position is hidden. DON-193 and the operator manual deliberately suspend refresh while paused and warn critically, while the manual also names the separate Current Location control as the live-hide action; that authority conflict is unresolved, so pause is not a settled exception. Automatic recovery is not an operator live-hide action and cannot qualify as one. | Pre-window startup in `electron/main.cjs`; durable unclean-session fence in `electron/renderer-teardown-coordinator.cjs`; synchronous store open/migration in `electron/mission-store.cjs`; renderer bootstrap in `src/features/runtime/bootstrap-app-runtime.ts` and `src/lib/register-service-worker.ts`; core startup/replacement in `src/features/runtime/start-app-runtime.ts`, `src/features/runtime/start-core-feature-runtimes.ts`, `src/features/mission/start-mission-runtime.ts`, `src/features/mission/start-mission-governance-runtime.ts`, `src/features/participants/start-participant-runtime.ts`, and `src/features/runtime/runtime-managed-services.ts`; current polling/publication and deferred-evidence/cache initialization in `src/features/tracking/current-position-poll.ts`, `src/features/tracking/polling-manager.ts`, and `src/features/tracking/start-tracking-runtime.ts`; participant scope in `src/features/participants/participant-runtime-bridge.tsx`; pause/recovery state in `src/features/tracking/mission-tracking-status-bridge.ts`; settings/credentials and cache files in `electron/settings-store.cjs` and `electron/runtime-files.cjs` | Pre-window startup/failure and a deliberately held unclean-session fence in `tests/unit/electron-main-startup.test.ts`; fence persistence/settlement in `tests/unit/renderer-teardown-coordinator.test.ts`; resolved diagnostics/crash/store startup and small migration cases in `tests/unit/electron-storage-diagnostics.test.ts`, `tests/unit/electron-crash-log.test.ts`, and `tests/unit/electron-mission-store.test.ts`; packaged newer-schema refusal contract in `tests/unit/release-smoke-safety.test.ts`; renderer startup/replacement in `tests/unit/bootstrap-app-runtime.test.ts`, `tests/unit/register-service-worker.test.ts`, `tests/unit/start-app-runtime.test.ts`, `tests/unit/start-core-feature-runtimes.test.ts`, `tests/unit/start-mission-runtime.test.ts`, `tests/unit/start-mission-governance-runtime.test.ts`, `tests/unit/start-participant-runtime.test.ts`, `tests/unit/electron-settings-store.test.ts`, and `tests/unit/electron-runtime-files.test.ts`; polling, pause/idle, scope, cache, and settlement in `tests/unit/polling-manager.test.ts`, `tests/unit/coverage-current-position-isolation.test.ts`, and `tests/unit/start-tracking-runtime.test.ts`; direct recovery clearing in `tests/unit/mission-tracking-status-bridge.test.ts`; rendered pause warning in `tests/unit/tracking-status-panel.test.ts` and `tests/e2e/visual/visual-tracking.spec.ts` | `T1` directly proves that a held unclean-session evidence fence prevents the BrowserWindow from opening, that the real fence waits for durable evidence-loss settlement, and that a held old-service stop prevents replacement start. Small store/migration contracts and a separate `T4` packaged newer-schema refusal prove fail-closed rejection and preservation, not bounded field-scale startup or current availability; see `PST-001` and `PST-005`. Renderer `T1` contracts and `T2` warning evidence prove history/coverage isolation, the 50 ms roster race, last-known-metadata fallback, resolved startup stages, direct recovery clearing, and the critical paused warning. Source inspection against those limits exposes confirmed gaps. No main-process watchdog protects diagnostics/crash I/O, synchronous database open/migration, the unclean-session fence, active-mission/restart bookkeeping, or a load stalled before renderer bootstrap; the renderer's 30-second watchdog cannot run until its bundle executes, and rejection handling does not detect a held gate. After the renderer begins, service-worker, mission/governance/participant, settings/credentials, deferred-evidence import, and cache must settle before the first client/poller exists. Participant-scope loading/error withholds snapshots, and `onSnapshot` settlement delays the next request. On reload, settings load while the old service remains active; only then are active handles cleared and old current/evidence/history work drained. The directly tested drain can remain held, and only after it settles do deferred-evidence import, cache read, and replacement client/poller start. No inspected test holds each real pre-window/renderer gate while asserting current availability, holds the dynamic import or cache through replacement while asserting a visible tracking gap, or joins recovery clearing through polling. Paused mode makes no provider request and replays last-good only when one exists; automatic recovery clears the snapshot and enters no-request idle. No authority resolves the pause conflict. | Current cadence [DON-267](https://linear.app/donal-oc/issue/DON-267); participant scope [DON-271](https://linear.app/donal-oc/issue/DON-271); boot phases/watchdog [DON-179](https://linear.app/donal-oc/issue/DON-179); mission-store reliability [DON-241](https://linear.app/donal-oc/issue/DON-241); bounded oversized startup [DON-250](https://linear.app/donal-oc/issue/DON-250); settings/credentials [DON-177](https://linear.app/donal-oc/issue/DON-177); Electron cache [DON-32](https://linear.app/donal-oc/issue/DON-32); pause warning [DON-193](https://linear.app/donal-oc/issue/DON-193); final qualification [DON-254](https://linear.app/donal-oc/issue/DON-254) | `unenforced-invariant` | Before the window exists, rejected startup can produce a native error and durable logs, but a slow/held gate has no watchdog or current-position path. Scope loading/error warns and preserves raw cache but withholds current positions. Paused mode warns `Live refresh suspended` and replays last-good only if available; recovery clears the snapshot and warns `Resume the mission before reconnecting live tracking.` Settings read leaves the old poller live, but the subsequent drain removes the request path; the last snapshot/status can appear current while drain or replacement initialization is blocked. Treat every affected point as potentially stale, never infer absence from a blank/cleared/withheld map, and retain the independent operational source. Bound or move pre-window main work without weakening the evidence-loss/store-preservation fence; prohibit O(database-size) startup on main; expose the exact blocking phase and a calm failure state; separate current request/visibility from scope/evidence/hydration/settings/import/cache settlement; keep or explicitly fail the old request path until replacement is live; reconcile pause authority; keep current safety polling/last-good visibility through recovery; and add held pre-window, renderer-startup, dynamic-import/cache, reload-drain/replacement, pause, and recovery regressions before DON-254 qualification. |
| `TRK-002` — non-authoritative time enters exact evidence | Substituted or malformed clocks corrupt chronology, replay, and accountability. | `SAR-QA-021`. **Invariant:** only provider `fixTime` enters exact breadcrumb evidence; another clock may support immediate current display only when labelled unverified and excluded from exact evidence. Replay admits a fix only when `timestamp_source='fix'` and its durable provenance was recorded by the selected time. | `src/features/tracking/traccar-normalization.ts`; `src/features/tracking/canonical-fix-time-evidence.ts`; `src/features/tracking/tracking-timestamp.ts`; `src/features/tracking/traccar-client.ts`; `src/features/tracking/start-tracking-runtime.ts`; `electron/mission-store.cjs`; `electron/mission-replay-query.cjs`; operator warning in `src/components/tracking-status-panel.tsx` | `tests/unit/traccar-normalization.test.ts`; `tests/unit/canonical-fix-time-evidence.test.ts`; `tests/unit/start-tracking-runtime.test.ts`; `tests/unit/traccar-client.test.ts`; `tests/unit/tracking-status-panel.test.ts`; `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/unit/electron-mission-replay-query.test.ts`; `tests/e2e/visual/visual-tracking.spec.ts` | `T4` for the exact `f5ba8647` artifact's synthetic packaged tracking-soak/current-ingest path in run `33260131951`; that package step did not drive Replay. `T2` directly proves labelled current visibility, exact-evidence exclusion, late-provenance invalidation, Replay admission, and explicit limitations for unproved or missing-recorded-time rows, including the checkout-module 960,000-position qualification. No final live-provider corpus, field duration, or every payload variant is proved. | [DON-267](https://linear.app/donal-oc/issue/DON-267), [DON-268](https://linear.app/donal-oc/issue/DON-268), replay consumer [DON-278](https://linear.app/donal-oc/issue/DON-278) | `evidence-tier-gap` | Unseen provider payloads and field clock behavior remain. Rejected-row diagnostics, `FIX TIME UNVERIFIED`, and replay limitation codes preserve visibility without asserting exact evidence. Retain the independent operational source and qualify live-provider/artifact behavior under DON-254; the prior four-review wave is not exact-head review of `f5ba8647`. |
| `TRK-003` — incremental history silently skips fixes | Missing track segments can produce incomplete evidence and later a false coverage claim. | `SAR-QA-001`, `SAR-QA-006`, `SAR-QA-008`, `SAR-QA-013`. **Invariant:** seed failure fails closed; device reads overlap their cursor; only validated maxima advance it; future times are clamped; repeat delivery is idempotent; replay generation changes when newly accepted exact fixes become queryable so an open page chain cannot silently omit them. | `src/features/tracking/polling-manager.ts`; `src/features/tracking/start-tracking-runtime.ts`; `electron/mission-store.cjs`; `electron/mission-replay-query.cjs` | `tests/unit/polling-manager.test.ts`; `tests/unit/start-tracking-runtime.test.ts`; `tests/unit/electron-mission-store.test.ts`; `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/unit/electron-mission-replay-query.test.ts` | `T4` exact-head run `33260131951` proves only the synthetic packaged tracking-soak portion of cursor/dedupe/persistence behavior. `T2` proves the deterministic failure contracts and Replay invalidation for late/equal-now accepted fixes; the indexed Replay qualification imported checkout modules directly. It is not exhaustive model-based concurrency, every provider paging/reordering pattern, or long live-provider proof. | [DON-267](https://linear.app/donal-oc/issue/DON-267), [DON-273](https://linear.app/donal-oc/issue/DON-273), replay consumer [DON-278](https://linear.app/donal-oc/issue/DON-278) | `evidence-tier-gap` | Provider paging/reordering outside fixtures can still expose gaps. History failure stays separate while current polling continues; coverage remains incomplete and stale replay pages require reseek. Run final scale/provider/long-duration qualification under DON-254. |
| `TRK-004` — stationary attention is late or noisy | A missed alert can delay casualty/team attention; jitter-driven alerts distract operators. | `SAR-QA-016`. **Invariant:** roughly 20 minutes within accuracy-aware bounds raises attention; meaningful movement clears it; invalid/outlier fixes cannot suppress or fabricate the state. | `src/features/tracking/stationary-attention.ts`; `src/features/tracking/stationary-attention-projection.ts` | `tests/unit/stationary-attention.test.ts`; `tests/e2e/stationary-attention.spec.ts` | `T2`: unit and rendered-browser behavior. No field receiver, terrain, or accuracy-distribution qualification. | [DON-269](https://linear.app/donal-oc/issue/DON-269) | `evidence-tier-gap` | Real GPS accuracy profiles may differ. Detect via the visible alert/ack state while current position remains available; validate in controlled field exercises before relying on thresholds. |

## 2. Mission lifecycle and scope

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `MIS-001` — lifecycle transition or recovery loses mission continuity | Wrong active/paused/finished state can corrupt timing, admit a second mission, or leave operators unable to recover after restart. | `CLAUDE.md` mission/persistence rules; ADR evidence-fence decisions. **Invariant:** one recoverable mission at a time; startup pauses an unclean active mission; start/pause/resume/finish are transactional and audited; finish crosses the evidence fence; recovery offers explicit Resume or Start Fresh. GPX receipts, legacy provenance reconstruction, attachment custody, and archive/finalization fences must settle or fail closed before the related lifecycle boundary. | `src/features/mission/start-mission-runtime.ts`; `src/features/tracking/ingest-evidence-finalization-boundary.ts`; `transitionMission`, `finishMission`, `prepareClose`, attachment/GPX lifecycle queues, and `finalizeMission` in `electron/mission-store.cjs` | `tests/unit/start-mission-runtime.test.ts`; `tests/unit/ingest-evidence-finalization-boundary.test.ts`; `tests/unit/electron-mission-store.test.ts`; `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/unit/electron-gpx-forced-kill-recovery.test.ts`; `tests/unit/electron-file-system.test.ts`; `tests/e2e/mission.spec.ts` | `T4` exact `f5ba8647` run `33260131951` proves packaged mission start, pause, restart recovery, resume, backup, and continued tracking under its soak profile. `T2` directly proves Finish plus unsettled GPX, attachment-custody, legacy-provenance, and archive/finalization fences. It does not exhaust abrupt power loss, every shutdown interleaving, packaged Finish/finalize/archive, archive-security/restore, or field recovery. | [DON-241](https://linear.app/donal-oc/issue/DON-241), [DON-209](https://linear.app/donal-oc/issue/DON-209), PR5 controls [DON-274](https://linear.app/donal-oc/issue/DON-274), [DON-277](https://linear.app/donal-oc/issue/DON-277), [DON-278](https://linear.app/donal-oc/issue/DON-278) | `evidence-tier-gap` | Abrupt power loss and the unmerged archive successor remain. Its active implementation is mutable and outside this snapshot. Detect with recovery dialog, mission phase, retained GPX failures, audit events, and evidence health; preserve profile and resume rather than inventing state. Reconcile again after `DON-248`/`DON-252`/`DON-253`, then run DON-254 field qualification. |
| `MIS-002` — outing boundaries attribute fixes to the wrong period | Evidence can appear under the wrong operational outing or disappear at a boundary. | `SAR-QA-010`, `SAR-QA-014`. **Invariant:** outings are canonical half-open, non-overlapping intervals; a boundary fix belongs to the later outing; the app never silently invents or force-closes an outing. A recorded search pass remains inside its assignment outing, and static GPX outing assignment is an immutable revision. | `electron/outing-store.cjs`; `electron/mission-store.cjs`; `electron/mission-replay-query.cjs`; `src/features/outings/outing-schedule.ts`; `src/features/outings/start-outing-runtime.ts` | `tests/unit/electron-outing-store.test.ts`; `tests/unit/outing-schedule.test.ts`; `tests/unit/start-outing-runtime.test.ts`; `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/unit/electron-mission-replay-query.test.ts`; `tests/e2e/outings.spec.ts`; `tests/e2e/mission-evidence-search-passes.spec.ts` | `T2`: transaction/domain/browser tests prove half-open outing behavior, immutable GPX outing revisions, Replay filters, and pass-within-outing rejection. Run `33260131951` did not drive a dedicated packaged outing-boundary flow, field oracle, or every archive consumer. | [DON-270](https://linear.app/donal-oc/issue/DON-270), PR5 consumers [DON-274](https://linear.app/donal-oc/issue/DON-274), [DON-278](https://linear.app/donal-oc/issue/DON-278), [DON-279](https://linear.app/donal-oc/issue/DON-279) | `evidence-tier-gap` | Future archive/restore consumers could reinterpret intervals. Overlap validation, explicit Unassigned state, immutable GPX revisions, and rejected out-of-window passes expose uncertainty. Reconcile the archive successor and qualify field boundary cases under DON-254. |
| `MIS-003` — mission participant scope is incomplete or over-inclusive | Selected-team history can be missing, or uninvolved devices can be recorded as mission evidence. | `SAR-QA-015`. **Invariant:** device/group participation is explicit, provenance-bound, and frozen over immutable selection windows; current position may remain operationally visible while exact persistence obeys scope; finish/coverage cannot complete before backfill. | `electron/participant-store.cjs`; participant/group known-at-time reads in `electron/mission-replay-query.cjs`; `src/features/participants/participation-scope.ts`; `src/features/participants/participant-backfill-runtime.ts`; `finishMission` in `electron/mission-store.cjs` | `tests/unit/electron-participant-store.test.ts`; `tests/unit/participant-backfill-runtime.test.ts`; `tests/unit/coverage-participant-scope.test.ts`; `tests/unit/electron-mission-replay-query.test.ts`; `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/e2e/participants.spec.ts` | `T2`: transaction/domain/browser tests plus an independent Replay oracle prove participant/group membership known at the selected time, and participant changes invalidate an open Replay page chain. No archive/restore equality, exact packaged participant-scope flow, or field proof exists. | [DON-271](https://linear.app/donal-oc/issue/DON-271), [DON-276](https://linear.app/donal-oc/issue/DON-276), merged Replay consumer [DON-278](https://linear.app/donal-oc/issue/DON-278) | `evidence-tier-gap` | Late selection and group history remain complex. Checkpoints, blocked-completion UI, and stale-generation rejection expose unfinished or changed scope. Retrace archive/restore equality after `DON-248`/`DON-252`/`DON-253`, then run final packaged/field qualification under DON-254. |

## 3. Persistence, migrations, WAL, backups, and growth

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `PST-001` — partial write or unsafe migration corrupts evidence | Restart can expose a mixed state, corrupt mission records, or make the app unavailable. | ADR persistence invariants; `SAR-QA-020`. **Invariant:** acknowledged writes are atomic under WAL with `synchronous=FULL`; migration is transactional, preserves evidence, and refuses a newer schema without modifying files. | startup/store initialization in `electron/main.cjs`; `createElectronMissionStore`, `migrate`, and schema backfills in `electron/mission-store.cjs` | `tests/unit/electron-mission-store.test.ts`; `tests/unit/release-smoke-safety.test.ts` | `T2` deterministic real-SQLite integration proves the named atomic-write, migration, and refusal contracts. Separately, `docs/releases/sartracker-electron-0.1.0-beta.12.11.md` records a `T4` exact CI-built schema-7 package refusing schema 8 while preserving byte-identical database and backup files. Neither supplies an interruption matrix for every migration/write step or every historical schema version at field scale. | [DON-250](https://linear.app/donal-oc/issue/DON-250), qualification [DON-254](https://linear.app/donal-oc/issue/DON-254) | `untested-invariant` | A legacy migration can still fail or delay startup without a fully characterized recovery path. Detect through startup fault/dialog/log; preserve the original profile and use a compatible newer build. Add fault seams and run the post-programme-PR-6 archive-lifecycle migration matrix. |
| `PST-002` — rolling backup is stale, corrupt, or replaces a good mirror | Recovery copy may be unusable after a live-store failure, creating silent evidence loss pressure. | `CLAUDE.md` backup contract; ADR persistence invariants. **Invariant:** backups serialize, copy live WAL off main, validate at fixed cost, use unique temp plus atomic rename, retain the prior mirror on failure, and surface failure. | `electron/mission-store.cjs`; `electron/sqlite-backup-runner.cjs`; `electron/sqlite-backup-worker.cjs`; `electron/sqlite-snapshot-sanity.cjs`; `src/features/persistence/mission-autosave.ts`; `src/features/persistence/autosave-status-store.ts` | `tests/unit/electron-sqlite-backup-runner.test.ts`; `tests/unit/electron-sqlite-snapshot-sanity.test.ts`; `tests/unit/electron-mission-store.test.ts`; `tests/unit/mission-autosave.test.ts` | `T4`: deterministic control plus historical 3.704-GB Ubuntu package runs. Those runs cover one artifact/I/O profile, not power loss, EIO, ENOSPC, or filesystem rename durability. | [DON-241](https://linear.app/donal-oc/issue/DON-241), [DON-249](https://linear.app/donal-oc/issue/DON-249), [DON-250](https://linear.app/donal-oc/issue/DON-250) | `untested-invariant` | Mirror behavior under full disk and abrupt power failure remains uncertain. Persistent autosave warning and bounded storage diagnostics detect failure; preserve live DB/profile and existing mirror. Add persistence fault injection, then requalify after programme PR-6 archive lifecycle. |
| `PST-003` — integrity checking is absent or re-freezes Electron main | Latent corruption may remain undetected; a full scan on main can make tracking and controls unresponsive. | [DON-249](https://linear.app/donal-oc/issue/DON-249); absolute corrupted-evidence and unbounded-main blockers. **Invariant:** recurring operational work is O(1) on main; full integrity work is cancellable, I/O-arbitrated, durable, and operator-visible. | `electron/sqlite-snapshot-sanity.cjs`; `validateSqliteDatabaseFile` in `electron/mission-store.cjs`. No recurring full-integrity controller exists on this base. | `tests/unit/electron-sqlite-snapshot-sanity.test.ts`; `tests/unit/electron-sqlite-backup-runner.test.ts` | `T4`: `docs/releases/beta12-mission-store-baseline.md` and `docs/releases/beta12-autosave-freeze-fix.md` record that recurring backup no longer scans the full DB on the measured artifact. The inspected tests prove only the fixed-cost sanity/runner contracts, and no evidence proves a background integrity controller because none is implemented. | [DON-249](https://linear.app/donal-oc/issue/DON-249) | `unenforced-invariant` | Corruption between explicit checks can remain latent. Detect via header sanity, backup/autosave warnings, and qualification checks; preserve live and mirror files. Implement only through DON-249 and the post-programme-PR-6 archive-lifecycle resilience slice. |
| `PST-004` — database/event growth makes operations unbounded | Review, migration, backup, and recovery can slow with mission age; unsafe cleanup could delete evidence. | `SAR-QA-020`; ADR no-deletion/no-unbounded-main rules. **Invariant:** repeating writes have change gates and growth budgets; every table has a retention/archive/recovery story; evidence is never silently purged; reads stay bounded/off-main. | `electron/mission-store.cjs`; `electron/mission-review-read-query.cjs`; `electron/mission-review-read-query-runner.cjs`; `electron/outing-fix-summary-query.cjs`; `electron/outing-fix-summary-runner.cjs` | `tests/unit/electron-mission-store.test.ts`; `tests/unit/electron-tracking-soak-lib.test.ts`; `tests/unit/electron-mission-review-read-query-runner.test.ts`; `tests/unit/electron-outing-fix-summary-runner.test.ts` | `T4`: historical five-/fourteen-day packaged growth evidence covers one 32-device compressed profile; `T2` covers the inspected real-worker review/outing reads. Neither proves seasonal retention, every table, or current exact-head indexed latency. | [DON-251](https://linear.app/donal-oc/issue/DON-251) | `unenforced-invariant` | The live-store retention story is incomplete. Detect DB/WAL/backup slopes and query timing; never purge or adopt daily-mission workarounds. Complete DON-251 after programme PR-6 archive lifecycle, then exact-scale DON-254 qualification. |
| `PST-005` — oversized legacy store blocks safe startup/recovery | Operators may face an unavailable app and pressure to rename, delete, or abandon recoverable evidence. | [DON-250](https://linear.app/donal-oc/issue/DON-250); ADR preservation and no-unbounded-main rules. **Invariant:** startup is bounded; expensive assessment is resumable/off-main; originals remain untouched on failure; no automatic discard or in-process multi-GB `VACUUM`. | `electron/main.cjs`; `createElectronMissionStore` and `migrate` in `electron/mission-store.cjs` | `tests/unit/electron-mission-store.test.ts`; `tests/unit/release-smoke-safety.test.ts` | `T1`: small deterministic fixtures. There is no reproduction proving a current v11 startup defect, so this is a confirmed control gap and hypothesis, not a defect claim. | [DON-250](https://linear.app/donal-oc/issue/DON-250) | `unenforced-invariant` | Large, unknown, or corrupt profiles lack state-aware recovery qualification. Preserve the original and work only on a copy. Implement/retest through DON-250 after programme PR-6 archive lifecycle. |

## 4. Replay, coverage, GPX, and archive

The `DON-274`/`DON-277`/`DON-278`/`DON-279` issue set landed from PR5 head
`f5ba8647131950dde457e50ce36fe0b8ded7337d` in merge commit
`eec92812b783a795c093f37268b295dd2179a3af`. The rows below were retraced
against the merged production, migration, IPC/worker, renderer, browser-parity,
and test boundaries. Exact-head Linux run `33260131951` supplies `T4` only for
the package flows it actually drove; its direct checkout-module Replay/GPX
qualification remains `T2`. The earlier four-review wave remains prior-head evidence. The
`DON-248`/`DON-252`/`DON-253` streamed archive/restore successor is still not
implemented, so only `RPL-004` remains `rewrite-pending` in this domain.

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `RPL-001` — false `Complete` or `100%` | Operators can mistake partial, stale, or omitted evidence for the whole mission record. | `SAR-QA-001`, `SAR-QA-008`, `SAR-QA-017`; ADR absolute blocker. **Invariant:** every exact selected revision is fresh, renderer-delivered, ledger-accepted, and confirmed by a fresh DB claim; any omission, filter change, renderer loss, evidence-health issue, or worker failure immediately revokes completion. Replay and Search Operations must disclose paging, filters, static/legacy limitations, and advisory coverage without converting them into a completion claim. | `src/features/tracking/coverage-progress.ts`; `src/features/tracking/coverage-controller.ts`; `src/components/coverage-status-panel.tsx`; `electron/coverage-ledger.cjs`; `electron/coverage-query.cjs`; `electron/coverage-query-runner.cjs`; `electron/coverage-ipc.cjs`; `electron/mission-replay-query.cjs`; `electron/search-operations-page-query.cjs`; `src/components/mission-evidence-replay-tabs.tsx` | `tests/unit/coverage-progress.test.ts`; `tests/unit/coverage-controller.test.ts`; `tests/unit/coverage-status-panel.test.ts`; `tests/unit/electron-coverage-ledger.test.ts`; `tests/unit/coverage-claim-revocation.test.ts`; `tests/unit/electron-coverage-query-runner.test.ts`; `tests/unit/electron-mission-replay-query.test.ts`; `tests/unit/electron-search-operations-page.test.ts`; `tests/e2e/coverage.spec.ts`; `tests/e2e/mission-evidence-search-passes.spec.ts`; `tests/e2e/visual/visual-coverage.spec.ts`; `tests/e2e/visual/visual-mission-evidence-replay.spec.ts` | `T2/T3` directly prove coverage claim revocation, Replay limitation/paging copy, coordinator-only pass authority, and the named browser/visual states. Run `33260131951` built and launched the package but did not drive this end-to-end completion invariant through that artifact; its 960,000-position Replay step was checkout-module `T2`. PR5 did not replace the core coverage claim machinery, and no evidence proves every cross-surface omission, final BCP-17/archive-backed candidate, or field operator interpretation. | [DON-275](https://linear.app/donal-oc/issue/DON-275), [DON-276](https://linear.app/donal-oc/issue/DON-276), final qualification [DON-254](https://linear.app/donal-oc/issue/DON-254) | `evidence-tier-gap` | A future consumer or archive restore can still bypass or misread the claim contract. Keep incomplete/limitation states explicit, treat coverage as advisory for search outcomes, and require fresh database claims rather than equal counts. Reconcile the archive successor and run a complete exact-candidate cross-surface/field matrix under DON-254. |
| `RPL-002` — replay reconstructs present state instead of historical state | A misleading chronology can change how operators interpret decisions and movement. | `SAR-QA-007`, `SAR-QA-017`. **Invariant:** replay is versioned and time-bounded, shows only evidence both effective and durably known at the selected time, loads progressively from one pinned/generation-bound snapshot, discloses legacy/static/summary limits, and never substitutes present-day state or changes the live current-position surface. | `electron/mission-evidence-version-store.cjs`; `electron/mission-event-provenance-backfill.cjs`; `electron/mission-replay-database.cjs`; `electron/mission-replay-query.cjs`; `electron/mission-replay-message-policy.cjs`; `electron/mission-replay-runner.cjs`; `electron/mission-replay-worker.cjs`; `electron/mission-replay-query-ipc.cjs`; `electron/mission-store.cjs`; `electron/preload.cjs`; `src/features/mission-review/dublin-local-time.ts`; `src/features/mission-review/start-mission-review-runtime.ts`; `src/components/mission-evidence-replay-tabs.tsx` | `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/unit/electron-mission-replay-database.test.ts`; `tests/unit/electron-mission-replay-message-policy.test.ts`; `tests/unit/electron-mission-replay-query.test.ts`; `tests/unit/electron-mission-replay-runner.test.ts`; `tests/unit/electron-mission-replay-query-ipc.test.ts`; `tests/unit/electron-legacy-evidence-backfill-runner.test.ts`; `tests/unit/mission-replay-dst-overlap.test.ts`; `tests/unit/mission-replay-object-page-limitation.test.ts`; `tests/unit/start-mission-review-runtime.test.ts`; `tests/e2e/mission-review.spec.ts`; `tests/e2e/visual/visual-mission-evidence-replay.spec.ts` | `T2` proves independent-oracle sampled reconstruction, late-known fix/object behavior, pinned WAL snapshots, generation invalidation, bounded messages/workers/IPC, Dublin overlap choice, progressive object/track/filter pages, and explicit static/legacy limitations. Exact-head run `33260131951` extended that integration tier to an indexed 960,000-position checkout-module workload; it did not drive Replay through the packaged artifact. It does not prove the 2,000,000-position headroom target on this exact head, archive-backed restore-and-replay, every legacy profile, live-provider chronology, or field interpretation. Earlier four-review verdicts are prior-head evidence. | [DON-277](https://linear.app/donal-oc/issue/DON-277), [DON-278](https://linear.app/donal-oc/issue/DON-278), final qualification [DON-254](https://linear.app/donal-oc/issue/DON-254) | `evidence-tier-gap` | Legacy baselines can truthfully expose unknown earlier history, large object state is summarized with hashes, undated GPX stays static, and stale page chains require reseek. Preserve the live operational source and do not treat Replay as archive restore proof. Reconcile `DON-248`/`DON-252`/`DON-253`, then qualify 2m/live-provider/field behavior under DON-254. |
| `RPL-003` — GPX time/provenance is invented or lost | Fabricated chronology can contaminate mission evidence; lost provenance prevents honest replay. | `SAR-QA-019`. **Invariant:** retain exact bounded source bytes and SHA-256, point order/time/elevation, explicit point/segment rejection provenance, receipt and revision lineage; distinguish fully dated, partially dated, and undated evidence; never interpolate missing time; and never publish a staged or failed import as complete evidence. | `shared/gpx-source-scalars.mjs`; `electron/gpx-import-envelope.cjs`; `electron/gpx-source-reader.cjs`; `electron/gpx-evidence-import-runner.cjs`; `electron/gpx-evidence-import-worker.cjs`; `electron/gpx-renderer-boundary.cjs`; GPX schema/migration/receipt/revision/publication paths in `electron/mission-store.cjs`; `electron/file-system.cjs`; `src/features/gpx/gpx-parser.ts`; `src/features/gpx/read-gpx-import-pages.ts`; `src/features/gpx/start-gpx-runtime.ts`; `src/infrastructure/gpx-import-source/electron-gpx-import-source.ts` | `tests/unit/gpx-parser.test.ts`; `tests/unit/electron-gpx-source-reader.test.ts`; `tests/unit/electron-gpx-evidence-import-runner.test.ts`; `tests/unit/electron-gpx-forced-kill-recovery.test.ts`; `tests/unit/electron-gpx-renderer-boundary.test.ts`; `tests/unit/electron-gpx-file-system.test.ts`; `tests/unit/electron-gpx-import-source.test.ts`; `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/unit/gpx-import-pagination.test.ts`; `tests/unit/read-gpx-import-pages.test.ts`; `tests/e2e/gpx-import.spec.ts`; `tests/e2e/visual/visual-mission-evidence-replay.spec.ts` | `T2` directly proves exact-byte hashing, strict timestamp/scalar parsing, timed/undated classification, explicit rejections/failures, immutable revisions/aliases, SIGKILL receipt recovery, 8 MiB/source and 100-path bounds, worker isolation, renderer pagination, and 50,000-point/current-write plus exact-limit-source gates. Run `33260131951` executed those scale checks against checkout modules, not the packaged artifact. The fixed 8 MiB engineering limit rejects rather than qualifies larger files; no field-source diversity, every filesystem failure, long-duration watch, or archive restore is proved. | [DON-274](https://linear.app/donal-oc/issue/DON-274), final qualification [DON-254](https://linear.app/donal-oc/issue/DON-254) | `evidence-tier-gap` | Files over 8 MiB require an explicit split-and-import fallback; malformed/missing/kill-interrupted sources remain retained failures rather than evidence. Preserve original files and inspect persisted issue/revision pages. Reconcile archive inclusion/restore in the successor and run representative field-source/provider qualification under DON-254. |
| `RPL-004` — archive is incomplete, corrupt, mutable, inaccessible, disclosed, or unbounded | Mission evidence may be lost, tampered with, undecryptable, exposed to an unauthorized person, or rendered unavailable while large finalization blocks Electron main. | `SAR-QA-020`; ADR finalization/archive decisions and corrupted-evidence/unbounded-main blockers. **Invariant:** retain finalized evidence indefinitely with no deletion path; build a mission-scoped, encrypted/locked, streamed archive with a cryptographic hash manifest; decide and test key custody, authorized unlock, key-loss recovery, and emergency access; include every active, superseded, retired, failed, static, and versioned evidence dependency; validate atomically; make finalization idempotent; prove restore-and-replay before any live-store cleanup; never buffer the whole archive/DB. | `createMissionArchive`, archive attachment enumeration, finalization epochs/fences, provenance readiness, and `finalizeMission` in `electron/mission-store.cjs`; `electron/zip-archive.cjs`; PR5 GPX revisions/receipts and `mission_object_versions`. The merged implementation includes versioned attachment paths and fail-closed provenance/finalization controls, but still reads the database/archive inputs into buffers and provides neither encryption/key custody nor a cryptographic authenticity manifest. | `tests/unit/electron-mission-store.test.ts`; `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/unit/electron-file-system.test.ts`; `tests/unit/electron-zip-archive.test.ts`; `tests/e2e/mission.spec.ts` | `T2` proves real SQLite/filesystem small archives, CRC/atomicity/restart/idempotence, finalization epochs, provenance fences, and inclusion of superseded/retired marker attachments. Exact-head run `33260131951` did not drive archive creation/restore through its packaged artifact. No evidence proves >2 GiB, streaming, encryption/authenticity, key custody/recovery/emergency access, full GPX/failed-evidence restore equality, indefinite archive-backed retention, or restore-and-replay; DON-252 records the known large-file failure. | [DON-248](https://linear.app/donal-oc/issue/DON-248), [DON-252](https://linear.app/donal-oc/issue/DON-252), [DON-253](https://linear.app/donal-oc/issue/DON-253) | `rewrite-pending` | Current validation detects some corruption and PR5 retains more complete custody, but CRC is not tamper evidence and recoverable finalization is not secure or large-archive-safe. Keep this row pending through the unmerged successor; its active implementation is mutable and not evidence. Require its decision, restore/equality, security, failure, and scale matrix. |
| `RPL-005` — search geometry declares an outcome or repeated pass is overwritten | Operators can mistake a drawn area or advisory coverage estimate for a completed search, while partial/reassigned/repeated work is lost. | `SAR-QA-018`; ADR search-area/pass decisions. **Invariant:** stable area identity/version, assignment, append-oriented pass, participant/evidence links, and coordinator-declared outcome remain separate; repeated/overlapping/partial/aborted passes stay visible; pass times remain inside the assignment outing; geometry/coverage is advisory and never declares completion; revise/retire replaces deletion. | Search-area/assignment/pass schema, validation, versioning, and links in `electron/mission-store.cjs`; `electron/search-operations-page-query.cjs`; `electron/search-operations-page-runner.cjs`; `electron/search-operations-page-worker.cjs`; `electron/preload.cjs`; `src/features/drawings/drawing-persistence/search-area-drawing-persistence.ts`; `src/features/mission-review/start-mission-review-runtime.ts`; `src/components/mission-evidence-replay-tabs.tsx`; `src/features/browser-validation/browser-harness-store.ts` | `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/unit/electron-search-operations-page.test.ts`; `tests/unit/search-operations-read-only.test.ts`; `tests/unit/start-mission-review-runtime.test.ts`; `tests/unit/drawing-dialog.test.ts`; `tests/e2e/drawing-tools.spec.ts`; `tests/e2e/mission-evidence-search-passes.spec.ts`; `tests/e2e/visual/visual-mission-evidence-replay.spec.ts` | `T3`: real SQLite/version tests plus browser and independently inspected critical visual evidence prove stable/revisioned areas, repeated assignments/passes, coordinator-declared outcomes, link validation, outing fences, bounded off-main pages, retained search scope, advisory-only copy, and finalized read-only visibility. Run `33260131951` did not drive a packaged operator search-pass flow; Dublin fall-back-hour pass entry, field exercise, and archive-backed restore remain unproved. | [DON-279](https://linear.app/donal-oc/issue/DON-279), final qualification [DON-254](https://linear.app/donal-oc/issue/DON-254) | `evidence-tier-gap` | Operator entry remains the outcome authority, so incorrect coordinator input is detectable through retained revisions/audit and corrected with a new revision, not inferred from coverage. Retain advisory wording and read-only review; add packaged/DST/field and archive-restore qualification before final acceptance. |

## 5. Electron IPC, workers, and process lifecycle

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `IPC-001` — untrusted renderer invokes desktop capability | Untrusted content could alter evidence, read local resources, or cancel another renderer's work. | `CLAUDE.md` fail-closed/security rules. **Invariant:** every enumerated handler validates the exact packaged/dev sender; preload exposes only fixed and bounded channels; context isolation/sandboxing stay enabled; navigation/new windows are denied; request/cancellation identity is renderer-scoped; raw GPX bytes never cross to the renderer. | `electron/main.cjs`; `electron/preload.cjs`; `electron/coverage-ipc.cjs`; `electron/breadcrumb-query-ipc.cjs`; `electron/mission-review-read-query-ipc.cjs`; `electron/mission-replay-query-ipc.cjs`; `electron/outing-fix-summary-ipc.cjs`; `electron/gpx-renderer-boundary.cjs`; `electron/file-system.cjs` | `tests/unit/electron-main-startup.test.ts`; `tests/unit/electron-coverage-ipc.test.ts`; `tests/unit/electron-breadcrumb-query-ipc.test.ts`; `tests/unit/electron-breadcrumb-dot-ipc.test.ts`; `tests/unit/electron-mission-review-read-query-ipc.test.ts`; `tests/unit/electron-mission-replay-query-ipc.test.ts`; `tests/unit/electron-outing-fix-summary-ipc.test.ts`; `tests/unit/electron-gpx-renderer-boundary.test.ts`; `tests/unit/electron-gpx-import-source.test.ts`; `tests/unit/electron-linux-renderer-workflows.test.ts` | `T1/T2` prove sender validation, sandbox-safe preload, renderer-scoped Replay cancellation, bounded UTF-8/request projection, fixed GPX projection pages, and denial of raw GPX-byte reads. Exact-head run `33260131951` built and launched the package but did not drive the complete handler/security invariant through that artifact. This is not a formal exhaustive channel-security proof, and the archive successor will add/change attack surface. | [DON-236](https://linear.app/donal-oc/issue/DON-236), qualification [DON-254](https://linear.app/donal-oc/issue/DON-254) | `evidence-tier-gap` | A future channel can escape the inventory. Validation failures reject the call; renderer destruction cancels only owned work; raw GPX bytes remain in the worker/store boundary. Repeat the full handler/preload inventory after the archive successor and in final DON-254 qualification. |
| `IPC-002` — close/reload/crash/duplicate instance outruns evidence drain | Duplicate writers or unclean teardown can lose evidence or create corrupt recovery state. | ADR evidence-fence decisions; absolute silent-loss blocker. **Invariant:** single instance; close/reload/quit awaits the exact renderer drain and unexpected-loss fence; fatal/unclean state becomes durable before relaunch/window/clean marker; failure keeps the process open or records critical health. | singleton, window guards, crash handlers, and quit path in `electron/main.cjs`; `electron/renderer-teardown-coordinator.cjs`; `src/features/runtime/install-app-runtime-teardown.ts` | `tests/unit/electron-main-startup.test.ts`; `tests/unit/renderer-teardown-coordinator.test.ts`; `tests/unit/renderer-teardown-wiring.test.ts`; `tests/unit/install-app-runtime-teardown.test.ts` | `T3`: deterministic lifecycle/failure paths plus historical packaged duplicate/restart smoke. It is not an exhaustive OS-kill/window-manager matrix, and DON-247 remains unresolved. | [DON-180](https://linear.app/donal-oc/issue/DON-180), [DON-209](https://linear.app/donal-oc/issue/DON-209), [DON-247](https://linear.app/donal-oc/issue/DON-247) | `evidence-tier-gap` | Abrupt OS termination and package-specific hangs remain. Durable crash/loss markers and retained process are the safe fallback. Recheck after the evidence/replay and archive successors and in the DON-247/DON-254 package matrices. |
| `IPC-003` — mission-size work blocks Electron main | Current tracking and controls can freeze exactly when history/archive size is largest. | ADR absolute blocker; `CLAUDE.md` near-perfect standard. **Invariant:** size-dependent DB/review/coverage/replay/GPX/search work runs in cancellable timeout-bounded workers, returns bounded/attested shapes, yields writer priority where required, and terminates/joins poisoned workers. | Breadcrumb Line `electron/breadcrumb-query-runner.cjs`, `electron/breadcrumb-query-worker.cjs`, `electron/breadcrumb-query-ipc.cjs`; Breadcrumb Dots `electron/breadcrumb-dot-query-runner.cjs`, `electron/breadcrumb-dot-query-worker.cjs`; coverage `electron/coverage-query-runner.cjs`, `electron/coverage-query-worker.cjs`, `electron/coverage-tile-runner.cjs`, `electron/coverage-tile-worker.cjs`, `electron/coverage-ipc.cjs`; outing summaries `electron/outing-fix-summary-runner.cjs`, `electron/outing-fix-summary-worker.cjs`, `electron/outing-fix-summary-ipc.cjs`; Mission Review `electron/mission-review-read-query-runner.cjs`, `electron/mission-review-read-query-worker.cjs`, `electron/mission-review-read-query-ipc.cjs`; PR5 `electron/gpx-evidence-import-runner.cjs`, `electron/gpx-evidence-import-worker.cjs`, `electron/legacy-evidence-backfill-runner.cjs`, `electron/legacy-evidence-backfill-worker.cjs`, `electron/mission-replay-runner.cjs`, `electron/mission-replay-worker.cjs`, `electron/mission-replay-query-ipc.cjs`, `electron/search-operations-page-runner.cjs`, `electron/search-operations-page-worker.cjs`, and lifecycle/serialization in `electron/mission-store.cjs` | `tests/unit/electron-breadcrumb-query-runner.test.ts`; `tests/unit/electron-breadcrumb-dot-runner.test.ts`; `tests/unit/electron-breadcrumb-query-ipc.test.ts`; `tests/unit/electron-breadcrumb-dot-ipc.test.ts`; `tests/unit/electron-coverage-query-runner.test.ts`; `tests/unit/electron-coverage-tile-runner.test.ts`; `tests/unit/electron-coverage-ipc.test.ts`; `tests/unit/electron-outing-fix-summary-runner.test.ts`; `tests/unit/electron-outing-fix-summary-ipc.test.ts`; `tests/unit/electron-mission-review-read-query-runner.test.ts`; `tests/unit/electron-mission-review-read-query-ipc.test.ts`; `tests/unit/electron-gpx-evidence-import-runner.test.ts`; `tests/unit/electron-gpx-forced-kill-recovery.test.ts`; `tests/unit/electron-legacy-evidence-backfill-runner.test.ts`; `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/unit/electron-mission-replay-message-policy.test.ts`; `tests/unit/electron-mission-replay-runner.test.ts`; `tests/unit/electron-mission-replay-query-ipc.test.ts`; `tests/unit/electron-search-operations-page.test.ts` | `T4` exact `f5ba8647` run `33260131951` proves package launch and the 6/6 8,664-position tracking soak stayed responsive; it did not run the indexed Replay/GPX work through the artifact. `T2` proves the named workers' construction, bounded results, timeout/cancellation/physical-exit fences, renderer-scoped ownership, background legacy turns, a 50,000-point import and exact-limit 8 MiB source while current writes stay below the 200 ms hard gate, a 50,000-pass off-main page, and the 960,000-position Replay checkout workload. This is not an exhaustive main-isolate inventory, the 2m headroom target is not exact-head proof, and buffered archive creation remains the confirmed successor boundary in `RPL-004`. | [DON-251](https://linear.app/donal-oc/issue/DON-251), [DON-252](https://linear.app/donal-oc/issue/DON-252), [DON-254](https://linear.app/donal-oc/issue/DON-254) | `evidence-tier-gap` | New or restored archive/evidence paths can reintroduce main work. Timeout/abort/worker replacement, bounded pages, GPX admission limits, and shutdown joins protect inspected paths. Keep archive buffering rewrite-pending, repeat the inventory after its merge, and enforce the final 2m/exact-scale responsiveness gate. |

## 6. Coordinates and geodesy

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `GEO-001` — coordinate transform/grid reference is wrong | Wrong tasking or display coordinates can send teams to the wrong location. | `CLAUDE.md` Coordinate Safety. **Invariant:** WGS84↔ITM/TM65 uses EPSG:2157/EPSG:1641 with the corrected negative-Y datum shift; TM65 remains display-only; non-finite/out-of-range values are rejected; production goldens match. | `src/lib/coordinates.ts`; `src/features/coordinates/coordinate-tool.ts` | `tests/unit/coordinates.test.ts`; `tests/unit/coordinate-tool.test.ts`; `tests/e2e/coordinate-converter.spec.ts` | `T2` directly proves deterministic production goldens, range guards, parser contracts, and the browser workflow. Separately, `docs/releases/sartracker-electron-0.1.0-beta.12.md` records a narrow `T4` exact CI-artifact smoke for coarse Irish Grid normalization and rejection. Neither is a full-envelope/property/fuzz or field oracle. | [DON-238](https://linear.app/donal-oc/issue/DON-238), final parity [DON-6](https://linear.app/donal-oc/issue/DON-6) | `evidence-tier-gap` | Edge-case parser/transform output remains possible despite the directly tested controls. Visible validation errors and independent map/grid cross-check are fallback. Expand the coordinate attack matrix before DON-6 acceptance. |
| `GEO-002` — bearing/distance/measurement math propagates unsafe input | Wrong search areas, sectors, bearings, or measurements can distort tasking. | `CLAUDE.md` declination and coordinate-validation rules. **Invariant:** true↔magnetic conversion uses −4.5°; destination/bearing/circle/sector/measurement math rejects non-finite and out-of-range coordinates, bearings, radii, distances, and segment counts while remaining geodesically correct. | `src/features/drawings/drawing-math.ts`; `src/features/measurements/start-measurement-runtime.ts` | `tests/unit/drawing-math.test.ts`; `tests/unit/start-measurement-runtime.test.ts`; `tests/e2e/measurement.spec.ts` | `T2`: selected vector and browser-harness behavior; tests cover negative radius/distance and degenerate bearing. The production math still accepts or propagates many NaN, Infinity, coordinate-range, bearing, and segment-count inputs, and there is no packaged/field oracle. | [DON-6](https://linear.app/donal-oc/issue/DON-6) | `unenforced-invariant` | Unsafe numeric tuples can enter through direct map/runtime calls. Current UI constraints and independent field/map measurement are fallback, not a production guard. Add finite/range validation plus adversarial tests, then reconcile changed DON-279 drawing consumers. |

## 7. Offline maps

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `MAP-001` — false offline “Field ready” | An operator can enter a no-connectivity area believing the required map is usable. | `docs/offline-map-resilience.md`; `docs/official-map-offline-strategy.md`. **Invariant:** readiness means a locally readable, correctly identified/provenanced package with checked mission-area coverage; metadata/bounds alone cannot establish full tile usability. | map-package validation in `electron/settings-store.cjs`; atomic import in `electron/file-system.cjs`; `src/features/map/offline-map-readiness.ts`; `src/features/map/offline-map-coverage.ts`; `src/features/map/field-readiness-checklist.ts`; `electron/official-map-proxy.cjs` | `tests/unit/electron-settings-store.test.ts`; `tests/unit/electron-file-system.test.ts`; `tests/unit/offline-map-readiness.test.ts`; `tests/unit/offline-map-coverage.test.ts`; `tests/unit/field-readiness-checklist.test.ts`; `tests/unit/electron-official-map-proxy.test.ts`; isolated red evidence in `scripts/assurance/war-04/maps/official-map-readiness-red.test.ts` | `T2`: WAR-04 joins the production registry, readiness checklist, proxy, importer, and real SQLite readers with four deterministic synthetic probes; the invalid-tile probe adds one local Playwright-managed headless-Chromium decode oracle on a blank data-URL page. It confirms stale positive readiness after removal, accepted browser-undecodable tile bytes, positive readiness over an absent in-bounds target tile, and stale live content after same-path replacement. No Electron, packaged, Windows, licensed-map, application-page, operator-workflow, or exhaustive national-coverage proof was run. | [DON-7](https://linear.app/donal-oc/issue/DON-7), [DON-76](https://linear.app/donal-oc/issue/DON-76) | `unenforced-invariant` | Confirmed findings `WAR04-MAP-01..03` and escape analysis are in [WAR-04 findings](findings/WAR-04.md). The no-coverage hatch and loud proxy error remain useful use-time fallback, but they do not make the earlier readiness claim true. Repair as one WAR-11 map qualification/freshness cluster under DON-7/DON-76, then defer cross-platform exact-artifact qualification to WAR-12. |
| `MAP-002` — supported platform lacks a safe licensed-map path | A deployed machine may have no usable official map or may expose private credentials/packages during distribution. | Offline-map strategy and support policy. **Invariant:** a licensed package reaches every advertised installed platform through an admin-safe private workflow, contains no credential leakage, and survives offline restart. | `electron/settings-store.cjs`; `electron/file-system.cjs`; `electron/official-map-proxy.cjs`; `src/features/map/offline-map-readiness.ts`; `src/features/map/offline-map-coverage.ts`; `src/features/map/field-readiness-checklist.ts`; `electron-builder.json`; `.github/workflows/electron-release.yml` | `tests/unit/electron-settings-store.test.ts`; `tests/unit/electron-file-system.test.ts`; `tests/unit/offline-map-readiness.test.ts`; `tests/unit/offline-map-coverage.test.ts`; `tests/unit/field-readiness-checklist.test.ts`; `tests/unit/electron-official-map-proxy.test.ts`; `tests/unit/electron-release-safety.test.ts` | `docs/official-map-national-package-measurement-don-108.md` records `T3` local packaged macOS official-map smoke. `docs/releases/sartracker-electron-0.1.0-beta.12.md` records `T4` exact CI-built AppImage Ubuntu official-map proof. Those older, platform-specific runs do not prove the current artifact, installed `.deb`, Windows, or the repeatable raw-source/private distribution path. No new package run was made for WAR-01. | [DON-115](https://linear.app/donal-oc/issue/DON-115), [DON-144](https://linear.app/donal-oc/issue/DON-144), [DON-76](https://linear.app/donal-oc/issue/DON-76) | `evidence-tier-gap` | Readiness checklist and public/online fallback expose degradation, but are not licensed offline equivalence. Complete DON-115/DON-144 and exact-artifact platform proof before widening support claims. |

## 8. Evidence and map surfaces

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `EVD-001` — persisted evidence identity/coordinates diverge from map | A marker, clue, casualty, helicopter, GPX track, or search area may appear in the wrong place, disappear, or be mistaken for another record. | ADR data-state/display-state separation; `SAR-QA-017`. **Invariant:** accepted persisted WGS84 coordinates, stable IDs, types, exact source revisions, and retained clinical/evidence fields are exactly those shaped, rendered, selected, filtered, and reviewed in the live store; display compaction/presentation never mutates evidence. Archive restore equality is separately owned by `RPL-004`. | Marker boundaries `src/features/markers/start-marker-runtime.ts`, `src/features/markers/marker-draft.ts`, `src/features/markers/marker-geojson.ts`; helicopter boundaries `src/features/helicopters/start-helicopter-runtime.ts`, `src/features/helicopters/helicopter-geojson.ts`; GPX boundaries `src/features/gpx/gpx-parser.ts`, `src/features/gpx/read-gpx-import-pages.ts`, `src/features/gpx/start-gpx-runtime.ts`, `electron/gpx-renderer-boundary.cjs`; version/store/review boundaries `electron/mission-evidence-version-store.cjs`, `electron/mission-store.cjs`, `src/components/mission-evidence-replay-tabs.tsx` | `tests/unit/start-marker-runtime.test.ts`; `tests/unit/marker-draft.test.ts`; `tests/unit/marker-geojson.test.ts`; `tests/unit/start-helicopter-runtime.test.ts`; `tests/unit/gpx-parser.test.ts`; `tests/unit/electron-gpx-renderer-boundary.test.ts`; `tests/unit/electron-mission-evidence-versioning.test.ts`; `tests/unit/read-gpx-import-pages.test.ts`; `tests/e2e/marker.spec.ts`; `tests/e2e/gpx-import.spec.ts`; `tests/e2e/helicopter-panel.spec.ts`; `tests/e2e/mission-review.spec.ts`; `tests/e2e/visual/visual-markers.spec.ts`; `tests/e2e/visual/visual-mission-evidence-replay.spec.ts` | `T2/T3` prove source-point evidence survives GPX display compaction, raw GPX bytes stay out of renderer pages, versioned marker/search/GPX state is retained, casualty treatment beyond 2,000 bytes survives unrelated edits, marker properties and helicopter persistence/feature presence are retained, and the named browser/visual surfaces render evidence. The opened marker GeoJSON test does not assert coordinates, and helicopter tests do not assert persisted-to-rendered coordinate equality; there is no single whole live-store→map→review equality oracle, exact packaged field flow, or archive-backed proof. | [DON-6](https://linear.app/donal-oc/issue/DON-6), merged evidence owners [DON-274](https://linear.app/donal-oc/issue/DON-274), [DON-277](https://linear.app/donal-oc/issue/DON-277) | `untested-invariant` | The inspected production mappers directly use persisted `[lon, lat]`, but the complete cross-surface coordinate/identity equality invariant lacks a direct test. Compare list/details/revision pages with map, retain visible save/import errors, and never treat display simplification as source mutation. Add the direct live-store equality oracle, then retrace archive restore under `RPL-004` and run field artifact proof before final parity acceptance. |
| `EVD-002` — visibility/filter/group state hides evidence or changes records | Operators may miss relevant evidence while believing a layer is visible, or a display action may alter mission truth. | ADR data-state/display-state separation. **Invariant:** parent/group/child/map/replay/search filters compose deterministically, remain inspectable and scope-bound across paging/remounts, disclose omitted/static evidence, and never mutate persisted evidence. | `src/features/layers/effective-overlay-visibility.ts`; `src/features/layers/layer-visibility-service.ts`; `src/features/layers/map-layer-filters.ts`; `src/features/map/use-map-overlays.ts`; `electron/mission-replay-query.cjs`; `electron/search-operations-page-query.cjs`; `src/features/mission-review/start-mission-review-runtime.ts`; `src/components/mission-evidence-replay-tabs.tsx` | `tests/unit/effective-overlay-visibility.test.ts`; `tests/unit/layer-visibility-service.test.ts`; `tests/unit/map-layer-filters.test.ts`; `tests/unit/layer-stale-refresh-integration.test.ts`; `tests/unit/electron-mission-replay-query.test.ts`; `tests/unit/mission-replay-object-page-limitation.test.ts`; `tests/unit/electron-search-operations-page.test.ts`; `tests/unit/search-operations-read-only.test.ts`; `tests/e2e/layer-panel.spec.ts`; `tests/e2e/parity-visibility.spec.ts`; `tests/e2e/mission-evidence-search-passes.spec.ts`; `tests/e2e/visual/visual-mission-evidence-replay.spec.ts` | `T3`: unit/integration/browser and critical visual evidence prove layer composition plus mission/time/generation-bound Replay filters, searchable bounded Search Operations pages, retained accepted filter scope, and explicit static/limitation copy. Run `33260131951` did not drive a complete packaged style rebuild/filter matrix or field workflow. | [DON-6](https://linear.app/donal-oc/issue/DON-6), Replay/search consumers [DON-278](https://linear.app/donal-oc/issue/DON-278), [DON-279](https://linear.app/donal-oc/issue/DON-279) | `evidence-tier-gap` | Layer/list/revision comparison and reset/reseek/re-enable are fallback; stale page generations fail closed. Run a complete map/layer/replay/search/archive visibility matrix on the final candidate and retain DON-264's separate persistent-sync warning gap. |
| `EVD-003` — persistent overlay synchronization failure is console-only | Stored evidence can remain stale/absent on the map without an operator-visible explanation. | [DON-264](https://linear.app/donal-oc/issue/DON-264). **Invariant:** bounded retries continue, persistent failure becomes a durable sanitized map-health/operator warning, and recovery clears it only after successful synchronization. | `src/features/map/map-style-sync.ts`; `src/features/map/use-map-overlays.ts`; `src/features/map/use-map-drawing-overlays.ts`; `src/features/map/use-map-gpx-overlays.ts`; `src/features/map/use-map-measurement-overlays.ts` | `tests/unit/map-style-sync.test.ts`; `tests/unit/map-overlay-primitives.test.ts` | `T1`: retry/backoff/coalescing/abort control. WAR-04 retraced this path against its base-map package, settings, and diagnostics seams and found no shared failing call path or state owner. Persistent overlay failure still remains `console.error`; there is no operator warning control to test. | [DON-264](https://linear.app/donal-oc/issue/DON-264) | `unenforced-invariant` | WAR-04 explicitly left DON-264 separately owned; see [WAR-04 findings §6](findings/WAR-04.md#6-don-264-assessment). Current detection is developer console or manual list-vs-map comparison. Keep source records as truth and do not infer deletion. Implement warning elevation/clearance under DON-264, then verify all merged evidence surfaces. |
| `EVD-004` — accepted mission evidence persistence fails without a durable completeness block | A current fix may remain visible while its accepted mission evidence is lost; after restart an operator could otherwise infer a complete record or archive an undetected gap. | `SAR-QA-001`; ADR silent-loss and false-Complete blockers. **Invariant:** live current position remains visible, but any accepted mission-write failure is durably marked against the exact mission before its observation/Finish fence settles; sticky critical health survives restart; `Complete`/`100%` remains permanently blocked; finalization/archive requires explicit mission-scoped authorized and audited acknowledgement of known permanent loss, which never clears health or restores completeness. The exact base's configured-roster membership is workflow attribution, not authenticated actor authority; see `SEC-004`. | `src/features/tracking/start-tracking-runtime.ts`; `src/features/tracking/rejection-evidence-delivery.ts`; `src/features/tracking/ingest-evidence-finalization-boundary.ts`; `electron/mission-store.cjs`; `electron/ingest-anomaly-outbox.cjs`; `src/components/tracking-status-panel.tsx`; `src/components/coverage-status-panel.tsx` | `tests/unit/start-tracking-runtime.test.ts`; `tests/unit/rejection-evidence-delivery.test.ts`; `tests/unit/ingest-anomaly-outbox.test.ts`; `tests/unit/ingest-evidence-finalization-boundary.test.ts`; `tests/unit/electron-mission-store.test.ts`; `tests/unit/tracking-status-panel.test.ts`; `tests/unit/coverage-status-panel.test.ts`; `tests/e2e/mission.spec.ts` | `T2`: runtime, real SQLite/filesystem restart/finalization integration, and rendered browser-harness warning/acknowledgement behavior. The tests prove configured-roster allow/deny and durable audit behavior, not actor identity or mission-scoped authorization. They do not prove packaged ENOSPC/EIO, crash/power-loss timing, every filesystem, or field recovery. | [DON-268](https://linear.app/donal-oc/issue/DON-268), [DON-276](https://linear.app/donal-oc/issue/DON-276); authorization boundary [DON-199](https://linear.app/donal-oc/issue/DON-199), [DON-220](https://linear.app/donal-oc/issue/DON-220) | `evidence-tier-gap` | If even the durable marker cannot be written, the in-process critical warning and unsettled lifecycle fence are the last control; restart behavior under that exact storage failure remains unqualified. Until `SEC-004` is remediated, configured-roster acknowledgement is accepted only inside the trusted-team-machine boundary and needs an independent operational authorization check. Preserve the profile and source record, never claim completeness, and run the authorization plus package/fault matrix before DON-254. |
| `EVD-005` — marker attachment identity is overwritten or detached from evidence | Two records can retain the same stored path while later bytes replace earlier evidence, destroying custody and corrupting review/archive truth. | ADR immutable version/provenance and no-silent-overwrite rules; `DON-277`. **Invariant:** every attachment receives a unique immutable path before marker/version association; ingestion is ordered with lifecycle and shutdown fences; same-name files cannot collide; superseded/retired revisions retain their referenced bytes; every required attachment is collision-safely included in the current archive. Cryptographic verification after restore is separately owned by `RPL-004`. | `ingestMarkerAttachment` and UUID-prefixed atomic writes in `electron/file-system.cjs`; attachment lifecycle queue and audit event, `mission_object_versions`, archive attachment enumeration, collision-safe archive names, and finalization in `electron/mission-store.cjs`; `electron/mission-evidence-version-store.cjs`; `src/infrastructure/marker-attachment-store/electron-marker-attachment-store.ts`; `src/features/markers/start-marker-runtime.ts` | `tests/unit/electron-file-system.test.ts`; `tests/unit/electron-marker-attachment-store.test.ts`; `tests/unit/start-marker-runtime.test.ts`; `tests/unit/electron-mission-store.test.ts`; `tests/unit/electron-mission-evidence-versioning.test.ts` | `T2`: real filesystem/SQLite tests prove sanitized UUID paths, same-name collision preservation, attachment-before-Finish ordering, shutdown join, marker version retention, and current-archive inclusion of superseded/retired paths with collision-safe names. Run `33260131951` did not drive those archive/attachment controls through the packaged artifact. No forced power-loss at the file-write/event boundary, file-content hash in the attachment identity, missing-file repair workflow, encrypted archive, or restore equality is proved. | [DON-277](https://linear.app/donal-oc/issue/DON-277), archive successor [DON-248](https://linear.app/donal-oc/issue/DON-248), [DON-252](https://linear.app/donal-oc/issue/DON-252), [DON-253](https://linear.app/donal-oc/issue/DON-253) | `evidence-tier-gap` | UUID paths prevent the confirmed overwrite mechanism, but custody is path/event based rather than a separately hashed attachment manifest and archive restore remains unimplemented. Preserve original source files, stop on missing attachment/archive errors, and add power-loss/hash/restore equality in the archive successor. |

## 9. Settings, credentials, diagnostics, and privacy

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `SEC-001` — corrupt settings/credential state prevents safe startup | The shell or tracking may become unavailable, or unsafe configuration may be used silently. | `CLAUDE.md` fail-visible rules. **Invariant:** missing/bad credentials disable tracking, not the shell; malformed settings fail visibly with actionable recovery; provider URLs with embedded credentials are rejected; writes are atomic. | `electron/settings-store.cjs`; runtime boot call path through `src/features/runtime/start-app-runtime.ts` and `src/features/runtime/bootstrap-app-runtime.ts`; actionable failed-boot shell and support export in `src/App.tsx`; Electron support export in `electron/runtime-files.cjs` | Existing corrupt/legacy credential, URL rejection, atomic write, clear/re-entry, boot-state, and generic fault-shell tests; isolated joined red evidence in `scripts/assurance/war-04/settings-privacy/settings-startup-red.test.ts` | `T2`: WAR-04 confirms that a non-`ENOENT` credential read fault aborts runtime bootstrap, a fault after credential commit cross-pairs provider settings and secret, and concurrent saves expose both an identical-timestamp rejection and a controlled unique-temp cross-file interleaving where both writes fulfil but the final pair is mixed. The concurrency oracle qualifies an enclosing full-save queue; transaction/generation or narrower publication-lock repairs require an explicit WAR-11 commit-admission seam and red-to-green proof. Corrupt settings also make startup-fault support export fail on the same settings re-read. Missing/garbage credentials, bounded interval normalization, visible malformed-settings failure, and credential-write-first failure remain cleared. No packaged or cross-platform permission/concurrency matrix was run. | [DON-177](https://linear.app/donal-oc/issue/DON-177), [DON-237](https://linear.app/donal-oc/issue/DON-237) | `unenforced-invariant` | Confirmed findings `WAR04-SET-01..03` and escape analysis are in [WAR-04 findings](findings/WAR-04.md). Preserve the profile and fault evidence; do not silently substitute settings. Repair as one WAR-11 settings/startup-consistency cluster before WAR-12 exact-artifact qualification. |
| `SEC-002` — local plaintext provider credentials are exposed | Host compromise or another local account can reveal tracking credentials. | Trusted-team-machine threat model recorded under DON-177. **Invariant:** secret stays out of normal settings/diagnostics; credential file gets best-effort mode `0600`; embedded URL credentials are rejected; clear/re-entry works. | `electron/settings-store.cjs` | Credential separation, best-effort `0600`, legacy migration, corrupt secret, URL-userinfo rejection, and clear cases in `tests/unit/electron-settings-store.test.ts`; URL-credential red evidence in `scripts/assurance/war-04/settings-privacy/diagnostics-privacy-red.test.ts` | The accepted plaintext trusted-machine residual and its historical `T3` packaged bad-secret evidence are unchanged. WAR-04 `T2` clears raw/encoded URL userinfo but confirms that raw/encoded `session` query and fragment placeholder credentials are accepted and exported; that separate privacy breach is reconciled under `SEC-003`/`WAR04-PRV-02`, not silently included in the plaintext residual. | [DON-177](https://linear.app/donal-oc/issue/DON-177) | `accepted-residual` | Continue relying on host account security, file permissions, and rapid credential clear/rotation for the explicitly accepted plaintext-at-rest risk. Do not treat that acceptance as permission for URL query/fragment credentials or diagnostic disclosure; repair `WAR04-PRV-02` under the WAR-11 diagnostics cluster. Revisit the threat model if deployment moves beyond trusted team machines. |
| `SEC-003` — diagnostics/support export leaks secrets, coordinates, or paths | Sharing support evidence can expose credentials, casualty/team locations, licensed-map details, or host identity. | `CLAUDE.md` privacy rules; diagnostic allow-list policy. **Invariant:** recursive sanitization removes authorization/secrets/provider URL credentials, precise coordinates, and private/userData paths while retaining bounded actionable evidence. | renderer and main-process ingestion in `src/features/diagnostics/diagnostic-event-log.ts` and `electron/main.cjs`; recursive main sanitizer/log/export path in `electron/diagnostic-sanitizer.cjs`, `electron/runtime-log.cjs`, and `electron/runtime-files.cjs`; `electron/crash-log.cjs`; `src/features/diagnostics/diagnostics-model.ts`; `src/features/diagnostics/start-diagnostics-runtime.ts`; `src/components/diagnostics-workspace.tsx`; `src/infrastructure/support-report/tauri-support-report-store.ts` | Existing diagnostic-event/model/runtime-files/runtime-log/crash-log/support-store and diagnostics browser tests; isolated joined red evidence in `scripts/assurance/war-04/settings-privacy/diagnostics-privacy-red.test.ts` | Existing historical `T3` evidence still proves canonical recursive secret/path redaction, top-level renderer coordinate keys, and a packaged sanitized-export subset. WAR-04 `T2` confirms three missed joins: Copy Report bypasses Electron profile-root redaction, persisted raw/encoded `session` query and fragment credentials reach copied/support output, and nested renderer plus direct-main coordinates survive durable log/report export. Current inspected renderer producers use primitive fields with no nested object, while inspected direct-main producers emit no coordinates or host identity; current operator-triggered disclosure through those two synthetic shapes is therefore unproved. | [DON-226](https://linear.app/donal-oc/issue/DON-226), [DON-237](https://linear.app/donal-oc/issue/DON-237) | `unenforced-invariant` | Confirmed findings `WAR04-PRV-01..03`, hypotheses for representation-specific free-form text, and escape analysis are in [WAR-04 findings](findings/WAR-04.md). Operators must preview every copied/file report and screenshot. Repair one recursive allow-list/canonicalization contract across Copy Report, renderer, main log, support, and incident output under DON-226/DON-237. |
| `SEC-004` — mutable local roster is mistaken for privileged authorization | A person with host/Settings access can add their own name, change mission-affecting settings, acknowledge permanent evidence loss, or unlock a finalized mission; operator accountability and mission truth can then be misattributed. | `docs/settings-access-control-decision.md`; operator manual Settings warning; [DON-199](https://linear.app/donal-oc/issue/DON-199). **Invariant:** the current admin/coordinator roster is only a local workflow and audit guard, never strong authentication; the trusted team machine is the current trust boundary. Before deployment expands beyond that bounded model, protected settings require a privileged mode, finalized-mission unlock authority is mission-scoped rather than derived only from a mutable current roster, and every privileged action/recovery path is audited without stranding authorized operators. | Mutable roster/settings in `src/components/settings-workspace.tsx`, `electron/settings-store.cjs`, and `electron/main.cjs`; acknowledgement/unlock UI and orchestration in `src/features/mission/use-mission-control-view-model.ts`, `src/components/mission-evidence-loss-dialog.tsx`, `src/components/mission-control-panel.tsx`, and `src/features/mission/start-mission-governance-runtime.ts`; configured-roster checks and audit writes in `electron/mission-store.cjs` | Ordinary Settings open/save behavior, without a privileged-access assertion, in `tests/unit/settings-workspace.test.ts` and `tests/e2e/settings.spec.ts`; configured-roster persistence in `tests/unit/electron-settings-store.test.ts`; acknowledgement/unlock selection, forwarding, configured-roster allow/deny, and durable audit behavior in `tests/unit/use-mission-control-view-model.test.ts`, `tests/unit/start-mission-governance-runtime.test.ts`, `tests/unit/electron-mission-store.test.ts`, and `tests/e2e/mission.spec.ts` | `T2`: deterministic Settings/SQLite integration and browser-harness workflows prove that the mutable configured roster gates acknowledgement/unlock and records the selected name/reason. They do not authenticate the actor, prevent self-grant through Settings, preserve mission-scoped historical authority, prove recovery when the roster changes, or turn browser `localStorage` into Electron identity evidence. | Decision [DON-199](https://linear.app/donal-oc/issue/DON-199); privileged guard [DON-219](https://linear.app/donal-oc/issue/DON-219); mission unlock authority [DON-220](https://linear.app/donal-oc/issue/DON-220); recovery/admin lifecycle [DON-221](https://linear.app/donal-oc/issue/DON-221) | `accepted-residual` | Anyone who already controls the trusted machine/Settings can self-add; the durable audit detects attribution after the fact but does not authenticate it. Current acceptance is limited to trusted team machines. Use OS-account/physical custody and a two-person or off-app authorization check for evidence-loss acknowledgement and unlock; preserve audit records; do not represent the roster as multi-user security. Complete DON-219/DON-220/DON-221 before broader deployment or final qualification. |

## 10. Packaging and platform divergence

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `PKG-001` — packaged app becomes noninteractive over time | Operators can no longer inspect devices or control the mission during a prolonged incident. | Field report/provenance in DON-247; ADR no-unbounded-main rule. **Invariant:** every supported package remains interactive for multi-day missions; click delivery, React state, main IPC, close path, event-loop latency, and mission-size work stay bounded. | Runtime in `electron/main.cjs` and `src/App.tsx`; qualification/capture boundaries in `scripts/electron-tracking-soak.mjs`, `build/electron-tracking-soak-lib.js`, `electron/tracking-soak-validation.cjs`, `field-tools/sartracker-linux-hang-collector.cjs`, and `field-tools/sartracker-linux-hang-collector-lib.cjs`; operator capture procedure in `docs/releases/beta12-mint-hang-capture-runbook.md` | `tests/unit/electron-tracking-soak-lib.test.ts`; `tests/unit/electron-tracking-soak-exact-action.test.ts`; `tests/unit/electron-tracking-soak-validation.test.ts`; `tests/unit/linux-hang-collector.test.ts` | `T5` is contradictory: PCLinuxOS AppImage stayed responsive for 218 hours; Mint `.deb` stopped accepting app actions around 182 hours while its clock continued. Different profiles prevent causal attribution. Deterministic T4 package evidence does not close that field split. | [DON-247](https://linear.app/donal-oc/issue/DON-247) | `evidence-tier-gap` | Capture the report-only collector before restart/profile reset and retain an independent operational fallback. Run the same Mint profile/workload as AppImage vs `.deb`; do not call the root cause proven until that comparison settles. |
| `PKG-002` — support policy overstates shipped/qualified platforms | Operators may rely on a package/platform or runtime that the current workflow does not build, support, or qualify. | `docs/desktop-runtime-support-policy.md`; Beta Release Safety Protocol. **Invariant:** every advertised tier maps to a maintained runtime, current build, and exact-artifact matrix; absent lanes are explicitly unsupported/unqualified. | `electron-builder.json`; `.github/workflows/electron-release.yml`; `docs/desktop-runtime-support-policy.md`; `docs/assurance/findings/WAR-04B.md` | Platform/job/asset expectations in `tests/unit/electron-release-safety.test.ts` | `T0/T1`: exact-base workflow/test inspection shows Linux x86-64 AppImage/`.deb`; the policy still describes Windows as CI-built/gated and macOS arm64 as Tier 1. WAR-04B also proves the shipped Electron 40 line reached EOL on 2026-06-30. These are documentation/support-control gaps, not proof of an unavailable-platform runtime defect. | [DON-254](https://linear.app/donal-oc/issue/DON-254), [DON-255](https://linear.app/donal-oc/issue/DON-255), platform parent [DON-25](https://linear.app/donal-oc/issue/DON-25) | `unenforced-invariant` | Release summaries/asset allow-lists are the current detection boundary. Advertise only exact qualified artifacts on a maintained runtime. Reconcile the policy after the controlled runtime upgrade and run real-machine platform gates before widening claims. |

## 11. Dependency and release integrity

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `REL-001` — published bytes differ from reviewed/smoked bytes | Testers can receive an unqualified or altered installer. | Beta Release Safety Protocol. **Invariant:** tag, version, gated commit/checkout, draft body, installer identities, `SHA256SUMS`, GitHub asset metadata, and fresh downloads agree; mutation/extra assets block publication; after publication the tag/assets must be technically immutable. | `.github/workflows/electron-release.yml`; `scripts/electron-release-publish.mjs`; `build/electron-release-lib.js`; `scripts/beta-verify.mjs`; `docs/assurance/findings/WAR-04B.md` | `tests/unit/electron-release-safety.test.ts`; `tests/unit/beta-verify-lib.test.ts` | `T4`: beta.12.11's tag/run/body/assets/API digests and one freshly inspected `.deb` agreed on 2026-08-30, and the publisher's double-observation guards are fail-closed. Live release state is `immutable:false`, no tag ruleset exists, the tag is unsigned, and the publisher is not a server-enforced only-path. This is no evidence of historical mutation and no audited-base artifact proof. | [DON-254](https://linear.app/donal-oc/issue/DON-254), [DON-255](https://linear.app/donal-oc/issue/DON-255) | `unenforced-invariant` | Keep releases draft and retain the prior artifact if identity diverges. Before the next publication, enable immutable releases and a compatible tag rule, retain the guarded publisher, and qualify the exact final candidate under DON-254/DON-255. |
| `REL-002` — dependency/native/build input is vulnerable or incompatible | A compromised/incompatible runtime can break launch, SQLite, mapping, or packaging. | Runtime support policy; `CLAUDE.md` native/release rules. **Invariant:** npm graph is lockfile-integrity-bound; shipped runtime and builder-generated code are maintained; target native module is rebuilt and inspected; vulnerability reachability is evaluated; CI actions/toolchains are reviewably pinned. | `package.json`; `package-lock.json`; `scripts/electron-package.mjs`; `electron-builder.json`; native ELF check in `.github/workflows/electron-release.yml`; `docs/assurance/findings/WAR-04B.md` | Native rebuild/failure cases in `tests/unit/verification-scripts.test.ts`; release workflow safety tests in `tests/unit/electron-release-safety.test.ts` | `T0/T1`: lock integrity and native controls are present. WAR-04B confirms Electron `40.10.0` is EOL and `electron-builder@26.0.12 -> app-builder-lib@26.0.12` generates an affected AppImage; vulnerable archive tooling is reached in the build path. The production-only audit is clean but omits those release-bearing dev dependencies. No compromise or current package run was claimed. | [DON-146](https://linear.app/donal-oc/issue/DON-146), release [DON-255](https://linear.app/donal-oc/issue/DON-255) | `unenforced-invariant` | Hold the exact-base release. Upgrade builder first, qualify `better-sqlite3` independently, then move Electron to a supported line and refresh remaining build inputs. DON-146's upstream blocker is cleared, but no local upgrade is implemented. |
| `REL-003` — unsigned artifacts and rollback are misused | Installer origin cannot be OS-authenticated beyond hashes; an update at the wrong time can disrupt a mission. | Runtime support policy update/rollback sections. **Invariant:** unsigned builds stay internal/shadow-only; checksums are independently verified; auto-update remains disabled; prior qualified artifact is retained; no mission machine is updated immediately before/during operations. | `.github/workflows/electron-release.yml`; `scripts/electron-release-publish.mjs`; `build/electron-release-lib.js`; `docs/desktop-runtime-support-policy.md`; `docs/assurance/findings/WAR-04B.md` | `tests/unit/electron-release-safety.test.ts`; there is deliberately no signing test because signing is absent | `T4`: historical hash/fresh-byte verification. WAR-04B confirms no signing, notarization, artifact/release attestation, or packaged SBOM; checksums are co-located with mutable unsigned assets. This remains an explicit internal-beta residual, not stable-field approval. | [DON-254](https://linear.app/donal-oc/issue/DON-254), [DON-255](https://linear.app/donal-oc/issue/DON-255) | `accepted-residual` | Use manual checksum verification, quiet-period install, an independent primary workflow, and retained rollback artifact. Add immutable release protection, attested exact installers and a packaged SBOM before a stable-field claim; signing remains a separate decision and qualification seam. |
| `REL-004` — direct repository mutation bypasses review, checks, or secret controls | Unreviewed or untested code—or a committed credential—can reach `master` and later a life-safety build without a technical prevention boundary. | `CLAUDE.md` Finish and Beta Release Safety Protocols; exact-head review rules in `docs/breadcrumb-programme-execution-policy.md`. **Invariant:** `master` rejects direct/unreviewed mutation; required exact-head review and status checks gate merge; secret scanning and push protection reject credentials; release guards remain a separate final boundary. | External GitHub branch-protection, ruleset, Actions, and security-analysis settings for `master`; `.github/workflows/electron-linux-validation.yml`; `.github/workflows/electron-release.yml`; `docs/assurance/findings/WAR-04B.md` | `tests/unit/electron-release-safety.test.ts`; `tests/unit/release-smoke-safety.test.ts` | `T0/T1`: live 2026-08-30 inspection, revalidated on conflict-refresh `master` `341d95add5a7eceb6db506a2afd0ea70cb1fc944`, returned unprotected `master`, zero rulesets/effective rules, zero exact-master checks/statuses, disabled secret/Dependabot/code scanning visibility, mutable action tags, and an unrestricted action allowlist. PR validation also omits the release workflow and packaged `field-tools/**`. Alert counts are unobservable, not zero. | [DON-254](https://linear.app/donal-oc/issue/DON-254), [DON-255](https://linear.app/donal-oc/issue/DON-255) | `unenforced-invariant` | First add an always-emitted release-policy gate, then require a PR, one independent latest-head approval, resolved conversations and the named check; block force-push/deletion; pin/allowlist actions; enable security visibility and triage before gating. Re-read live settings at release time. |

## 12. Operator warning, training, and fallback

| ID / hazard | Consequence and safety/operator impact | Authority and exact invariant | Controlling production boundary | Existing tests opened and inspected | Existing evidence and proof limit | Existing Linear owner | Gap | Residual risk, detection/fallback, prerequisite/follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `OPS-001` — warning/demo evidence is mistaken for field readiness | Operators may over-trust browser or beta behavior, miss a degraded warning, or lack a rehearsed fallback under pressure. | `public/manual/index.html`; `CLAUDE.md` proof-tier rules; final parity gate. **Invariant:** every degraded/unavailable state gives a plain warning and action; browser harness/internal beta is never presented as operational qualification; shadow use retains an independent primary source and rehearsed stop/revert/recovery path. | `src/components/tracking-status-panel.tsx`; `src/components/coverage-status-panel.tsx`; `src/features/persistence/autosave-status-store.ts`; `src/features/runtime/runtime-boot-store.ts`; `src/features/map/field-readiness-checklist.ts`; `src/components/diagnostics-workspace.tsx`; known console-only exception in `src/features/map/map-style-sync.ts`; `public/manual/index.html` | `tests/unit/tracking-status-panel.test.ts`; `tests/unit/coverage-status-panel.test.ts`; `tests/unit/mission-autosave.test.ts`; `tests/unit/runtime-boot-store.test.ts`; `tests/unit/field-readiness-checklist.test.ts`; `tests/unit/map-style-sync.test.ts`; `tests/e2e/diagnostics.spec.ts` | `T2`: documentation plus unit and rendered-browser evidence for the named warning states. It cannot prove the “every state” claim because `EVD-003` remains console-only; no independently reviewed visual or local package is cited, and comprehension, team-led training, fallback drills, and field behavior are unproved. | Final acceptance [DON-6](https://linear.app/donal-oc/issue/DON-6), warning owner [DON-264](https://linear.app/donal-oc/issue/DON-264), map owners [DON-7](https://linear.app/donal-oc/issue/DON-7), [DON-115](https://linear.app/donal-oc/issue/DON-115), [DON-144](https://linear.app/donal-oc/issue/DON-144) | `unenforced-invariant` | Persistent overlay-sync failure is detectable only in developer console or by manual list-versus-map comparison. Until DON-264 adds and clears a durable operator warning, treat source records as truth, retain an independent operational source, preserve diagnostics/profile, and stop/revert on uncertainty; then run a team-led field scorecard before DON-6 closure. |

## Register disposition

This reconciled baseline contains confirmed controls, lower-tier evidence,
explicit residuals, and the remaining archive rewrite-pending boundary. It does
not certify the application for field use. The five absolute blockers remain
release-blocking regardless of the primary gap label on an individual row.

The PR5 reconciliation is recorded against merge
`eec92812b783a795c093f37268b295dd2179a3af`; its exact-head Linux proof and
prior-head independent reviews retain the separate limits stated above. The
unmerged `DON-248`/`DON-252`/`DON-253` successor remains the prerequisite for
streamed encrypted archive/restore resilience work. Its active mutable
implementation is outside this snapshot; merged PR5 custody controls, current
archive tests, and ancestry cannot substitute for the exact merged successor
and its own qualification.
