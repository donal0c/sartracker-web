# A-R20/A-R21 GitHub follow-up

Both comments were present on the older GitHub review and were missed by the
pasted-review intake. They are accepted and repaired, not dismissed as stale.
Source red and actual packaged omission red are retained here.

Final local verification: **427 files / 4,377 tests**, lint, TypeScript and
production build/bundle budgets. The repeated native omission/rejection probe
uses actual Settings Save Connect and Devices Reconnect, a synthetic HTTP
provider and the real Electron/SQLite runtime. After an empty successful
replacement, connection is online but the retained coordinate is **Unknown /
Last known**. The next automatic poll restores **Online / Live** with a fresh
coordinate before old-response release (measured 5 ms from fresh response release).
The old fix persists; four rejection records are durable; selected warning
remains visible; two later polls resume. Custody is healthy with no pending,
corrupt or conflicting observations. Both screenshots were inspected.

`local-source-binding.json` binds the precommit unsigned macOS package ASAR and
30 source/test/manual/probe blobs to the subsequent source commit. No operational
profile is used. Prior stationary policy/browser/renderer inputs are unchanged;
their bounded three-flow / 78.5 ms evidence in `../remediation` is reused.

The full source run separately logged a GPX heartbeat diagnostic of 278.39 ms;
its actual current-write gate passed at 107.47 ms. That diagnostic is preserved
in `source.log`, without claiming all heartbeat samples were under 200 ms.
New exact-head Linux CI remains required for this changed application.
